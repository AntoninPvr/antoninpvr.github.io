<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Antonin Pivard – 3D Printing</title><link>https://antoninpvr.github.io/tags/3d-printing/</link><description>Recent content in 3D Printing on Antonin Pivard</description><generator>Hugo -- gohugo.io</generator><language>en</language><lastBuildDate>Tue, 23 Apr 2024 12:10:00 +0200</lastBuildDate><atom:link href="https://antoninpvr.github.io/tags/3d-printing/index.xml" rel="self" type="application/rss+xml"/><item><title>MarcoX</title><link>https://antoninpvr.github.io/hardware/marco/marcox/</link><pubDate>Tue, 23 Apr 2024 12:10:00 +0200</pubDate><guid>https://antoninpvr.github.io/hardware/marco/marcox/</guid><description>
&lt;img src="https://antoninpvr.github.io/hardware/marco/marcox/featured_hu_7cc3a49a2a6a06d3.jpg" width="640" height="455"/>&lt;div class="hextra-cards hx:mt-4 hx:gap-4 hx:grid not-prose"
style="--hextra-cards-grid-cols: 3;">&lt;a
class="hextra-card hx:relative hx:group hx:flex hx:flex-col hx:justify-start hx:overflow-hidden hx:rounded-lg hx:border hx:border-gray-200 hx:text-current hx:no-underline hx:dark:shadow-none hx:hover:shadow-gray-100 hx:dark:hover:shadow-none hx:shadow-gray-100 hx:active:shadow-sm hx:active:shadow-gray-200 hx:transition-all hx:duration-200 hx:hover:border-gray-300 hx:bg-transparent hx:shadow-xs hx:dark:border-neutral-800 hx:hover:bg-slate-50 hx:hover:shadow-md hx:dark:hover:border-neutral-700 hx:dark:hover:bg-neutral-900"href="https://github.com/AntoninPvr/MarcoX"
target="_blank" rel="noreferrer">
&lt;span class="hextra-card-icon hx:flex hx:font-semibold hx:items-start hx:gap-2 hx:pt-4 hx:px-4 hx:text-gray-700 hx:hover:text-gray-900 hx:dark:text-neutral-200 hx:dark:hover:text-neutral-50">&lt;svg fill="currentColor" viewBox="3 3 18 18">
&lt;path d="M12 3C7.0275 3 3 7.12937 3 12.2276C3 16.3109 5.57625 19.7597 9.15374 20.9824C9.60374 21.0631 9.77249 20.7863 9.77249 20.5441C9.77249 20.3249 9.76125 19.5982 9.76125 18.8254C7.5 19.2522 6.915 18.2602 6.735 17.7412C6.63375 17.4759 6.19499 16.6569 5.8125 16.4378C5.4975 16.2647 5.0475 15.838 5.80124 15.8264C6.51 15.8149 7.01625 16.4954 7.18499 16.7723C7.99499 18.1679 9.28875 17.7758 9.80625 17.5335C9.885 16.9337 10.1212 16.53 10.38 16.2993C8.3775 16.0687 6.285 15.2728 6.285 11.7432C6.285 10.7397 6.63375 9.9092 7.20749 9.26326C7.1175 9.03257 6.8025 8.08674 7.2975 6.81794C7.2975 6.81794 8.05125 6.57571 9.77249 7.76377C10.4925 7.55615 11.2575 7.45234 12.0225 7.45234C12.7875 7.45234 13.5525 7.55615 14.2725 7.76377C15.9937 6.56418 16.7475 6.81794 16.7475 6.81794C17.2424 8.08674 16.9275 9.03257 16.8375 9.26326C17.4113 9.9092 17.76 10.7281 17.76 11.7432C17.76 15.2843 15.6563 16.0687 13.6537 16.2993C13.98 16.5877 14.2613 17.1414 14.2613 18.0065C14.2613 19.2407 14.25 20.2326 14.25 20.5441C14.25 20.7863 14.4188 21.0746 14.8688 20.9824C16.6554 20.364 18.2079 19.1866 19.3078 17.6162C20.4077 16.0457 20.9995 14.1611 21 12.2276C21 7.12937 16.9725 3 12 3Z">&lt;/path>
&lt;/svg>
MarcoX&lt;/span>
&lt;div class="hextra-card-subtitle hx:line-clamp-3 hx:text-sm hx:font-normal hx:text-gray-500 hx:dark:text-gray-400 hx:px-4 hx:mb-4 hx:mt-2">MarcoX is the new version of Marco payment system for Télécom Physique Strasbourg. This repo focus on hardware. All parts are designed to be easily print or laser cut by begginers.&lt;/div>&lt;div class="hextra-badge hextra-card-tag">
&lt;div class="hx:inline-flex hx:gap-1 hx:items-center hx:rounded-full hx:px-2.5 hx:leading-6 hx:text-[.65rem] hx:border hx:text-gray-600 hx:bg-gray-100 hx:dark:bg-neutral-800 hx:dark:text-neutral-200 hx:border-gray-200 hx:dark:border-neutral-700">&lt;svg height=12 xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" aria-hidden="true">&lt;path stroke-linecap="round" stroke-linejoin="round" d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z"/>&lt;/svg>v8.2.1&lt;/div>
&lt;/div>
&lt;/a>
&lt;/div>
&lt;hr>
&lt;p>MarcoX is the new version of the original Marco project based on Raspberry PI and old style resistive screen. This new version is based on Orange PI zero 2W with a 7&amp;quot; capacitive touch screen and aims to be more reliable and easier to replicate.&lt;/p>
&lt;p>Software was also modernized and is available here :
&lt;div class="hextra-cards hx:mt-4 hx:gap-4 hx:grid not-prose"
style="--hextra-cards-grid-cols: 3;">&lt;a
class="hextra-card hx:relative hx:group hx:flex hx:flex-col hx:justify-start hx:overflow-hidden hx:rounded-lg hx:border hx:border-gray-200 hx:text-current hx:no-underline hx:dark:shadow-none hx:hover:shadow-gray-100 hx:dark:hover:shadow-none hx:shadow-gray-100 hx:active:shadow-sm hx:active:shadow-gray-200 hx:transition-all hx:duration-200 hx:hover:border-gray-300 hx:bg-transparent hx:shadow-xs hx:dark:border-neutral-800 hx:hover:bg-slate-50 hx:hover:shadow-md hx:dark:hover:border-neutral-700 hx:dark:hover:bg-neutral-900">
&lt;span class="hextra-card-icon hx:flex hx:font-semibold hx:items-start hx:gap-2 hx:pt-4 hx:px-4 hx:text-gray-700 hx:hover:text-gray-900 hx:dark:text-neutral-200 hx:dark:hover:text-neutral-50">&lt;svg fill="currentColor" viewBox="3 3 18 18">
&lt;path d="M12 3C7.0275 3 3 7.12937 3 12.2276C3 16.3109 5.57625 19.7597 9.15374 20.9824C9.60374 21.0631 9.77249 20.7863 9.77249 20.5441C9.77249 20.3249 9.76125 19.5982 9.76125 18.8254C7.5 19.2522 6.915 18.2602 6.735 17.7412C6.63375 17.4759 6.19499 16.6569 5.8125 16.4378C5.4975 16.2647 5.0475 15.838 5.80124 15.8264C6.51 15.8149 7.01625 16.4954 7.18499 16.7723C7.99499 18.1679 9.28875 17.7758 9.80625 17.5335C9.885 16.9337 10.1212 16.53 10.38 16.2993C8.3775 16.0687 6.285 15.2728 6.285 11.7432C6.285 10.7397 6.63375 9.9092 7.20749 9.26326C7.1175 9.03257 6.8025 8.08674 7.2975 6.81794C7.2975 6.81794 8.05125 6.57571 9.77249 7.76377C10.4925 7.55615 11.2575 7.45234 12.0225 7.45234C12.7875 7.45234 13.5525 7.55615 14.2725 7.76377C15.9937 6.56418 16.7475 6.81794 16.7475 6.81794C17.2424 8.08674 16.9275 9.03257 16.8375 9.26326C17.4113 9.9092 17.76 10.7281 17.76 11.7432C17.76 15.2843 15.6563 16.0687 13.6537 16.2993C13.98 16.5877 14.2613 17.1414 14.2613 18.0065C14.2613 19.2407 14.25 20.2326 14.25 20.5441C14.25 20.7863 14.4188 21.0746 14.8688 20.9824C16.6554 20.364 18.2079 19.1866 19.3078 17.6162C20.4077 16.0457 20.9995 14.1611 21 12.2276C21 7.12937 16.9725 3 12 3Z">&lt;/path>
&lt;/svg>
GitHub Repo&lt;/span>
&lt;div class="hextra-card-subtitle hx:line-clamp-3 hx:text-sm hx:font-normal hx:text-gray-500 hx:dark:text-gray-400 hx:px-4 hx:mb-4 hx:mt-2">GitHub repository&lt;/div>&lt;div class="hextra-badge hextra-card-tag">
&lt;div class="hx:inline-flex hx:gap-1 hx:items-center hx:rounded-full hx:px-2.5 hx:leading-6 hx:text-[.65rem] hx:border hx:text-gray-600 hx:bg-gray-100 hx:dark:bg-neutral-800 hx:dark:text-neutral-200 hx:border-gray-200 hx:dark:border-neutral-700">&lt;svg height=12 xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" aria-hidden="true">&lt;path stroke-linecap="round" stroke-linejoin="round" d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z"/>&lt;/svg>no-release&lt;/div>
&lt;/div>
&lt;/a>
&lt;/div>
&lt;/p>
&lt;p>Marco is a project that aims to create a payment terminal for a school BDE (French equivalent of Student Union). It is based on a RFID card system. The terminal is used by the students to pay for their meals or drinks. The terminal is connected to a self-hosted server that manages the accounts of the students. This terminal act as cash register.&lt;/p>
&lt;p>This project is related to InsidePSBS app. Students can check their account and history on this mobile app available on IOS and Android. &lt;a href="https://github.com/info-telecom-strasbourg/InsidePSBS"target="_blank" rel="noopener">https://github.com/info-telecom-strasbourg/InsidePSBS&lt;/a> (Reserved to TPS and ESBS students)&lt;/p>
&lt;p>&lt;figure>
&lt;img src="https://github.com/AntoninPvr/MarcoX/blob/main/img/render/full.JPG?raw=true" title="Front view" alt="Marco slim front" loading="lazy" />
&lt;figcaption>Front view&lt;/figcaption>
&lt;/figure>&lt;/p>
&lt;p>&lt;figure>
&lt;img src="https://github.com/AntoninPvr/MarcoX/blob/main/img/render/frame_bellow.JPG?raw=true" title="Internal view, with electrical components exposed" alt="“Internal view, with electrical components exposed”" loading="lazy" />
&lt;figcaption>Internal view, with electrical components exposed&lt;/figcaption>
&lt;/figure>&lt;/p>
&lt;h2>Specifications&lt;span class="hx:absolute hx:-mt-20" id="specifications">&lt;/span>
&lt;a href="#specifications" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ul>
&lt;li>&lt;strong>Screen&lt;/strong>: 7&amp;quot; capacitive touch screen&lt;/li>
&lt;li>&lt;strong>RFID reader&lt;/strong>: 125kHz compatible&lt;/li>
&lt;li>&lt;strong>Power supply&lt;/strong>: USB-C 5V 3A&lt;/li>
&lt;li>&lt;strong>Battery&lt;/strong>: Support power bank up to 20mm thickness&lt;/li>
&lt;li>&lt;strong>SBC&lt;/strong>: Orange PI zero 2W
&lt;ul>
&lt;li>SoC: Allwinner H618 quad-core Cortex-A53 @ 1.5GHz&lt;/li>
&lt;li>RAM: 1GB LPDDR4&lt;/li>
&lt;li>Wi-Fi: 802.11 ac (Wi-Fi 5)&lt;/li>
&lt;li>Bluetooth: 5.0&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>Dimensions&lt;/strong>:
&lt;ul>
&lt;li>Length: 188mm&lt;/li>
&lt;li>Width: 137mm&lt;/li>
&lt;li>Height: 71mm&lt;/li>
&lt;li>1.8L volume&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ul>
&lt;h2>Shopping list&lt;span class="hx:absolute hx:-mt-20" id="shopping-list">&lt;/span>
&lt;a href="#shopping-list" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;h3>Electronic parts&lt;span class="hx:absolute hx:-mt-20" id="electronic-parts">&lt;/span>
&lt;a href="#electronic-parts" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;ul>
&lt;li>
&lt;p>Orange PI zero 2W with 1GB RAM or more&lt;/p>
&lt;/li>
&lt;li>
&lt;p>Micro SD card 16GB ore more, U3 speed class or more&lt;/p>
&lt;/li>
&lt;li>
&lt;p>7&amp;quot; Waveshare touch screen or compatible with HDMI input&lt;/p>
&lt;/li>
&lt;li>
&lt;p>125kHz RFID reader&lt;/p>
&lt;/li>
&lt;li>
&lt;p>USB-C power supply&lt;/p>
&lt;/li>
&lt;li>
&lt;p>mini HDMI to HDMI 100mm cable: A2 to C1&lt;/p>
&lt;/li>
&lt;li>
&lt;p>USB-C to micro usb 100mm cable: W3R to T1B&lt;/p>
&lt;/li>
&lt;li>
&lt;p>USB-C pannel 17mm center distance mount connector 200mm&lt;/p>
&lt;/li>
&lt;li>
&lt;p>Power bank (optional)&lt;/p>
&lt;hr>
&lt;/li>
&lt;/ul>
&lt;h3>Mechanical parts&lt;span class="hx:absolute hx:-mt-20" id="mechanical-parts">&lt;/span>
&lt;a href="#mechanical-parts" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;ul>
&lt;li>
&lt;p>M3 screws 16mm flat-head x4&lt;/p>
&lt;/li>
&lt;li>
&lt;p>M3 screws 10mm x10&lt;/p>
&lt;/li>
&lt;li>
&lt;p>M3 inserts x14&lt;/p>
&lt;/li>
&lt;li>
&lt;p>6mm plywood for the enclosure&lt;/p>
&lt;hr>
&lt;/li>
&lt;/ul>
&lt;h3>Parts details&lt;span class="hx:absolute hx:-mt-20" id="parts-details">&lt;/span>
&lt;a href="#parts-details" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Part&lt;/th>
&lt;th>Image&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>Orange PI zero 2W&lt;/td>
&lt;td>&lt;img alt="Orange PI zero 2W" src="https://github.com/AntoninPvr/MarcoX/blob/main/img/orange_pi_zero_2w.jpg?raw=true" width="200">&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>Mini HDMI to HDMI 100mm cable: (A2 to C1). Large side of HDMI is on ribbon cable side. Mini HDMI large side is on ribbon connector side.&lt;/td>
&lt;td>&lt;img alt="HDMI to Micro HDMI adapter" src="https://github.com/AntoninPvr/MarcoX/blob/main/img/hdmi_to_micro_hdmi.jpg?raw=true" width="200">&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>USB-C to micro usb 100mm cable: W3R to T1B&lt;/td>
&lt;td>&lt;img alt="HDMI to Micro HDMI adapter" src="https://github.com/AntoninPvr/MarcoX/blob/main/img/usbc_to_micro_usb.jpg?raw=true" width="200">&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>USB-C pannel 17mm center distance mount connector 200mm&lt;/td>
&lt;td>&lt;img alt="HDMI to Micro HDMI adapter" src="https://github.com/AntoninPvr/MarcoX/blob/main/img/usbc_pannel_mount.jpg?raw=true" width="200">&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>Power bank 10000mAh (optional)&lt;/td>
&lt;td>&lt;img alt="Power bank" src="https://github.com/AntoninPvr/MarcoX/blob/main/img/power_bank.jpg?raw=true" width="200">&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h3>Screen&lt;span class="hx:absolute hx:-mt-20" id="screen">&lt;/span>
&lt;a href="#screen" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Screen is 7&amp;quot; Waveshare touch screen or compatible with HDMI input it can be a compatible one as long as it has the same connectors and the same size.&lt;/p>
&lt;p>&lt;figure>
&lt;img src="https://github.com/AntoninPvr/MarcoX/blob/main/img/screen_front.jpg?raw=true" title="7\&amp;#34; inch Screen" alt="“Front screen view”" loading="lazy" />
&lt;figcaption>7\&amp;#34; inch Screen&lt;/figcaption>
&lt;/figure>&lt;/p>
&lt;p>&lt;figure>
&lt;img src="https://github.com/AntoninPvr/MarcoX/blob/main/img/screen_back.jpg?raw=true" title="Screen PCB" alt="“Back screen PCB view”" loading="lazy" />
&lt;figcaption>Screen PCB&lt;/figcaption>
&lt;/figure>&lt;/p>
&lt;h2>3D printed parts&lt;span class="hx:absolute hx:-mt-20" id="3d-printed-parts">&lt;/span>
&lt;a href="#3d-printed-parts" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>All parts are in &lt;code>.stl&lt;/code> format. They are designed to be printed without support.&lt;/p>
&lt;h3>Printer settings&lt;span class="hx:absolute hx:-mt-20" id="printer-settings">&lt;/span>
&lt;a href="#printer-settings" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Tested settings, but feel free to adapt them to your printer and material.:&lt;/p>
&lt;ul>
&lt;li>Layer height: 0.2mm&lt;/li>
&lt;li>Infill: 20%&lt;/li>
&lt;li>Nozzle diameter: 0.4mm&lt;/li>
&lt;li>Material: PLA or PETG&lt;/li>
&lt;/ul>
&lt;h4>Screen frame&lt;span class="hx:absolute hx:-mt-20" id="screen-frame">&lt;/span>
&lt;a href="#screen-frame" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h4>&lt;div style="display: flex">
&lt;div>
&lt;p>&lt;strong>File:&lt;/strong> &lt;a href="https://github.com/AntoninPvr/MarcoX/blob/main/screen_frame.STL"target="_blank" rel="noopener">screen_frame.STL&lt;/a>&lt;/p>
&lt;p>This frame allow screen and top cover to be attached without overthickness and to secure enclosure and removable top cover.&lt;/p>
&lt;/div>
&lt;div id="./models/screen_frame.glb" style="width: 50%; height: 50%">
&lt;/div>
&lt;script type="importmap">
{
"imports": {
"three": "https://unpkg.com/three@0.161.0/build/three.module.js",
"three/addons/": "https://unpkg.com/three@0.161.0/examples/jsm/"
}
}
&lt;/script>
&lt;script type="module">
import * as THREE from 'three';
import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { MeshoptDecoder } from 'three/addons/libs/meshopt_decoder.module.js';
import { DRACOLoader } from 'three/addons/loaders/DRACOLoader.js'
let container;
let camera, scene, renderer, clock;
let mesh;
let rotate, controls;
init();
render();
function init() {
container = document.getElementById(".\/models\/screen_frame.glb");
container.onload = () => {
onWindowResize();
}
const aspectRatio = "1.3333";
const model_path = ".\/models\/screen_frame.glb";
const modelColor = "#3f4d52";
rotate = "true" === "true";
renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
renderer.setPixelRatio(window.devicePixelRatio);
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1;
renderer.outputColorSpace = THREE.SRGBColorSpace;
container.appendChild(renderer.domElement);
camera = new THREE.PerspectiveCamera(45, aspectRatio, 0.1, 1000);
camera.position.set(1, 0, 1);
camera.lookAt(0, 0, 0);
controls = new OrbitControls(camera, renderer.domElement);
controls.addEventListener('change', render);
controls.minDistance = 1;
controls.maxDistance = 10;
controls.target.set(0, 0, 0);
controls.update();
const environment = new RoomEnvironment(renderer);
const pmremGenerator = new THREE.PMREMGenerator(renderer);
scene = new THREE.Scene();
scene.environment = pmremGenerator.fromScene(environment).texture;
const ambientLight = new THREE.AmbientLight(0xffffff, 1);
scene.add(ambientLight);
const light1 = new THREE.PointLight( 0xffeeee, 50, 0);
light1.position.set( 0, 2, 1 );
light1.castShadow = true;
scene.add( light1 );
const loader = new GLTFLoader();
const dracoLoader = new DRACOLoader();
dracoLoader.setDecoderPath('/examples/jsm/libs/draco/');
loader.setMeshoptDecoder(MeshoptDecoder);
loader.setDRACOLoader(dracoLoader);
loader.load(model_path, function (gltf) {
const root = gltf.scene;
const box = new THREE.Box3().setFromObject(root);
const size = box.getSize(new THREE.Vector3());
const center = box.getCenter(new THREE.Vector3());
root.position.x += (root.position.x - center.x);
root.position.y += (root.position.y - center.y);
root.position.z += (root.position.z - center.z);
mesh = new THREE.Group();
mesh.add(root);
if (modelColor &amp;&amp; modelColor !== "") {
root.traverse((child) => {
if (child.isMesh) {
child.material.color.set(modelColor);
child.material.roughness = 0.8;
child.material.metalness = 0.2;
}
});
}
root.traverse((child) => {
if (child.isMesh) {
child.castShadow = true;
child.receiveShadow = true;
}
});
const maxSize = Math.max(size.x, size.y, size.z);
mesh.scale.set(1 / maxSize, 1 / maxSize, 1 / maxSize);
scene.add(mesh);
if ( rotate ) {
clock = new THREE.Clock();
renderer.setAnimationLoop( animate );
controls.addEventListener( 'start', () => rotate = false );
controls.addEventListener( 'end', () => rotate = true );
}
render();
});
window.addEventListener('resize', onWindowResize);
}
function onWindowResize() {
const aspectRatio = "1.3333";
camera.aspect = aspectRatio;
camera.updateProjectionMatrix();
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
render();
}
function render() {
renderer.render(scene, camera);
}
function animate() {
const delta = clock.getDelta();
const direction = "left";
if ( rotate &amp;&amp; mesh ) {
const ry = 0.5;
if (direction === "left") {
mesh.rotation.y -= delta * ry;
} else {
mesh.rotation.y += delta * ry;
}
render();
}
}
&lt;/script>
&lt;/div>
&lt;h4>Internal frame&lt;span class="hx:absolute hx:-mt-20" id="internal-frame">&lt;/span>
&lt;a href="#internal-frame" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h4>&lt;div style="display: flex">
&lt;div>
&lt;p>&lt;strong>File:&lt;/strong> &lt;a href="https://github.com/AntoninPvr/MarcoX/blob/main/internal_frame.STL"target="_blank" rel="noopener">internal_frame.STL&lt;/a>&lt;/p>
&lt;p>This frame is attached to the screen and is used to fix all other parts.&lt;/p>
&lt;p>&lt;em>Note: For this part adding support may improve print quality&lt;/em>&lt;/p>
&lt;/div>
&lt;div id="./models/internal_frame.glb" style="width: 50%; height: 50%">
&lt;/div>
&lt;script type="importmap">
{
"imports": {
"three": "https://unpkg.com/three@0.161.0/build/three.module.js",
"three/addons/": "https://unpkg.com/three@0.161.0/examples/jsm/"
}
}
&lt;/script>
&lt;script type="module">
import * as THREE from 'three';
import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { MeshoptDecoder } from 'three/addons/libs/meshopt_decoder.module.js';
import { DRACOLoader } from 'three/addons/loaders/DRACOLoader.js'
let container;
let camera, scene, renderer, clock;
let mesh;
let rotate, controls;
init();
render();
function init() {
container = document.getElementById(".\/models\/internal_frame.glb");
container.onload = () => {
onWindowResize();
}
const aspectRatio = "1.3333";
const model_path = ".\/models\/internal_frame.glb";
const modelColor = "#3f4d52";
rotate = "true" === "true";
renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
renderer.setPixelRatio(window.devicePixelRatio);
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1;
renderer.outputColorSpace = THREE.SRGBColorSpace;
container.appendChild(renderer.domElement);
camera = new THREE.PerspectiveCamera(45, aspectRatio, 0.1, 1000);
camera.position.set(1, 0, 1);
camera.lookAt(0, 0, 0);
controls = new OrbitControls(camera, renderer.domElement);
controls.addEventListener('change', render);
controls.minDistance = 1;
controls.maxDistance = 10;
controls.target.set(0, 0, 0);
controls.update();
const environment = new RoomEnvironment(renderer);
const pmremGenerator = new THREE.PMREMGenerator(renderer);
scene = new THREE.Scene();
scene.environment = pmremGenerator.fromScene(environment).texture;
const ambientLight = new THREE.AmbientLight(0xffffff, 1);
scene.add(ambientLight);
const light1 = new THREE.PointLight( 0xffeeee, 50, 0);
light1.position.set( 0, 2, 1 );
light1.castShadow = true;
scene.add( light1 );
const loader = new GLTFLoader();
const dracoLoader = new DRACOLoader();
dracoLoader.setDecoderPath('/examples/jsm/libs/draco/');
loader.setMeshoptDecoder(MeshoptDecoder);
loader.setDRACOLoader(dracoLoader);
loader.load(model_path, function (gltf) {
const root = gltf.scene;
const box = new THREE.Box3().setFromObject(root);
const size = box.getSize(new THREE.Vector3());
const center = box.getCenter(new THREE.Vector3());
root.position.x += (root.position.x - center.x);
root.position.y += (root.position.y - center.y);
root.position.z += (root.position.z - center.z);
mesh = new THREE.Group();
mesh.add(root);
if (modelColor &amp;&amp; modelColor !== "") {
root.traverse((child) => {
if (child.isMesh) {
child.material.color.set(modelColor);
child.material.roughness = 0.8;
child.material.metalness = 0.2;
}
});
}
root.traverse((child) => {
if (child.isMesh) {
child.castShadow = true;
child.receiveShadow = true;
}
});
const maxSize = Math.max(size.x, size.y, size.z);
mesh.scale.set(1 / maxSize, 1 / maxSize, 1 / maxSize);
scene.add(mesh);
if ( rotate ) {
clock = new THREE.Clock();
renderer.setAnimationLoop( animate );
controls.addEventListener( 'start', () => rotate = false );
controls.addEventListener( 'end', () => rotate = true );
}
render();
});
window.addEventListener('resize', onWindowResize);
}
function onWindowResize() {
const aspectRatio = "1.3333";
camera.aspect = aspectRatio;
camera.updateProjectionMatrix();
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
render();
}
function render() {
renderer.render(scene, camera);
}
function animate() {
const delta = clock.getDelta();
const direction = "right";
if ( rotate &amp;&amp; mesh ) {
const ry = 0.5;
if (direction === "left") {
mesh.rotation.y -= delta * ry;
} else {
mesh.rotation.y += delta * ry;
}
render();
}
}
&lt;/script>
&lt;/div>
&lt;h4>Card reader&lt;span class="hx:absolute hx:-mt-20" id="card-reader">&lt;/span>
&lt;a href="#card-reader" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h4>&lt;div style="display: flex">
&lt;div>
&lt;p>&lt;strong>File:&lt;/strong> &lt;a href="https://github.com/AntoninPvr/MarcoX/blob/main/card_reader.STL"target="_blank" rel="noopener">card_reader.STL&lt;/a>&lt;/p>
&lt;p>This mount is used to fix the RFID reader to the enclosure as close as possible to improve card detection.&lt;/p>
&lt;/div>
&lt;div id="./models/card_reader.glb" style="width: 50%; height: 50%">
&lt;/div>
&lt;script type="importmap">
{
"imports": {
"three": "https://unpkg.com/three@0.161.0/build/three.module.js",
"three/addons/": "https://unpkg.com/three@0.161.0/examples/jsm/"
}
}
&lt;/script>
&lt;script type="module">
import * as THREE from 'three';
import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { MeshoptDecoder } from 'three/addons/libs/meshopt_decoder.module.js';
import { DRACOLoader } from 'three/addons/loaders/DRACOLoader.js'
let container;
let camera, scene, renderer, clock;
let mesh;
let rotate, controls;
init();
render();
function init() {
container = document.getElementById(".\/models\/card_reader.glb");
container.onload = () => {
onWindowResize();
}
const aspectRatio = "1.3333";
const model_path = ".\/models\/card_reader.glb";
const modelColor = "#3f4d52";
rotate = "true" === "true";
renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
renderer.setPixelRatio(window.devicePixelRatio);
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1;
renderer.outputColorSpace = THREE.SRGBColorSpace;
container.appendChild(renderer.domElement);
camera = new THREE.PerspectiveCamera(45, aspectRatio, 0.1, 1000);
camera.position.set(1, 0, 1);
camera.lookAt(0, 0, 0);
controls = new OrbitControls(camera, renderer.domElement);
controls.addEventListener('change', render);
controls.minDistance = 1;
controls.maxDistance = 10;
controls.target.set(0, 0, 0);
controls.update();
const environment = new RoomEnvironment(renderer);
const pmremGenerator = new THREE.PMREMGenerator(renderer);
scene = new THREE.Scene();
scene.environment = pmremGenerator.fromScene(environment).texture;
const ambientLight = new THREE.AmbientLight(0xffffff, 1);
scene.add(ambientLight);
const light1 = new THREE.PointLight( 0xffeeee, 50, 0);
light1.position.set( 0, 2, 1 );
light1.castShadow = true;
scene.add( light1 );
const loader = new GLTFLoader();
const dracoLoader = new DRACOLoader();
dracoLoader.setDecoderPath('/examples/jsm/libs/draco/');
loader.setMeshoptDecoder(MeshoptDecoder);
loader.setDRACOLoader(dracoLoader);
loader.load(model_path, function (gltf) {
const root = gltf.scene;
const box = new THREE.Box3().setFromObject(root);
const size = box.getSize(new THREE.Vector3());
const center = box.getCenter(new THREE.Vector3());
root.position.x += (root.position.x - center.x);
root.position.y += (root.position.y - center.y);
root.position.z += (root.position.z - center.z);
mesh = new THREE.Group();
mesh.add(root);
if (modelColor &amp;&amp; modelColor !== "") {
root.traverse((child) => {
if (child.isMesh) {
child.material.color.set(modelColor);
child.material.roughness = 0.8;
child.material.metalness = 0.2;
}
});
}
root.traverse((child) => {
if (child.isMesh) {
child.castShadow = true;
child.receiveShadow = true;
}
});
const maxSize = Math.max(size.x, size.y, size.z);
mesh.scale.set(1 / maxSize, 1 / maxSize, 1 / maxSize);
scene.add(mesh);
if ( rotate ) {
clock = new THREE.Clock();
renderer.setAnimationLoop( animate );
controls.addEventListener( 'start', () => rotate = false );
controls.addEventListener( 'end', () => rotate = true );
}
render();
});
window.addEventListener('resize', onWindowResize);
}
function onWindowResize() {
const aspectRatio = "1.3333";
camera.aspect = aspectRatio;
camera.updateProjectionMatrix();
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
render();
}
function render() {
renderer.render(scene, camera);
}
function animate() {
const delta = clock.getDelta();
const direction = "left";
if ( rotate &amp;&amp; mesh ) {
const ry = 0.5;
if (direction === "left") {
mesh.rotation.y -= delta * ry;
} else {
mesh.rotation.y += delta * ry;
}
render();
}
}
&lt;/script>
&lt;/div>
&lt;h4>Antenna support&lt;span class="hx:absolute hx:-mt-20" id="antenna-support">&lt;/span>
&lt;a href="#antenna-support" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h4>&lt;div style="display: flex">
&lt;div>
&lt;p>&lt;strong>File:&lt;/strong> &lt;a href="https://github.com/AntoninPvr/MarcoX/blob/main/antenna_support.STL"target="_blank" rel="noopener">antenna_support.STL&lt;/a>&lt;/p>
&lt;p>This part is used to fix the WiFi antenna and improve signal quality.&lt;/p>
&lt;/div>
&lt;div id="./models/antenna_support.glb" style="width: 50%; height: 50%">
&lt;/div>
&lt;script type="importmap">
{
"imports": {
"three": "https://unpkg.com/three@0.161.0/build/three.module.js",
"three/addons/": "https://unpkg.com/three@0.161.0/examples/jsm/"
}
}
&lt;/script>
&lt;script type="module">
import * as THREE from 'three';
import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { MeshoptDecoder } from 'three/addons/libs/meshopt_decoder.module.js';
import { DRACOLoader } from 'three/addons/loaders/DRACOLoader.js'
let container;
let camera, scene, renderer, clock;
let mesh;
let rotate, controls;
init();
render();
function init() {
container = document.getElementById(".\/models\/antenna_support.glb");
container.onload = () => {
onWindowResize();
}
const aspectRatio = "1.3333";
const model_path = ".\/models\/antenna_support.glb";
const modelColor = "#3f4d52";
rotate = "true" === "true";
renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
renderer.setPixelRatio(window.devicePixelRatio);
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1;
renderer.outputColorSpace = THREE.SRGBColorSpace;
container.appendChild(renderer.domElement);
camera = new THREE.PerspectiveCamera(45, aspectRatio, 0.1, 1000);
camera.position.set(1, 0, 1);
camera.lookAt(0, 0, 0);
controls = new OrbitControls(camera, renderer.domElement);
controls.addEventListener('change', render);
controls.minDistance = 1;
controls.maxDistance = 10;
controls.target.set(0, 0, 0);
controls.update();
const environment = new RoomEnvironment(renderer);
const pmremGenerator = new THREE.PMREMGenerator(renderer);
scene = new THREE.Scene();
scene.environment = pmremGenerator.fromScene(environment).texture;
const ambientLight = new THREE.AmbientLight(0xffffff, 1);
scene.add(ambientLight);
const light1 = new THREE.PointLight( 0xffeeee, 50, 0);
light1.position.set( 0, 2, 1 );
light1.castShadow = true;
scene.add( light1 );
const loader = new GLTFLoader();
const dracoLoader = new DRACOLoader();
dracoLoader.setDecoderPath('/examples/jsm/libs/draco/');
loader.setMeshoptDecoder(MeshoptDecoder);
loader.setDRACOLoader(dracoLoader);
loader.load(model_path, function (gltf) {
const root = gltf.scene;
const box = new THREE.Box3().setFromObject(root);
const size = box.getSize(new THREE.Vector3());
const center = box.getCenter(new THREE.Vector3());
root.position.x += (root.position.x - center.x);
root.position.y += (root.position.y - center.y);
root.position.z += (root.position.z - center.z);
mesh = new THREE.Group();
mesh.add(root);
if (modelColor &amp;&amp; modelColor !== "") {
root.traverse((child) => {
if (child.isMesh) {
child.material.color.set(modelColor);
child.material.roughness = 0.8;
child.material.metalness = 0.2;
}
});
}
root.traverse((child) => {
if (child.isMesh) {
child.castShadow = true;
child.receiveShadow = true;
}
});
const maxSize = Math.max(size.x, size.y, size.z);
mesh.scale.set(1 / maxSize, 1 / maxSize, 1 / maxSize);
scene.add(mesh);
if ( rotate ) {
clock = new THREE.Clock();
renderer.setAnimationLoop( animate );
controls.addEventListener( 'start', () => rotate = false );
controls.addEventListener( 'end', () => rotate = true );
}
render();
});
window.addEventListener('resize', onWindowResize);
}
function onWindowResize() {
const aspectRatio = "1.3333";
camera.aspect = aspectRatio;
camera.updateProjectionMatrix();
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
render();
}
function render() {
renderer.render(scene, camera);
}
function animate() {
const delta = clock.getDelta();
const direction = "right";
if ( rotate &amp;&amp; mesh ) {
const ry = 0.5;
if (direction === "left") {
mesh.rotation.y -= delta * ry;
} else {
mesh.rotation.y += delta * ry;
}
render();
}
}
&lt;/script>
&lt;/div>
&lt;h2>Enclosure&lt;span class="hx:absolute hx:-mt-20" id="enclosure">&lt;/span>
&lt;a href="#enclosure" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>&lt;figure>
&lt;img src="https://github.com/AntoninPvr/MarcoX/blob/main/img/render/enclosure.JPG?raw=true" title="Wooden enclosure" alt="“Wooden enclosure”" loading="lazy" />
&lt;figcaption>Wooden enclosure&lt;/figcaption>
&lt;/figure>&lt;/p>
&lt;p>This is basically a wooden box with finger joints. It is composed of 5 parts: 1 base, 2 long sides and 2 short sides.&lt;/p>
&lt;h3>Laser cut parts&lt;span class="hx:absolute hx:-mt-20" id="laser-cut-parts">&lt;/span>
&lt;a href="#laser-cut-parts" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Laser cut parts are in &lt;code>.svg&lt;/code> format. Red lines width is 0.2mm and are adapted for &lt;a href="https://www.troteclaser.com/static/pdf/speedy-100/Fiche-technique-Speedy-100C-8063-fr.pdf"target="_blank" rel="noopener">Trotec Speedy 100&lt;/a> laser cutter.
Material: 6mm plywood recommended.&lt;/p>
&lt;div class="hextra-cards hx:mt-4 hx:gap-4 hx:grid not-prose" style="--hextra-cards-grid-cols: 3;">
&lt;img src="https://github.com/AntoninPvr/MarcoX/blob/main/laser/thickness_6mm/top_cover.svg?raw=true" class="grid-w50 md:grid-w33 xl:grid-w25" />
&lt;img src="https://github.com/AntoninPvr/MarcoX/blob/main/laser/thickness_6mm/base.svg?raw=true" class="grid-w50 md:grid-w33 xl:grid-w25" />
&lt;img src="https://github.com/AntoninPvr/MarcoX/blob/main/laser/thickness_6mm/long_side.svg?raw=true" class="grid-w50 md:grid-w33 xl:grid-w25" />
&lt;img src="https://github.com/AntoninPvr/MarcoX/blob/main/laser/thickness_6mm/long_side_usb-c.svg?raw=true" class="grid-w50 md:grid-w33 xl:grid-w25" />
&lt;img src="https://github.com/AntoninPvr/MarcoX/blob/main/laser/thickness_6mm/short_side.svg?raw=true" class="grid-w50 md:grid-w33 xl:grid-w25" />
&lt;/div>
&lt;h2>Related projects&lt;span class="hx:absolute hx:-mt-20" id="related-projects">&lt;/span>
&lt;a href="#related-projects" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>
&lt;div class="hextra-cards hx:mt-4 hx:gap-4 hx:grid not-prose"
style="--hextra-cards-grid-cols: 3;">&lt;a
class="hextra-card hx:relative hx:group hx:flex hx:flex-col hx:justify-start hx:overflow-hidden hx:rounded-lg hx:border hx:border-gray-200 hx:text-current hx:no-underline hx:dark:shadow-none hx:hover:shadow-gray-100 hx:dark:hover:shadow-none hx:shadow-gray-100 hx:active:shadow-sm hx:active:shadow-gray-200 hx:transition-all hx:duration-200 hx:hover:border-gray-300 hx:bg-transparent hx:shadow-xs hx:dark:border-neutral-800 hx:hover:bg-slate-50 hx:hover:shadow-md hx:dark:hover:border-neutral-700 hx:dark:hover:bg-neutral-900">
&lt;span class="hextra-card-icon hx:flex hx:font-semibold hx:items-start hx:gap-2 hx:pt-4 hx:px-4 hx:text-gray-700 hx:hover:text-gray-900 hx:dark:text-neutral-200 hx:dark:hover:text-neutral-50">&lt;svg fill="currentColor" viewBox="3 3 18 18">
&lt;path d="M12 3C7.0275 3 3 7.12937 3 12.2276C3 16.3109 5.57625 19.7597 9.15374 20.9824C9.60374 21.0631 9.77249 20.7863 9.77249 20.5441C9.77249 20.3249 9.76125 19.5982 9.76125 18.8254C7.5 19.2522 6.915 18.2602 6.735 17.7412C6.63375 17.4759 6.19499 16.6569 5.8125 16.4378C5.4975 16.2647 5.0475 15.838 5.80124 15.8264C6.51 15.8149 7.01625 16.4954 7.18499 16.7723C7.99499 18.1679 9.28875 17.7758 9.80625 17.5335C9.885 16.9337 10.1212 16.53 10.38 16.2993C8.3775 16.0687 6.285 15.2728 6.285 11.7432C6.285 10.7397 6.63375 9.9092 7.20749 9.26326C7.1175 9.03257 6.8025 8.08674 7.2975 6.81794C7.2975 6.81794 8.05125 6.57571 9.77249 7.76377C10.4925 7.55615 11.2575 7.45234 12.0225 7.45234C12.7875 7.45234 13.5525 7.55615 14.2725 7.76377C15.9937 6.56418 16.7475 6.81794 16.7475 6.81794C17.2424 8.08674 16.9275 9.03257 16.8375 9.26326C17.4113 9.9092 17.76 10.7281 17.76 11.7432C17.76 15.2843 15.6563 16.0687 13.6537 16.2993C13.98 16.5877 14.2613 17.1414 14.2613 18.0065C14.2613 19.2407 14.25 20.2326 14.25 20.5441C14.25 20.7863 14.4188 21.0746 14.8688 20.9824C16.6554 20.364 18.2079 19.1866 19.3078 17.6162C20.4077 16.0457 20.9995 14.1611 21 12.2276C21 7.12937 16.9725 3 12 3Z">&lt;/path>
&lt;/svg>
GitHub Repo&lt;/span>
&lt;div class="hextra-card-subtitle hx:line-clamp-3 hx:text-sm hx:font-normal hx:text-gray-500 hx:dark:text-gray-400 hx:px-4 hx:mb-4 hx:mt-2">GitHub repository&lt;/div>&lt;div class="hextra-badge hextra-card-tag">
&lt;div class="hx:inline-flex hx:gap-1 hx:items-center hx:rounded-full hx:px-2.5 hx:leading-6 hx:text-[.65rem] hx:border hx:text-gray-600 hx:bg-gray-100 hx:dark:bg-neutral-800 hx:dark:text-neutral-200 hx:border-gray-200 hx:dark:border-neutral-700">&lt;svg height=12 xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" aria-hidden="true">&lt;path stroke-linecap="round" stroke-linejoin="round" d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z"/>&lt;/svg>no-release&lt;/div>
&lt;/div>
&lt;/a>
&lt;/div>
&lt;div class="hextra-cards hx:mt-4 hx:gap-4 hx:grid not-prose"
style="--hextra-cards-grid-cols: 3;">&lt;a
class="hextra-card hx:relative hx:group hx:flex hx:flex-col hx:justify-start hx:overflow-hidden hx:rounded-lg hx:border hx:border-gray-200 hx:text-current hx:no-underline hx:dark:shadow-none hx:hover:shadow-gray-100 hx:dark:hover:shadow-none hx:shadow-gray-100 hx:active:shadow-sm hx:active:shadow-gray-200 hx:transition-all hx:duration-200 hx:hover:border-gray-300 hx:bg-transparent hx:shadow-xs hx:dark:border-neutral-800 hx:hover:bg-slate-50 hx:hover:shadow-md hx:dark:hover:border-neutral-700 hx:dark:hover:bg-neutral-900"href="https://github.com/info-telecom-strasbourg/InsidePSBS"
target="_blank" rel="noreferrer">
&lt;span class="hextra-card-icon hx:flex hx:font-semibold hx:items-start hx:gap-2 hx:pt-4 hx:px-4 hx:text-gray-700 hx:hover:text-gray-900 hx:dark:text-neutral-200 hx:dark:hover:text-neutral-50">&lt;svg fill="currentColor" viewBox="3 3 18 18">
&lt;path d="M12 3C7.0275 3 3 7.12937 3 12.2276C3 16.3109 5.57625 19.7597 9.15374 20.9824C9.60374 21.0631 9.77249 20.7863 9.77249 20.5441C9.77249 20.3249 9.76125 19.5982 9.76125 18.8254C7.5 19.2522 6.915 18.2602 6.735 17.7412C6.63375 17.4759 6.19499 16.6569 5.8125 16.4378C5.4975 16.2647 5.0475 15.838 5.80124 15.8264C6.51 15.8149 7.01625 16.4954 7.18499 16.7723C7.99499 18.1679 9.28875 17.7758 9.80625 17.5335C9.885 16.9337 10.1212 16.53 10.38 16.2993C8.3775 16.0687 6.285 15.2728 6.285 11.7432C6.285 10.7397 6.63375 9.9092 7.20749 9.26326C7.1175 9.03257 6.8025 8.08674 7.2975 6.81794C7.2975 6.81794 8.05125 6.57571 9.77249 7.76377C10.4925 7.55615 11.2575 7.45234 12.0225 7.45234C12.7875 7.45234 13.5525 7.55615 14.2725 7.76377C15.9937 6.56418 16.7475 6.81794 16.7475 6.81794C17.2424 8.08674 16.9275 9.03257 16.8375 9.26326C17.4113 9.9092 17.76 10.7281 17.76 11.7432C17.76 15.2843 15.6563 16.0687 13.6537 16.2993C13.98 16.5877 14.2613 17.1414 14.2613 18.0065C14.2613 19.2407 14.25 20.2326 14.25 20.5441C14.25 20.7863 14.4188 21.0746 14.8688 20.9824C16.6554 20.364 18.2079 19.1866 19.3078 17.6162C20.4077 16.0457 20.9995 14.1611 21 12.2276C21 7.12937 16.9725 3 12 3Z">&lt;/path>
&lt;/svg>
InsidePSBS&lt;/span>
&lt;div class="hextra-card-subtitle hx:line-clamp-3 hx:text-sm hx:font-normal hx:text-gray-500 hx:dark:text-gray-400 hx:px-4 hx:mb-4 hx:mt-2">Application de TPS et BS&lt;/div>&lt;div class="hextra-badge hextra-card-tag">
&lt;div class="hx:inline-flex hx:gap-1 hx:items-center hx:rounded-full hx:px-2.5 hx:leading-6 hx:text-[.65rem] hx:border hx:text-gray-600 hx:bg-gray-100 hx:dark:bg-neutral-800 hx:dark:text-neutral-200 hx:border-gray-200 hx:dark:border-neutral-700">&lt;svg height=12 xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" aria-hidden="true">&lt;path stroke-linecap="round" stroke-linejoin="round" d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z"/>&lt;/svg>no-release&lt;/div>
&lt;/div>
&lt;/a>
&lt;/div></description></item><item><title>Raspberry PI 5 Enclosure</title><link>https://antoninpvr.github.io/hardware/rpi_case/rpi5/</link><pubDate>Tue, 12 Mar 2024 10:34:00 +0200</pubDate><guid>https://antoninpvr.github.io/hardware/rpi_case/rpi5/</guid><description>
&lt;img src="https://antoninpvr.github.io/hardware/rpi_case/rpi5/featured_hu_eceb78a1f0f91433.jpg" width="640" height="423"/>&lt;div class="hextra-cards hx:mt-4 hx:gap-4 hx:grid not-prose"
style="--hextra-cards-grid-cols: 3;">&lt;a
class="hextra-card hx:relative hx:group hx:flex hx:flex-col hx:justify-start hx:overflow-hidden hx:rounded-lg hx:border hx:border-gray-200 hx:text-current hx:no-underline hx:dark:shadow-none hx:hover:shadow-gray-100 hx:dark:hover:shadow-none hx:shadow-gray-100 hx:active:shadow-sm hx:active:shadow-gray-200 hx:transition-all hx:duration-200 hx:hover:border-gray-300 hx:bg-transparent hx:shadow-xs hx:dark:border-neutral-800 hx:hover:bg-slate-50 hx:hover:shadow-md hx:dark:hover:border-neutral-700 hx:dark:hover:bg-neutral-900"href="https://github.com/AntoninPvr/RPI5_Case"
target="_blank" rel="noreferrer">
&lt;span class="hextra-card-icon hx:flex hx:font-semibold hx:items-start hx:gap-2 hx:pt-4 hx:px-4 hx:text-gray-700 hx:hover:text-gray-900 hx:dark:text-neutral-200 hx:dark:hover:text-neutral-50">&lt;svg fill="currentColor" viewBox="3 3 18 18">
&lt;path d="M12 3C7.0275 3 3 7.12937 3 12.2276C3 16.3109 5.57625 19.7597 9.15374 20.9824C9.60374 21.0631 9.77249 20.7863 9.77249 20.5441C9.77249 20.3249 9.76125 19.5982 9.76125 18.8254C7.5 19.2522 6.915 18.2602 6.735 17.7412C6.63375 17.4759 6.19499 16.6569 5.8125 16.4378C5.4975 16.2647 5.0475 15.838 5.80124 15.8264C6.51 15.8149 7.01625 16.4954 7.18499 16.7723C7.99499 18.1679 9.28875 17.7758 9.80625 17.5335C9.885 16.9337 10.1212 16.53 10.38 16.2993C8.3775 16.0687 6.285 15.2728 6.285 11.7432C6.285 10.7397 6.63375 9.9092 7.20749 9.26326C7.1175 9.03257 6.8025 8.08674 7.2975 6.81794C7.2975 6.81794 8.05125 6.57571 9.77249 7.76377C10.4925 7.55615 11.2575 7.45234 12.0225 7.45234C12.7875 7.45234 13.5525 7.55615 14.2725 7.76377C15.9937 6.56418 16.7475 6.81794 16.7475 6.81794C17.2424 8.08674 16.9275 9.03257 16.8375 9.26326C17.4113 9.9092 17.76 10.7281 17.76 11.7432C17.76 15.2843 15.6563 16.0687 13.6537 16.2993C13.98 16.5877 14.2613 17.1414 14.2613 18.0065C14.2613 19.2407 14.25 20.2326 14.25 20.5441C14.25 20.7863 14.4188 21.0746 14.8688 20.9824C16.6554 20.364 18.2079 19.1866 19.3078 17.6162C20.4077 16.0457 20.9995 14.1611 21 12.2276C21 7.12937 16.9725 3 12 3Z">&lt;/path>
&lt;/svg>
RPI5_Case&lt;/span>
&lt;div class="hextra-card-subtitle hx:line-clamp-3 hx:text-sm hx:font-normal hx:text-gray-500 hx:dark:text-gray-400 hx:px-4 hx:mb-4 hx:mt-2">Compact case for Raspberry PI 5. It support RPI 5 and is compatible with Raspberry Pi Active Cooler&lt;/div>&lt;div class="hextra-badge hextra-card-tag">
&lt;div class="hx:inline-flex hx:gap-1 hx:items-center hx:rounded-full hx:px-2.5 hx:leading-6 hx:text-[.65rem] hx:border hx:text-gray-600 hx:bg-gray-100 hx:dark:bg-neutral-800 hx:dark:text-neutral-200 hx:border-gray-200 hx:dark:border-neutral-700">&lt;svg height=12 xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" aria-hidden="true">&lt;path stroke-linecap="round" stroke-linejoin="round" d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z"/>&lt;/svg>v1.1.0&lt;/div>
&lt;/div>
&lt;/a>
&lt;/div>
&lt;hr>
&lt;p>This enclosure is designed to be sleek and minimalistic. It is designed to be 3D printed. This case does not provide any camera or display port access. But GPIO are easily accessibles.&lt;/p>
&lt;p>&lt;img src="https://github.com/AntoninPvr/RPI5_Case/blob/main/img/render_sd_hdmi.jpg?raw=true" alt="HDMI side view&amp;#34;" loading="lazy" />&lt;/p>
&lt;p>&lt;img src="https://github.com/AntoninPvr/RPI5_Case/blob/main/img/render_hdmi_usb.jpg?raw=true" alt="“USB side view”" loading="lazy" />&lt;/p>
&lt;h2>Compatibility&lt;span class="hx:absolute hx:-mt-20" id="compatibility">&lt;/span>
&lt;a href="#compatibility" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>This enclosure support following boards:&lt;/p>
&lt;ul>
&lt;li>Raspberry PI 5&lt;/li>
&lt;/ul>
&lt;h2>Files&lt;span class="hx:absolute hx:-mt-20" id="files">&lt;/span>
&lt;a href="#files" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Two versions of the top part are available. One with GPIO access and one without. Ideal for a clean look or server applications like VPN, DNS&amp;hellip;&lt;/p>
&lt;div style="display: flex">
&lt;div style="width: 50%">
&lt;h3>Lower part&lt;span class="hx:absolute hx:-mt-20" id="lower-part">&lt;/span>
&lt;a href="#lower-part" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>&lt;strong>File&lt;/strong>: &lt;code>RPI_5_case_bottom.STL&lt;/code> Bottom part of the enclosure common to all&lt;/p>
&lt;/div>
&lt;div id="./models/RPI_5_case_bottom.glb" style="width: 50%; height: 50%">
&lt;/div>
&lt;script type="importmap">
{
"imports": {
"three": "https://unpkg.com/three@0.161.0/build/three.module.js",
"three/addons/": "https://unpkg.com/three@0.161.0/examples/jsm/"
}
}
&lt;/script>
&lt;script type="module">
import * as THREE from 'three';
import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { MeshoptDecoder } from 'three/addons/libs/meshopt_decoder.module.js';
import { DRACOLoader } from 'three/addons/loaders/DRACOLoader.js'
let container;
let camera, scene, renderer, clock;
let mesh;
let rotate, controls;
init();
render();
function init() {
container = document.getElementById(".\/models\/RPI_5_case_bottom.glb");
container.onload = () => {
onWindowResize();
}
const aspectRatio = "1.3333";
const model_path = ".\/models\/RPI_5_case_bottom.glb";
const modelColor = "#3f4d52";
rotate = "true" === "true";
renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
renderer.setPixelRatio(window.devicePixelRatio);
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1;
renderer.outputColorSpace = THREE.SRGBColorSpace;
container.appendChild(renderer.domElement);
camera = new THREE.PerspectiveCamera(45, aspectRatio, 0.1, 1000);
camera.position.set(1, 0, 1);
camera.lookAt(0, 0, 0);
controls = new OrbitControls(camera, renderer.domElement);
controls.addEventListener('change', render);
controls.minDistance = 1;
controls.maxDistance = 10;
controls.target.set(0, 0, 0);
controls.update();
const environment = new RoomEnvironment(renderer);
const pmremGenerator = new THREE.PMREMGenerator(renderer);
scene = new THREE.Scene();
scene.environment = pmremGenerator.fromScene(environment).texture;
const ambientLight = new THREE.AmbientLight(0xffffff, 1);
scene.add(ambientLight);
const light1 = new THREE.PointLight( 0xffeeee, 50, 0);
light1.position.set( 0, 2, 1 );
light1.castShadow = true;
scene.add( light1 );
const loader = new GLTFLoader();
const dracoLoader = new DRACOLoader();
dracoLoader.setDecoderPath('/examples/jsm/libs/draco/');
loader.setMeshoptDecoder(MeshoptDecoder);
loader.setDRACOLoader(dracoLoader);
loader.load(model_path, function (gltf) {
const root = gltf.scene;
const box = new THREE.Box3().setFromObject(root);
const size = box.getSize(new THREE.Vector3());
const center = box.getCenter(new THREE.Vector3());
root.position.x += (root.position.x - center.x);
root.position.y += (root.position.y - center.y);
root.position.z += (root.position.z - center.z);
mesh = new THREE.Group();
mesh.add(root);
if (modelColor &amp;&amp; modelColor !== "") {
root.traverse((child) => {
if (child.isMesh) {
child.material.color.set(modelColor);
child.material.roughness = 0.8;
child.material.metalness = 0.2;
}
});
}
root.traverse((child) => {
if (child.isMesh) {
child.castShadow = true;
child.receiveShadow = true;
}
});
const maxSize = Math.max(size.x, size.y, size.z);
mesh.scale.set(1 / maxSize, 1 / maxSize, 1 / maxSize);
scene.add(mesh);
if ( rotate ) {
clock = new THREE.Clock();
renderer.setAnimationLoop( animate );
controls.addEventListener( 'start', () => rotate = false );
controls.addEventListener( 'end', () => rotate = true );
}
render();
});
window.addEventListener('resize', onWindowResize);
}
function onWindowResize() {
const aspectRatio = "1.3333";
camera.aspect = aspectRatio;
camera.updateProjectionMatrix();
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
render();
}
function render() {
renderer.render(scene, camera);
}
function animate() {
const delta = clock.getDelta();
const direction = "right";
if ( rotate &amp;&amp; mesh ) {
const ry = 0.5;
if (direction === "left") {
mesh.rotation.y -= delta * ry;
} else {
mesh.rotation.y += delta * ry;
}
render();
}
}
&lt;/script>
&lt;/div>
&lt;div style="display: flex">
&lt;div style="width: 50%">
&lt;h3>Upper part GPIO&lt;span class="hx:absolute hx:-mt-20" id="upper-part-gpio">&lt;/span>
&lt;a href="#upper-part-gpio" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>&lt;strong>File&lt;/strong>: &lt;code>RPI_5_case_top.STL&lt;/code> Orginal top part of the enclosure with GPIO access&lt;/p>
&lt;/div>
&lt;div id="./models/RPI_5_case_top.glb" style="width: 50%; height: 50%">
&lt;/div>
&lt;script type="importmap">
{
"imports": {
"three": "https://unpkg.com/three@0.161.0/build/three.module.js",
"three/addons/": "https://unpkg.com/three@0.161.0/examples/jsm/"
}
}
&lt;/script>
&lt;script type="module">
import * as THREE from 'three';
import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { MeshoptDecoder } from 'three/addons/libs/meshopt_decoder.module.js';
import { DRACOLoader } from 'three/addons/loaders/DRACOLoader.js'
let container;
let camera, scene, renderer, clock;
let mesh;
let rotate, controls;
init();
render();
function init() {
container = document.getElementById(".\/models\/RPI_5_case_top.glb");
container.onload = () => {
onWindowResize();
}
const aspectRatio = "1.3333";
const model_path = ".\/models\/RPI_5_case_top.glb";
const modelColor = "#3f4d52";
rotate = "true" === "true";
renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
renderer.setPixelRatio(window.devicePixelRatio);
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1;
renderer.outputColorSpace = THREE.SRGBColorSpace;
container.appendChild(renderer.domElement);
camera = new THREE.PerspectiveCamera(45, aspectRatio, 0.1, 1000);
camera.position.set(1, 0, 1);
camera.lookAt(0, 0, 0);
controls = new OrbitControls(camera, renderer.domElement);
controls.addEventListener('change', render);
controls.minDistance = 1;
controls.maxDistance = 10;
controls.target.set(0, 0, 0);
controls.update();
const environment = new RoomEnvironment(renderer);
const pmremGenerator = new THREE.PMREMGenerator(renderer);
scene = new THREE.Scene();
scene.environment = pmremGenerator.fromScene(environment).texture;
const ambientLight = new THREE.AmbientLight(0xffffff, 1);
scene.add(ambientLight);
const light1 = new THREE.PointLight( 0xffeeee, 50, 0);
light1.position.set( 0, 2, 1 );
light1.castShadow = true;
scene.add( light1 );
const loader = new GLTFLoader();
const dracoLoader = new DRACOLoader();
dracoLoader.setDecoderPath('/examples/jsm/libs/draco/');
loader.setMeshoptDecoder(MeshoptDecoder);
loader.setDRACOLoader(dracoLoader);
loader.load(model_path, function (gltf) {
const root = gltf.scene;
const box = new THREE.Box3().setFromObject(root);
const size = box.getSize(new THREE.Vector3());
const center = box.getCenter(new THREE.Vector3());
root.position.x += (root.position.x - center.x);
root.position.y += (root.position.y - center.y);
root.position.z += (root.position.z - center.z);
mesh = new THREE.Group();
mesh.add(root);
if (modelColor &amp;&amp; modelColor !== "") {
root.traverse((child) => {
if (child.isMesh) {
child.material.color.set(modelColor);
child.material.roughness = 0.8;
child.material.metalness = 0.2;
}
});
}
root.traverse((child) => {
if (child.isMesh) {
child.castShadow = true;
child.receiveShadow = true;
}
});
const maxSize = Math.max(size.x, size.y, size.z);
mesh.scale.set(1 / maxSize, 1 / maxSize, 1 / maxSize);
scene.add(mesh);
if ( rotate ) {
clock = new THREE.Clock();
renderer.setAnimationLoop( animate );
controls.addEventListener( 'start', () => rotate = false );
controls.addEventListener( 'end', () => rotate = true );
}
render();
});
window.addEventListener('resize', onWindowResize);
}
function onWindowResize() {
const aspectRatio = "1.3333";
camera.aspect = aspectRatio;
camera.updateProjectionMatrix();
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
render();
}
function render() {
renderer.render(scene, camera);
}
function animate() {
const delta = clock.getDelta();
const direction = "left";
if ( rotate &amp;&amp; mesh ) {
const ry = 0.5;
if (direction === "left") {
mesh.rotation.y -= delta * ry;
} else {
mesh.rotation.y += delta * ry;
}
render();
}
}
&lt;/script>
&lt;/div>
&lt;div style="display: flex">
&lt;div style="width: 50%">
&lt;h3>Upper part without GPIO&lt;span class="hx:absolute hx:-mt-20" id="upper-part-without-gpio">&lt;/span>
&lt;a href="#upper-part-without-gpio" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>&lt;strong>File&lt;/strong>: &lt;code>RPI_5_case_top_no_gpio.STL&lt;/code> Top part of the enclosure without GPIO access&lt;/p>
&lt;/div>
&lt;div id="./models/RPI_5_case_top_no_gpio.glb" style="width: 50%; height: 50%">
&lt;/div>
&lt;script type="importmap">
{
"imports": {
"three": "https://unpkg.com/three@0.161.0/build/three.module.js",
"three/addons/": "https://unpkg.com/three@0.161.0/examples/jsm/"
}
}
&lt;/script>
&lt;script type="module">
import * as THREE from 'three';
import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { MeshoptDecoder } from 'three/addons/libs/meshopt_decoder.module.js';
import { DRACOLoader } from 'three/addons/loaders/DRACOLoader.js'
let container;
let camera, scene, renderer, clock;
let mesh;
let rotate, controls;
init();
render();
function init() {
container = document.getElementById(".\/models\/RPI_5_case_top_no_gpio.glb");
container.onload = () => {
onWindowResize();
}
const aspectRatio = "1.3333";
const model_path = ".\/models\/RPI_5_case_top_no_gpio.glb";
const modelColor = "#3f4d52";
rotate = "true" === "true";
renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
renderer.setPixelRatio(window.devicePixelRatio);
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1;
renderer.outputColorSpace = THREE.SRGBColorSpace;
container.appendChild(renderer.domElement);
camera = new THREE.PerspectiveCamera(45, aspectRatio, 0.1, 1000);
camera.position.set(1, 0, 1);
camera.lookAt(0, 0, 0);
controls = new OrbitControls(camera, renderer.domElement);
controls.addEventListener('change', render);
controls.minDistance = 1;
controls.maxDistance = 10;
controls.target.set(0, 0, 0);
controls.update();
const environment = new RoomEnvironment(renderer);
const pmremGenerator = new THREE.PMREMGenerator(renderer);
scene = new THREE.Scene();
scene.environment = pmremGenerator.fromScene(environment).texture;
const ambientLight = new THREE.AmbientLight(0xffffff, 1);
scene.add(ambientLight);
const light1 = new THREE.PointLight( 0xffeeee, 50, 0);
light1.position.set( 0, 2, 1 );
light1.castShadow = true;
scene.add( light1 );
const loader = new GLTFLoader();
const dracoLoader = new DRACOLoader();
dracoLoader.setDecoderPath('/examples/jsm/libs/draco/');
loader.setMeshoptDecoder(MeshoptDecoder);
loader.setDRACOLoader(dracoLoader);
loader.load(model_path, function (gltf) {
const root = gltf.scene;
const box = new THREE.Box3().setFromObject(root);
const size = box.getSize(new THREE.Vector3());
const center = box.getCenter(new THREE.Vector3());
root.position.x += (root.position.x - center.x);
root.position.y += (root.position.y - center.y);
root.position.z += (root.position.z - center.z);
mesh = new THREE.Group();
mesh.add(root);
if (modelColor &amp;&amp; modelColor !== "") {
root.traverse((child) => {
if (child.isMesh) {
child.material.color.set(modelColor);
child.material.roughness = 0.8;
child.material.metalness = 0.2;
}
});
}
root.traverse((child) => {
if (child.isMesh) {
child.castShadow = true;
child.receiveShadow = true;
}
});
const maxSize = Math.max(size.x, size.y, size.z);
mesh.scale.set(1 / maxSize, 1 / maxSize, 1 / maxSize);
scene.add(mesh);
if ( rotate ) {
clock = new THREE.Clock();
renderer.setAnimationLoop( animate );
controls.addEventListener( 'start', () => rotate = false );
controls.addEventListener( 'end', () => rotate = true );
}
render();
});
window.addEventListener('resize', onWindowResize);
}
function onWindowResize() {
const aspectRatio = "1.3333";
camera.aspect = aspectRatio;
camera.updateProjectionMatrix();
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
render();
}
function render() {
renderer.render(scene, camera);
}
function animate() {
const delta = clock.getDelta();
const direction = "left";
if ( rotate &amp;&amp; mesh ) {
const ry = 0.5;
if (direction === "left") {
mesh.rotation.y -= delta * ry;
} else {
mesh.rotation.y += delta * ry;
}
render();
}
}
&lt;/script>
&lt;/div>
&lt;div style="display: flex">
&lt;div style="width: 50%">
&lt;h3>GPIO Cover&lt;span class="hx:absolute hx:-mt-20" id="gpio-cover">&lt;/span>
&lt;a href="#gpio-cover" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>&lt;strong>File&lt;/strong>: &lt;code>gpio_cover.STL&lt;/code> Cover for GPIO. Best of both worlds, clean look and easy access to GPIO&lt;/p>
&lt;/div>
&lt;div id="./models/gpio_cover.glb" style="width: 50%; height: 50%">
&lt;/div>
&lt;script type="importmap">
{
"imports": {
"three": "https://unpkg.com/three@0.161.0/build/three.module.js",
"three/addons/": "https://unpkg.com/three@0.161.0/examples/jsm/"
}
}
&lt;/script>
&lt;script type="module">
import * as THREE from 'three';
import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { MeshoptDecoder } from 'three/addons/libs/meshopt_decoder.module.js';
import { DRACOLoader } from 'three/addons/loaders/DRACOLoader.js'
let container;
let camera, scene, renderer, clock;
let mesh;
let rotate, controls;
init();
render();
function init() {
container = document.getElementById(".\/models\/gpio_cover.glb");
container.onload = () => {
onWindowResize();
}
const aspectRatio = "1.3333";
const model_path = ".\/models\/gpio_cover.glb";
const modelColor = "#3f4d52";
rotate = "true" === "true";
renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
renderer.setPixelRatio(window.devicePixelRatio);
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1;
renderer.outputColorSpace = THREE.SRGBColorSpace;
container.appendChild(renderer.domElement);
camera = new THREE.PerspectiveCamera(45, aspectRatio, 0.1, 1000);
camera.position.set(1, 0, 1);
camera.lookAt(0, 0, 0);
controls = new OrbitControls(camera, renderer.domElement);
controls.addEventListener('change', render);
controls.minDistance = 1;
controls.maxDistance = 10;
controls.target.set(0, 0, 0);
controls.update();
const environment = new RoomEnvironment(renderer);
const pmremGenerator = new THREE.PMREMGenerator(renderer);
scene = new THREE.Scene();
scene.environment = pmremGenerator.fromScene(environment).texture;
const ambientLight = new THREE.AmbientLight(0xffffff, 1);
scene.add(ambientLight);
const light1 = new THREE.PointLight( 0xffeeee, 50, 0);
light1.position.set( 0, 2, 1 );
light1.castShadow = true;
scene.add( light1 );
const loader = new GLTFLoader();
const dracoLoader = new DRACOLoader();
dracoLoader.setDecoderPath('/examples/jsm/libs/draco/');
loader.setMeshoptDecoder(MeshoptDecoder);
loader.setDRACOLoader(dracoLoader);
loader.load(model_path, function (gltf) {
const root = gltf.scene;
const box = new THREE.Box3().setFromObject(root);
const size = box.getSize(new THREE.Vector3());
const center = box.getCenter(new THREE.Vector3());
root.position.x += (root.position.x - center.x);
root.position.y += (root.position.y - center.y);
root.position.z += (root.position.z - center.z);
mesh = new THREE.Group();
mesh.add(root);
if (modelColor &amp;&amp; modelColor !== "") {
root.traverse((child) => {
if (child.isMesh) {
child.material.color.set(modelColor);
child.material.roughness = 0.8;
child.material.metalness = 0.2;
}
});
}
root.traverse((child) => {
if (child.isMesh) {
child.castShadow = true;
child.receiveShadow = true;
}
});
const maxSize = Math.max(size.x, size.y, size.z);
mesh.scale.set(1 / maxSize, 1 / maxSize, 1 / maxSize);
scene.add(mesh);
if ( rotate ) {
clock = new THREE.Clock();
renderer.setAnimationLoop( animate );
controls.addEventListener( 'start', () => rotate = false );
controls.addEventListener( 'end', () => rotate = true );
}
render();
});
window.addEventListener('resize', onWindowResize);
}
function onWindowResize() {
const aspectRatio = "1.3333";
camera.aspect = aspectRatio;
camera.updateProjectionMatrix();
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
render();
}
function render() {
renderer.render(scene, camera);
}
function animate() {
const delta = clock.getDelta();
const direction = "right";
if ( rotate &amp;&amp; mesh ) {
const ry = 0.5;
if (direction === "left") {
mesh.rotation.y -= delta * ry;
} else {
mesh.rotation.y += delta * ry;
}
render();
}
}
&lt;/script>
&lt;/div>
&lt;h2>Thermal management&lt;span class="hx:absolute hx:-mt-20" id="thermal-management">&lt;/span>
&lt;a href="#thermal-management" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>This enclosure is designed to be compatible with the official &lt;a href="https://www.raspberrypi.com/products/active-cooler/"target="_blank" rel="noopener">Raspberry Pi Active Cooler&lt;/a>&lt;/p>
&lt;p>&lt;img src="https://github.com/AntoninPvr/RPI5_Case/blob/main/img/internal.jpg?raw=true" alt="“Internal view”" loading="lazy" />&lt;/p>
&lt;p>&lt;img src="https://github.com/AntoninPvr/RPI5_Case/blob/main/img/internal_heatsink.jpg?raw=true" alt="“Internal view with heatsink”" loading="lazy" />&lt;/p>
&lt;h2>Material and print settings&lt;span class="hx:absolute hx:-mt-20" id="material-and-print-settings">&lt;/span>
&lt;a href="#material-and-print-settings" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Clearance is 0.2mm for all parts to facilitate assembly. No support is needed.&lt;/p>
&lt;p>4x M3x24mm flat-head screws and 4x M3 nuts are needed to assemble the case.&lt;/p>
&lt;h2>Run button&lt;span class="hx:absolute hx:-mt-20" id="run-button">&lt;/span>
&lt;a href="#run-button" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>1mm hole near micro sd-card.&lt;/p>
&lt;h2>RTC battery&lt;span class="hx:absolute hx:-mt-20" id="rtc-battery">&lt;/span>
&lt;a href="#rtc-battery" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Dedicated space for a CR2020 battery above Ethernet port.&lt;/p></description></item><item><title>Orange PI Base CM4 Case</title><link>https://antoninpvr.github.io/hardware/rpi_case/orange_pi_cm4_base/</link><pubDate>Tue, 20 Feb 2024 14:22:00 +0200</pubDate><guid>https://antoninpvr.github.io/hardware/rpi_case/orange_pi_cm4_base/</guid><description>
&lt;img src="https://antoninpvr.github.io/hardware/rpi_case/orange_pi_cm4_base/featured_hu_967cbda8701e98e6.jpg" width="640" height="350"/>&lt;div class="hextra-cards hx:mt-4 hx:gap-4 hx:grid not-prose"
style="--hextra-cards-grid-cols: 3;">&lt;a
class="hextra-card hx:relative hx:group hx:flex hx:flex-col hx:justify-start hx:overflow-hidden hx:rounded-lg hx:border hx:border-gray-200 hx:text-current hx:no-underline hx:dark:shadow-none hx:hover:shadow-gray-100 hx:dark:hover:shadow-none hx:shadow-gray-100 hx:active:shadow-sm hx:active:shadow-gray-200 hx:transition-all hx:duration-200 hx:hover:border-gray-300 hx:bg-transparent hx:shadow-xs hx:dark:border-neutral-800 hx:hover:bg-slate-50 hx:hover:shadow-md hx:dark:hover:border-neutral-700 hx:dark:hover:bg-neutral-900"href="https://github.com/AntoninPvr/OrangePI_Base_CM4_Case"
target="_blank" rel="noreferrer">
&lt;span class="hextra-card-icon hx:flex hx:font-semibold hx:items-start hx:gap-2 hx:pt-4 hx:px-4 hx:text-gray-700 hx:hover:text-gray-900 hx:dark:text-neutral-200 hx:dark:hover:text-neutral-50">&lt;svg fill="currentColor" viewBox="3 3 18 18">
&lt;path d="M12 3C7.0275 3 3 7.12937 3 12.2276C3 16.3109 5.57625 19.7597 9.15374 20.9824C9.60374 21.0631 9.77249 20.7863 9.77249 20.5441C9.77249 20.3249 9.76125 19.5982 9.76125 18.8254C7.5 19.2522 6.915 18.2602 6.735 17.7412C6.63375 17.4759 6.19499 16.6569 5.8125 16.4378C5.4975 16.2647 5.0475 15.838 5.80124 15.8264C6.51 15.8149 7.01625 16.4954 7.18499 16.7723C7.99499 18.1679 9.28875 17.7758 9.80625 17.5335C9.885 16.9337 10.1212 16.53 10.38 16.2993C8.3775 16.0687 6.285 15.2728 6.285 11.7432C6.285 10.7397 6.63375 9.9092 7.20749 9.26326C7.1175 9.03257 6.8025 8.08674 7.2975 6.81794C7.2975 6.81794 8.05125 6.57571 9.77249 7.76377C10.4925 7.55615 11.2575 7.45234 12.0225 7.45234C12.7875 7.45234 13.5525 7.55615 14.2725 7.76377C15.9937 6.56418 16.7475 6.81794 16.7475 6.81794C17.2424 8.08674 16.9275 9.03257 16.8375 9.26326C17.4113 9.9092 17.76 10.7281 17.76 11.7432C17.76 15.2843 15.6563 16.0687 13.6537 16.2993C13.98 16.5877 14.2613 17.1414 14.2613 18.0065C14.2613 19.2407 14.25 20.2326 14.25 20.5441C14.25 20.7863 14.4188 21.0746 14.8688 20.9824C16.6554 20.364 18.2079 19.1866 19.3078 17.6162C20.4077 16.0457 20.9995 14.1611 21 12.2276C21 7.12937 16.9725 3 12 3Z">&lt;/path>
&lt;/svg>
OrangePI_Base_CM4_Case&lt;/span>
&lt;div class="hextra-card-subtitle hx:line-clamp-3 hx:text-sm hx:font-normal hx:text-gray-500 hx:dark:text-gray-400 hx:px-4 hx:mb-4 hx:mt-2">Compact case for Orange PI Compute Module 4 Base board. Intended for passive cooling. This case add support for 2280 M2 module&lt;/div>&lt;div class="hextra-badge hextra-card-tag">
&lt;div class="hx:inline-flex hx:gap-1 hx:items-center hx:rounded-full hx:px-2.5 hx:leading-6 hx:text-[.65rem] hx:border hx:text-gray-600 hx:bg-gray-100 hx:dark:bg-neutral-800 hx:dark:text-neutral-200 hx:border-gray-200 hx:dark:border-neutral-700">&lt;svg height=12 xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" aria-hidden="true">&lt;path stroke-linecap="round" stroke-linejoin="round" d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z"/>&lt;/svg>v0.0.0&lt;/div>
&lt;/div>
&lt;/a>
&lt;/div>
&lt;hr>
&lt;p>This enclosure is designed to be sleek and minimalistic. It is designed to be 3D printed. This case does not provide any camera or display port access. But allow use of M2 expension card / SSD and add compatibility with M2 2280 module.
As OrangePI Base board is compatible with Raspberry PI CM4, this case is also compatible with Raspberry PI CM4.&lt;/p>
&lt;p>Ideal for a clean look or server applications like VPN, DNS&amp;hellip;&lt;/p>
&lt;p>&lt;img src="https://github.com/AntoninPvr/OrangePI_Base_CM4_Case/blob/main/img/full.JPG?raw=true" alt="“Enclosure view”" loading="lazy" />&lt;/p>
&lt;p>&lt;img src="https://github.com/AntoninPvr/OrangePI_Base_CM4_Case/blob/main/img/full_transparent.JPG?raw=true" alt="“Enclosure transparancy view”" loading="lazy" />&lt;/p>
&lt;h2>Compatibility&lt;span class="hx:absolute hx:-mt-20" id="compatibility">&lt;/span>
&lt;a href="#compatibility" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>This enclosure support following boards:&lt;/p>
&lt;ul>
&lt;li>Orange PI CM4 Base&lt;/li>
&lt;/ul>
&lt;h2>Files&lt;span class="hx:absolute hx:-mt-20" id="files">&lt;/span>
&lt;a href="#files" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;div style="display: flex">
&lt;div style="width: 50%">
&lt;ul>
&lt;li>&lt;code>case_orange_pi_bottom.STL&lt;/code> Bottom part of the enclosure common to all
&lt;/div>
&lt;/li>
&lt;/ul>
&lt;div id="./models/case_orange_pi_bottom.glb" style="width: 50%; height: 50%">
&lt;/div>
&lt;script type="importmap">
{
"imports": {
"three": "https://unpkg.com/three@0.161.0/build/three.module.js",
"three/addons/": "https://unpkg.com/three@0.161.0/examples/jsm/"
}
}
&lt;/script>
&lt;script type="module">
import * as THREE from 'three';
import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { MeshoptDecoder } from 'three/addons/libs/meshopt_decoder.module.js';
import { DRACOLoader } from 'three/addons/loaders/DRACOLoader.js'
let container;
let camera, scene, renderer, clock;
let mesh;
let rotate, controls;
init();
render();
function init() {
container = document.getElementById(".\/models\/case_orange_pi_bottom.glb");
container.onload = () => {
onWindowResize();
}
const aspectRatio = "1.3333";
const model_path = ".\/models\/case_orange_pi_bottom.glb";
const modelColor = "#3f4d52";
rotate = "true" === "true";
renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
renderer.setPixelRatio(window.devicePixelRatio);
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1;
renderer.outputColorSpace = THREE.SRGBColorSpace;
container.appendChild(renderer.domElement);
camera = new THREE.PerspectiveCamera(45, aspectRatio, 0.1, 1000);
camera.position.set(1, 0, 1);
camera.lookAt(0, 0, 0);
controls = new OrbitControls(camera, renderer.domElement);
controls.addEventListener('change', render);
controls.minDistance = 1;
controls.maxDistance = 10;
controls.target.set(0, 0, 0);
controls.update();
const environment = new RoomEnvironment(renderer);
const pmremGenerator = new THREE.PMREMGenerator(renderer);
scene = new THREE.Scene();
scene.environment = pmremGenerator.fromScene(environment).texture;
const ambientLight = new THREE.AmbientLight(0xffffff, 1);
scene.add(ambientLight);
const light1 = new THREE.PointLight( 0xffeeee, 50, 0);
light1.position.set( 0, 2, 1 );
light1.castShadow = true;
scene.add( light1 );
const loader = new GLTFLoader();
const dracoLoader = new DRACOLoader();
dracoLoader.setDecoderPath('/examples/jsm/libs/draco/');
loader.setMeshoptDecoder(MeshoptDecoder);
loader.setDRACOLoader(dracoLoader);
loader.load(model_path, function (gltf) {
const root = gltf.scene;
const box = new THREE.Box3().setFromObject(root);
const size = box.getSize(new THREE.Vector3());
const center = box.getCenter(new THREE.Vector3());
root.position.x += (root.position.x - center.x);
root.position.y += (root.position.y - center.y);
root.position.z += (root.position.z - center.z);
mesh = new THREE.Group();
mesh.add(root);
if (modelColor &amp;&amp; modelColor !== "") {
root.traverse((child) => {
if (child.isMesh) {
child.material.color.set(modelColor);
child.material.roughness = 0.8;
child.material.metalness = 0.2;
}
});
}
root.traverse((child) => {
if (child.isMesh) {
child.castShadow = true;
child.receiveShadow = true;
}
});
const maxSize = Math.max(size.x, size.y, size.z);
mesh.scale.set(1 / maxSize, 1 / maxSize, 1 / maxSize);
scene.add(mesh);
if ( rotate ) {
clock = new THREE.Clock();
renderer.setAnimationLoop( animate );
controls.addEventListener( 'start', () => rotate = false );
controls.addEventListener( 'end', () => rotate = true );
}
render();
});
window.addEventListener('resize', onWindowResize);
}
function onWindowResize() {
const aspectRatio = "1.3333";
camera.aspect = aspectRatio;
camera.updateProjectionMatrix();
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
render();
}
function render() {
renderer.render(scene, camera);
}
function animate() {
const delta = clock.getDelta();
const direction = "left";
if ( rotate &amp;&amp; mesh ) {
const ry = 0.5;
if (direction === "left") {
mesh.rotation.y -= delta * ry;
} else {
mesh.rotation.y += delta * ry;
}
render();
}
}
&lt;/script>
&lt;/div>
&lt;div style="display: flex">
&lt;div style="width: 50%">
&lt;ul>
&lt;li>&lt;code>case_orange_pi_top.STL&lt;/code> Orginal top part of the enclosure
&lt;/div>
&lt;/li>
&lt;/ul>
&lt;div id="./models/case_orange_pi_top.glb" style="width: 50%; height: 50%">
&lt;/div>
&lt;script type="importmap">
{
"imports": {
"three": "https://unpkg.com/three@0.161.0/build/three.module.js",
"three/addons/": "https://unpkg.com/three@0.161.0/examples/jsm/"
}
}
&lt;/script>
&lt;script type="module">
import * as THREE from 'three';
import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { MeshoptDecoder } from 'three/addons/libs/meshopt_decoder.module.js';
import { DRACOLoader } from 'three/addons/loaders/DRACOLoader.js'
let container;
let camera, scene, renderer, clock;
let mesh;
let rotate, controls;
init();
render();
function init() {
container = document.getElementById(".\/models\/case_orange_pi_top.glb");
container.onload = () => {
onWindowResize();
}
const aspectRatio = "1.3333";
const model_path = ".\/models\/case_orange_pi_top.glb";
const modelColor = "#3f4d52";
rotate = "true" === "true";
renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
renderer.setPixelRatio(window.devicePixelRatio);
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1;
renderer.outputColorSpace = THREE.SRGBColorSpace;
container.appendChild(renderer.domElement);
camera = new THREE.PerspectiveCamera(45, aspectRatio, 0.1, 1000);
camera.position.set(1, 0, 1);
camera.lookAt(0, 0, 0);
controls = new OrbitControls(camera, renderer.domElement);
controls.addEventListener('change', render);
controls.minDistance = 1;
controls.maxDistance = 10;
controls.target.set(0, 0, 0);
controls.update();
const environment = new RoomEnvironment(renderer);
const pmremGenerator = new THREE.PMREMGenerator(renderer);
scene = new THREE.Scene();
scene.environment = pmremGenerator.fromScene(environment).texture;
const ambientLight = new THREE.AmbientLight(0xffffff, 1);
scene.add(ambientLight);
const light1 = new THREE.PointLight( 0xffeeee, 50, 0);
light1.position.set( 0, 2, 1 );
light1.castShadow = true;
scene.add( light1 );
const loader = new GLTFLoader();
const dracoLoader = new DRACOLoader();
dracoLoader.setDecoderPath('/examples/jsm/libs/draco/');
loader.setMeshoptDecoder(MeshoptDecoder);
loader.setDRACOLoader(dracoLoader);
loader.load(model_path, function (gltf) {
const root = gltf.scene;
const box = new THREE.Box3().setFromObject(root);
const size = box.getSize(new THREE.Vector3());
const center = box.getCenter(new THREE.Vector3());
root.position.x += (root.position.x - center.x);
root.position.y += (root.position.y - center.y);
root.position.z += (root.position.z - center.z);
mesh = new THREE.Group();
mesh.add(root);
if (modelColor &amp;&amp; modelColor !== "") {
root.traverse((child) => {
if (child.isMesh) {
child.material.color.set(modelColor);
child.material.roughness = 0.8;
child.material.metalness = 0.2;
}
});
}
root.traverse((child) => {
if (child.isMesh) {
child.castShadow = true;
child.receiveShadow = true;
}
});
const maxSize = Math.max(size.x, size.y, size.z);
mesh.scale.set(1 / maxSize, 1 / maxSize, 1 / maxSize);
scene.add(mesh);
if ( rotate ) {
clock = new THREE.Clock();
renderer.setAnimationLoop( animate );
controls.addEventListener( 'start', () => rotate = false );
controls.addEventListener( 'end', () => rotate = true );
}
render();
});
window.addEventListener('resize', onWindowResize);
}
function onWindowResize() {
const aspectRatio = "1.3333";
camera.aspect = aspectRatio;
camera.updateProjectionMatrix();
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
render();
}
function render() {
renderer.render(scene, camera);
}
function animate() {
const delta = clock.getDelta();
const direction = "right";
if ( rotate &amp;&amp; mesh ) {
const ry = 0.5;
if (direction === "left") {
mesh.rotation.y -= delta * ry;
} else {
mesh.rotation.y += delta * ry;
}
render();
}
}
&lt;/script>
&lt;/div>
&lt;div style="display: flex">
&lt;div style="width: 50%">
&lt;ul>
&lt;li>&lt;code>m2_cache.STL&lt;/code> Cache when M2 port is not used
&lt;/div>
&lt;/li>
&lt;/ul>
&lt;div id="./models/m2_cache.glb" style="width: 50%; height: 50%">
&lt;/div>
&lt;script type="importmap">
{
"imports": {
"three": "https://unpkg.com/three@0.161.0/build/three.module.js",
"three/addons/": "https://unpkg.com/three@0.161.0/examples/jsm/"
}
}
&lt;/script>
&lt;script type="module">
import * as THREE from 'three';
import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { MeshoptDecoder } from 'three/addons/libs/meshopt_decoder.module.js';
import { DRACOLoader } from 'three/addons/loaders/DRACOLoader.js'
let container;
let camera, scene, renderer, clock;
let mesh;
let rotate, controls;
init();
render();
function init() {
container = document.getElementById(".\/models\/m2_cache.glb");
container.onload = () => {
onWindowResize();
}
const aspectRatio = "1.3333";
const model_path = ".\/models\/m2_cache.glb";
const modelColor = "#3f4d52";
rotate = "true" === "true";
renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
renderer.setPixelRatio(window.devicePixelRatio);
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1;
renderer.outputColorSpace = THREE.SRGBColorSpace;
container.appendChild(renderer.domElement);
camera = new THREE.PerspectiveCamera(45, aspectRatio, 0.1, 1000);
camera.position.set(1, 0, 1);
camera.lookAt(0, 0, 0);
controls = new OrbitControls(camera, renderer.domElement);
controls.addEventListener('change', render);
controls.minDistance = 1;
controls.maxDistance = 10;
controls.target.set(0, 0, 0);
controls.update();
const environment = new RoomEnvironment(renderer);
const pmremGenerator = new THREE.PMREMGenerator(renderer);
scene = new THREE.Scene();
scene.environment = pmremGenerator.fromScene(environment).texture;
const ambientLight = new THREE.AmbientLight(0xffffff, 1);
scene.add(ambientLight);
const light1 = new THREE.PointLight( 0xffeeee, 50, 0);
light1.position.set( 0, 2, 1 );
light1.castShadow = true;
scene.add( light1 );
const loader = new GLTFLoader();
const dracoLoader = new DRACOLoader();
dracoLoader.setDecoderPath('/examples/jsm/libs/draco/');
loader.setMeshoptDecoder(MeshoptDecoder);
loader.setDRACOLoader(dracoLoader);
loader.load(model_path, function (gltf) {
const root = gltf.scene;
const box = new THREE.Box3().setFromObject(root);
const size = box.getSize(new THREE.Vector3());
const center = box.getCenter(new THREE.Vector3());
root.position.x += (root.position.x - center.x);
root.position.y += (root.position.y - center.y);
root.position.z += (root.position.z - center.z);
mesh = new THREE.Group();
mesh.add(root);
if (modelColor &amp;&amp; modelColor !== "") {
root.traverse((child) => {
if (child.isMesh) {
child.material.color.set(modelColor);
child.material.roughness = 0.8;
child.material.metalness = 0.2;
}
});
}
root.traverse((child) => {
if (child.isMesh) {
child.castShadow = true;
child.receiveShadow = true;
}
});
const maxSize = Math.max(size.x, size.y, size.z);
mesh.scale.set(1 / maxSize, 1 / maxSize, 1 / maxSize);
scene.add(mesh);
if ( rotate ) {
clock = new THREE.Clock();
renderer.setAnimationLoop( animate );
controls.addEventListener( 'start', () => rotate = false );
controls.addEventListener( 'end', () => rotate = true );
}
render();
});
window.addEventListener('resize', onWindowResize);
}
function onWindowResize() {
const aspectRatio = "1.3333";
camera.aspect = aspectRatio;
camera.updateProjectionMatrix();
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
render();
}
function render() {
renderer.render(scene, camera);
}
function animate() {
const delta = clock.getDelta();
const direction = "left";
if ( rotate &amp;&amp; mesh ) {
const ry = 0.5;
if (direction === "left") {
mesh.rotation.y -= delta * ry;
} else {
mesh.rotation.y += delta * ry;
}
render();
}
}
&lt;/script>
&lt;/div>
&lt;h2>M2 port&lt;span class="hx:absolute hx:-mt-20" id="m2-port">&lt;/span>
&lt;a href="#m2-port" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>The case is designed to support M2 2230, 2242 and 2280 card. The M2 port is accessible from the bottom of the case.&lt;/p>
&lt;p>&lt;figure>
&lt;img src="https://github.com/AntoninPvr/OrangePI_Base_CM4_Case/blob/main/img/m2.JPG?raw=true" title="M2 slot" alt="“M2 slot”" loading="lazy" />
&lt;figcaption>M2 slot&lt;/figcaption>
&lt;/figure>&lt;/p>
&lt;p>&lt;figure>
&lt;img src="https://github.com/AntoninPvr/OrangePI_Base_CM4_Case/blob/main/img/m2_cover.JPG?raw=true" title="M2 slot cover" alt="“M2 cover”" loading="lazy" />
&lt;figcaption>M2 slot cover&lt;/figcaption>
&lt;/figure>&lt;/p>
&lt;h2>Thermal management&lt;span class="hx:absolute hx:-mt-20" id="thermal-management">&lt;/span>
&lt;a href="#thermal-management" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>This enclosure is designed to be passive.&lt;/p>
&lt;p>&lt;img src="https://github.com/AntoninPvr/OrangePI_Base_CM4_Case/blob/main/img/internal.JPG?raw=true%22" alt="“Internal view”" loading="lazy" />&lt;/p>
&lt;h2>Material and print settings&lt;span class="hx:absolute hx:-mt-20" id="material-and-print-settings">&lt;/span>
&lt;a href="#material-and-print-settings" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Clearance is 0.2mm for all parts to facilitate assembly. No support is needed.&lt;/p>
&lt;p>4x M2.5x24mm flat-head screws and 4x M2.5 nuts are needed to assemble the case.&lt;/p>
&lt;p>3x M2.5x24mm flat-head screws and 3x M2.5 nuts can be add.&lt;/p>
&lt;h2>Boot/Reset/Recovery button&lt;span class="hx:absolute hx:-mt-20" id="bootresetrecovery-button">&lt;/span>
&lt;a href="#bootresetrecovery-button" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>3 integrated buttons are available on the top part of the enclosure.&lt;/p></description></item><item><title>Raspberry PI 4 Enclosure</title><link>https://antoninpvr.github.io/hardware/rpi_case/rpi4/</link><pubDate>Tue, 20 Feb 2024 14:22:00 +0200</pubDate><guid>https://antoninpvr.github.io/hardware/rpi_case/rpi4/</guid><description>
&lt;img src="https://antoninpvr.github.io/hardware/rpi_case/rpi4/featured_hu_9da9b7df292243df.jpg" width="640" height="379"/>&lt;div class="hextra-cards hx:mt-4 hx:gap-4 hx:grid not-prose"
style="--hextra-cards-grid-cols: 3;">&lt;a
class="hextra-card hx:relative hx:group hx:flex hx:flex-col hx:justify-start hx:overflow-hidden hx:rounded-lg hx:border hx:border-gray-200 hx:text-current hx:no-underline hx:dark:shadow-none hx:hover:shadow-gray-100 hx:dark:hover:shadow-none hx:shadow-gray-100 hx:active:shadow-sm hx:active:shadow-gray-200 hx:transition-all hx:duration-200 hx:hover:border-gray-300 hx:bg-transparent hx:shadow-xs hx:dark:border-neutral-800 hx:hover:bg-slate-50 hx:hover:shadow-md hx:dark:hover:border-neutral-700 hx:dark:hover:bg-neutral-900"href="https://github.com/AntoninPvr/RPI4B_Case"
target="_blank" rel="noreferrer">
&lt;span class="hextra-card-icon hx:flex hx:font-semibold hx:items-start hx:gap-2 hx:pt-4 hx:px-4 hx:text-gray-700 hx:hover:text-gray-900 hx:dark:text-neutral-200 hx:dark:hover:text-neutral-50">&lt;svg fill="currentColor" viewBox="3 3 18 18">
&lt;path d="M12 3C7.0275 3 3 7.12937 3 12.2276C3 16.3109 5.57625 19.7597 9.15374 20.9824C9.60374 21.0631 9.77249 20.7863 9.77249 20.5441C9.77249 20.3249 9.76125 19.5982 9.76125 18.8254C7.5 19.2522 6.915 18.2602 6.735 17.7412C6.63375 17.4759 6.19499 16.6569 5.8125 16.4378C5.4975 16.2647 5.0475 15.838 5.80124 15.8264C6.51 15.8149 7.01625 16.4954 7.18499 16.7723C7.99499 18.1679 9.28875 17.7758 9.80625 17.5335C9.885 16.9337 10.1212 16.53 10.38 16.2993C8.3775 16.0687 6.285 15.2728 6.285 11.7432C6.285 10.7397 6.63375 9.9092 7.20749 9.26326C7.1175 9.03257 6.8025 8.08674 7.2975 6.81794C7.2975 6.81794 8.05125 6.57571 9.77249 7.76377C10.4925 7.55615 11.2575 7.45234 12.0225 7.45234C12.7875 7.45234 13.5525 7.55615 14.2725 7.76377C15.9937 6.56418 16.7475 6.81794 16.7475 6.81794C17.2424 8.08674 16.9275 9.03257 16.8375 9.26326C17.4113 9.9092 17.76 10.7281 17.76 11.7432C17.76 15.2843 15.6563 16.0687 13.6537 16.2993C13.98 16.5877 14.2613 17.1414 14.2613 18.0065C14.2613 19.2407 14.25 20.2326 14.25 20.5441C14.25 20.7863 14.4188 21.0746 14.8688 20.9824C16.6554 20.364 18.2079 19.1866 19.3078 17.6162C20.4077 16.0457 20.9995 14.1611 21 12.2276C21 7.12937 16.9725 3 12 3Z">&lt;/path>
&lt;/svg>
RPI4B_Case&lt;/span>
&lt;div class="hextra-card-subtitle hx:line-clamp-3 hx:text-sm hx:font-normal hx:text-gray-500 hx:dark:text-gray-400 hx:px-4 hx:mb-4 hx:mt-2">Compact enclosure for Raspberry PI 4. It support RPI 4B and provide space for a 35*35 heatsink.&lt;/div>&lt;div class="hextra-badge hextra-card-tag">
&lt;div class="hx:inline-flex hx:gap-1 hx:items-center hx:rounded-full hx:px-2.5 hx:leading-6 hx:text-[.65rem] hx:border hx:text-gray-600 hx:bg-gray-100 hx:dark:bg-neutral-800 hx:dark:text-neutral-200 hx:border-gray-200 hx:dark:border-neutral-700">&lt;svg height=12 xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" aria-hidden="true">&lt;path stroke-linecap="round" stroke-linejoin="round" d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z"/>&lt;/svg>v1.1.0&lt;/div>
&lt;/div>
&lt;/a>
&lt;/div>
&lt;hr>
&lt;p>This enclosure is designed to be sleek and minimalistic. It is designed to be 3D printed. This case does not provide any camera or display port access. But GPIO are easily accessibles.&lt;/p>
&lt;p>&lt;img src="https://github.com/AntoninPvr/RPI4B_Case/blob/main/img/render_sd_hdmi.jpg?raw=true" alt="HDMI side view&amp;#34;" loading="lazy" />&lt;/p>
&lt;p>&lt;img src="https://github.com/AntoninPvr/RPI4B_Case/blob/main/img/render_hdmi_usb.jpg?raw=true" alt="“USB side view”" loading="lazy" />&lt;/p>
&lt;h2>Compatibility&lt;span class="hx:absolute hx:-mt-20" id="compatibility">&lt;/span>
&lt;a href="#compatibility" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>This enclosure support following boards:&lt;/p>
&lt;ul>
&lt;li>Raspberry PI 4B&lt;/li>
&lt;/ul>
&lt;h2>Files&lt;span class="hx:absolute hx:-mt-20" id="files">&lt;/span>
&lt;a href="#files" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Two versions of the upper part are available. One with GPIO access and one without. Ideal for a clean look or server applications like VPN, DNS&amp;hellip;&lt;/p>
&lt;div style="display: flex">
&lt;div style="width: 50%">
&lt;h3>Lower part&lt;span class="hx:absolute hx:-mt-20" id="lower-part">&lt;/span>
&lt;a href="#lower-part" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>&lt;strong>File&lt;/strong>: &lt;code>RPI_4b_case_bottom.STL&lt;/code> Bottom part of the enclosure common to all&lt;/p>
&lt;/div>
&lt;div id="./models/RPI_4b_case_bottom.glb" style="width: 50%; height: 50%">
&lt;/div>
&lt;script type="importmap">
{
"imports": {
"three": "https://unpkg.com/three@0.161.0/build/three.module.js",
"three/addons/": "https://unpkg.com/three@0.161.0/examples/jsm/"
}
}
&lt;/script>
&lt;script type="module">
import * as THREE from 'three';
import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { MeshoptDecoder } from 'three/addons/libs/meshopt_decoder.module.js';
import { DRACOLoader } from 'three/addons/loaders/DRACOLoader.js'
let container;
let camera, scene, renderer, clock;
let mesh;
let rotate, controls;
init();
render();
function init() {
container = document.getElementById(".\/models\/RPI_4b_case_bottom.glb");
container.onload = () => {
onWindowResize();
}
const aspectRatio = "1.3333";
const model_path = ".\/models\/RPI_4b_case_bottom.glb";
const modelColor = "#3f4d52";
rotate = "true" === "true";
renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
renderer.setPixelRatio(window.devicePixelRatio);
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1;
renderer.outputColorSpace = THREE.SRGBColorSpace;
container.appendChild(renderer.domElement);
camera = new THREE.PerspectiveCamera(45, aspectRatio, 0.1, 1000);
camera.position.set(1, 0, 1);
camera.lookAt(0, 0, 0);
controls = new OrbitControls(camera, renderer.domElement);
controls.addEventListener('change', render);
controls.minDistance = 1;
controls.maxDistance = 10;
controls.target.set(0, 0, 0);
controls.update();
const environment = new RoomEnvironment(renderer);
const pmremGenerator = new THREE.PMREMGenerator(renderer);
scene = new THREE.Scene();
scene.environment = pmremGenerator.fromScene(environment).texture;
const ambientLight = new THREE.AmbientLight(0xffffff, 1);
scene.add(ambientLight);
const light1 = new THREE.PointLight( 0xffeeee, 50, 0);
light1.position.set( 0, 2, 1 );
light1.castShadow = true;
scene.add( light1 );
const loader = new GLTFLoader();
const dracoLoader = new DRACOLoader();
dracoLoader.setDecoderPath('/examples/jsm/libs/draco/');
loader.setMeshoptDecoder(MeshoptDecoder);
loader.setDRACOLoader(dracoLoader);
loader.load(model_path, function (gltf) {
const root = gltf.scene;
const box = new THREE.Box3().setFromObject(root);
const size = box.getSize(new THREE.Vector3());
const center = box.getCenter(new THREE.Vector3());
root.position.x += (root.position.x - center.x);
root.position.y += (root.position.y - center.y);
root.position.z += (root.position.z - center.z);
mesh = new THREE.Group();
mesh.add(root);
if (modelColor &amp;&amp; modelColor !== "") {
root.traverse((child) => {
if (child.isMesh) {
child.material.color.set(modelColor);
child.material.roughness = 0.8;
child.material.metalness = 0.2;
}
});
}
root.traverse((child) => {
if (child.isMesh) {
child.castShadow = true;
child.receiveShadow = true;
}
});
const maxSize = Math.max(size.x, size.y, size.z);
mesh.scale.set(1 / maxSize, 1 / maxSize, 1 / maxSize);
scene.add(mesh);
if ( rotate ) {
clock = new THREE.Clock();
renderer.setAnimationLoop( animate );
controls.addEventListener( 'start', () => rotate = false );
controls.addEventListener( 'end', () => rotate = true );
}
render();
});
window.addEventListener('resize', onWindowResize);
}
function onWindowResize() {
const aspectRatio = "1.3333";
camera.aspect = aspectRatio;
camera.updateProjectionMatrix();
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
render();
}
function render() {
renderer.render(scene, camera);
}
function animate() {
const delta = clock.getDelta();
const direction = "right";
if ( rotate &amp;&amp; mesh ) {
const ry = 0.5;
if (direction === "left") {
mesh.rotation.y -= delta * ry;
} else {
mesh.rotation.y += delta * ry;
}
render();
}
}
&lt;/script>
&lt;/div>
&lt;div style="display: flex">
&lt;div style="width: 50%">
&lt;h3>Upper part GPIO&lt;span class="hx:absolute hx:-mt-20" id="upper-part-gpio">&lt;/span>
&lt;a href="#upper-part-gpio" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>&lt;strong>File&lt;/strong>: &lt;code>RPI_4b_case_top.STL&lt;/code> Orginal top part of the enclosure with GPIO access&lt;/p>
&lt;/div>
&lt;div id="./models/RPI_4b_case_top.glb" style="width: 50%; height: 50%">
&lt;/div>
&lt;script type="importmap">
{
"imports": {
"three": "https://unpkg.com/three@0.161.0/build/three.module.js",
"three/addons/": "https://unpkg.com/three@0.161.0/examples/jsm/"
}
}
&lt;/script>
&lt;script type="module">
import * as THREE from 'three';
import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { MeshoptDecoder } from 'three/addons/libs/meshopt_decoder.module.js';
import { DRACOLoader } from 'three/addons/loaders/DRACOLoader.js'
let container;
let camera, scene, renderer, clock;
let mesh;
let rotate, controls;
init();
render();
function init() {
container = document.getElementById(".\/models\/RPI_4b_case_top.glb");
container.onload = () => {
onWindowResize();
}
const aspectRatio = "1.3333";
const model_path = ".\/models\/RPI_4b_case_top.glb";
const modelColor = "#3f4d52";
rotate = "true" === "true";
renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
renderer.setPixelRatio(window.devicePixelRatio);
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1;
renderer.outputColorSpace = THREE.SRGBColorSpace;
container.appendChild(renderer.domElement);
camera = new THREE.PerspectiveCamera(45, aspectRatio, 0.1, 1000);
camera.position.set(1, 0, 1);
camera.lookAt(0, 0, 0);
controls = new OrbitControls(camera, renderer.domElement);
controls.addEventListener('change', render);
controls.minDistance = 1;
controls.maxDistance = 10;
controls.target.set(0, 0, 0);
controls.update();
const environment = new RoomEnvironment(renderer);
const pmremGenerator = new THREE.PMREMGenerator(renderer);
scene = new THREE.Scene();
scene.environment = pmremGenerator.fromScene(environment).texture;
const ambientLight = new THREE.AmbientLight(0xffffff, 1);
scene.add(ambientLight);
const light1 = new THREE.PointLight( 0xffeeee, 50, 0);
light1.position.set( 0, 2, 1 );
light1.castShadow = true;
scene.add( light1 );
const loader = new GLTFLoader();
const dracoLoader = new DRACOLoader();
dracoLoader.setDecoderPath('/examples/jsm/libs/draco/');
loader.setMeshoptDecoder(MeshoptDecoder);
loader.setDRACOLoader(dracoLoader);
loader.load(model_path, function (gltf) {
const root = gltf.scene;
const box = new THREE.Box3().setFromObject(root);
const size = box.getSize(new THREE.Vector3());
const center = box.getCenter(new THREE.Vector3());
root.position.x += (root.position.x - center.x);
root.position.y += (root.position.y - center.y);
root.position.z += (root.position.z - center.z);
mesh = new THREE.Group();
mesh.add(root);
if (modelColor &amp;&amp; modelColor !== "") {
root.traverse((child) => {
if (child.isMesh) {
child.material.color.set(modelColor);
child.material.roughness = 0.8;
child.material.metalness = 0.2;
}
});
}
root.traverse((child) => {
if (child.isMesh) {
child.castShadow = true;
child.receiveShadow = true;
}
});
const maxSize = Math.max(size.x, size.y, size.z);
mesh.scale.set(1 / maxSize, 1 / maxSize, 1 / maxSize);
scene.add(mesh);
if ( rotate ) {
clock = new THREE.Clock();
renderer.setAnimationLoop( animate );
controls.addEventListener( 'start', () => rotate = false );
controls.addEventListener( 'end', () => rotate = true );
}
render();
});
window.addEventListener('resize', onWindowResize);
}
function onWindowResize() {
const aspectRatio = "1.3333";
camera.aspect = aspectRatio;
camera.updateProjectionMatrix();
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
render();
}
function render() {
renderer.render(scene, camera);
}
function animate() {
const delta = clock.getDelta();
const direction = "left";
if ( rotate &amp;&amp; mesh ) {
const ry = 0.5;
if (direction === "left") {
mesh.rotation.y -= delta * ry;
} else {
mesh.rotation.y += delta * ry;
}
render();
}
}
&lt;/script>
&lt;/div>
&lt;div style="display: flex">
&lt;div style="width: 50%">
&lt;h3>Upper part without GPIO&lt;span class="hx:absolute hx:-mt-20" id="upper-part-without-gpio">&lt;/span>
&lt;a href="#upper-part-without-gpio" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>&lt;strong>File&lt;/strong>: &lt;code>RPI_4b_case_top_no_gpio.STL&lt;/code> Top part of the enclosure without GPIO access&lt;/p>
&lt;/div>
&lt;div id="./models/RPI_4b_case_top_no_gpio.glb" style="width: 50%; height: 50%">
&lt;/div>
&lt;script type="importmap">
{
"imports": {
"three": "https://unpkg.com/three@0.161.0/build/three.module.js",
"three/addons/": "https://unpkg.com/three@0.161.0/examples/jsm/"
}
}
&lt;/script>
&lt;script type="module">
import * as THREE from 'three';
import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { MeshoptDecoder } from 'three/addons/libs/meshopt_decoder.module.js';
import { DRACOLoader } from 'three/addons/loaders/DRACOLoader.js'
let container;
let camera, scene, renderer, clock;
let mesh;
let rotate, controls;
init();
render();
function init() {
container = document.getElementById(".\/models\/RPI_4b_case_top_no_gpio.glb");
container.onload = () => {
onWindowResize();
}
const aspectRatio = "1.3333";
const model_path = ".\/models\/RPI_4b_case_top_no_gpio.glb";
const modelColor = "#3f4d52";
rotate = "true" === "true";
renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
renderer.setPixelRatio(window.devicePixelRatio);
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1;
renderer.outputColorSpace = THREE.SRGBColorSpace;
container.appendChild(renderer.domElement);
camera = new THREE.PerspectiveCamera(45, aspectRatio, 0.1, 1000);
camera.position.set(1, 0, 1);
camera.lookAt(0, 0, 0);
controls = new OrbitControls(camera, renderer.domElement);
controls.addEventListener('change', render);
controls.minDistance = 1;
controls.maxDistance = 10;
controls.target.set(0, 0, 0);
controls.update();
const environment = new RoomEnvironment(renderer);
const pmremGenerator = new THREE.PMREMGenerator(renderer);
scene = new THREE.Scene();
scene.environment = pmremGenerator.fromScene(environment).texture;
const ambientLight = new THREE.AmbientLight(0xffffff, 1);
scene.add(ambientLight);
const light1 = new THREE.PointLight( 0xffeeee, 50, 0);
light1.position.set( 0, 2, 1 );
light1.castShadow = true;
scene.add( light1 );
const loader = new GLTFLoader();
const dracoLoader = new DRACOLoader();
dracoLoader.setDecoderPath('/examples/jsm/libs/draco/');
loader.setMeshoptDecoder(MeshoptDecoder);
loader.setDRACOLoader(dracoLoader);
loader.load(model_path, function (gltf) {
const root = gltf.scene;
const box = new THREE.Box3().setFromObject(root);
const size = box.getSize(new THREE.Vector3());
const center = box.getCenter(new THREE.Vector3());
root.position.x += (root.position.x - center.x);
root.position.y += (root.position.y - center.y);
root.position.z += (root.position.z - center.z);
mesh = new THREE.Group();
mesh.add(root);
if (modelColor &amp;&amp; modelColor !== "") {
root.traverse((child) => {
if (child.isMesh) {
child.material.color.set(modelColor);
child.material.roughness = 0.8;
child.material.metalness = 0.2;
}
});
}
root.traverse((child) => {
if (child.isMesh) {
child.castShadow = true;
child.receiveShadow = true;
}
});
const maxSize = Math.max(size.x, size.y, size.z);
mesh.scale.set(1 / maxSize, 1 / maxSize, 1 / maxSize);
scene.add(mesh);
if ( rotate ) {
clock = new THREE.Clock();
renderer.setAnimationLoop( animate );
controls.addEventListener( 'start', () => rotate = false );
controls.addEventListener( 'end', () => rotate = true );
}
render();
});
window.addEventListener('resize', onWindowResize);
}
function onWindowResize() {
const aspectRatio = "1.3333";
camera.aspect = aspectRatio;
camera.updateProjectionMatrix();
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
render();
}
function render() {
renderer.render(scene, camera);
}
function animate() {
const delta = clock.getDelta();
const direction = "left";
if ( rotate &amp;&amp; mesh ) {
const ry = 0.5;
if (direction === "left") {
mesh.rotation.y -= delta * ry;
} else {
mesh.rotation.y += delta * ry;
}
render();
}
}
&lt;/script>
&lt;/div>
&lt;div style="display: flex">
&lt;div style="width: 50%">
&lt;h3>GPIO Cover&lt;span class="hx:absolute hx:-mt-20" id="gpio-cover">&lt;/span>
&lt;a href="#gpio-cover" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>&lt;strong>File&lt;/strong>: &lt;code>gpio_cover.STL&lt;/code> Cover for GPIO. Best of both worlds, clean look and easy access to GPIO&lt;/p>
&lt;/div>
&lt;div id="./models/gpio_cover.glb" style="width: 50%; height: 50%">
&lt;/div>
&lt;script type="importmap">
{
"imports": {
"three": "https://unpkg.com/three@0.161.0/build/three.module.js",
"three/addons/": "https://unpkg.com/three@0.161.0/examples/jsm/"
}
}
&lt;/script>
&lt;script type="module">
import * as THREE from 'three';
import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { MeshoptDecoder } from 'three/addons/libs/meshopt_decoder.module.js';
import { DRACOLoader } from 'three/addons/loaders/DRACOLoader.js'
let container;
let camera, scene, renderer, clock;
let mesh;
let rotate, controls;
init();
render();
function init() {
container = document.getElementById(".\/models\/gpio_cover.glb");
container.onload = () => {
onWindowResize();
}
const aspectRatio = "1.3333";
const model_path = ".\/models\/gpio_cover.glb";
const modelColor = "#3f4d52";
rotate = "true" === "true";
renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
renderer.setPixelRatio(window.devicePixelRatio);
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1;
renderer.outputColorSpace = THREE.SRGBColorSpace;
container.appendChild(renderer.domElement);
camera = new THREE.PerspectiveCamera(45, aspectRatio, 0.1, 1000);
camera.position.set(1, 0, 1);
camera.lookAt(0, 0, 0);
controls = new OrbitControls(camera, renderer.domElement);
controls.addEventListener('change', render);
controls.minDistance = 1;
controls.maxDistance = 10;
controls.target.set(0, 0, 0);
controls.update();
const environment = new RoomEnvironment(renderer);
const pmremGenerator = new THREE.PMREMGenerator(renderer);
scene = new THREE.Scene();
scene.environment = pmremGenerator.fromScene(environment).texture;
const ambientLight = new THREE.AmbientLight(0xffffff, 1);
scene.add(ambientLight);
const light1 = new THREE.PointLight( 0xffeeee, 50, 0);
light1.position.set( 0, 2, 1 );
light1.castShadow = true;
scene.add( light1 );
const loader = new GLTFLoader();
const dracoLoader = new DRACOLoader();
dracoLoader.setDecoderPath('/examples/jsm/libs/draco/');
loader.setMeshoptDecoder(MeshoptDecoder);
loader.setDRACOLoader(dracoLoader);
loader.load(model_path, function (gltf) {
const root = gltf.scene;
const box = new THREE.Box3().setFromObject(root);
const size = box.getSize(new THREE.Vector3());
const center = box.getCenter(new THREE.Vector3());
root.position.x += (root.position.x - center.x);
root.position.y += (root.position.y - center.y);
root.position.z += (root.position.z - center.z);
mesh = new THREE.Group();
mesh.add(root);
if (modelColor &amp;&amp; modelColor !== "") {
root.traverse((child) => {
if (child.isMesh) {
child.material.color.set(modelColor);
child.material.roughness = 0.8;
child.material.metalness = 0.2;
}
});
}
root.traverse((child) => {
if (child.isMesh) {
child.castShadow = true;
child.receiveShadow = true;
}
});
const maxSize = Math.max(size.x, size.y, size.z);
mesh.scale.set(1 / maxSize, 1 / maxSize, 1 / maxSize);
scene.add(mesh);
if ( rotate ) {
clock = new THREE.Clock();
renderer.setAnimationLoop( animate );
controls.addEventListener( 'start', () => rotate = false );
controls.addEventListener( 'end', () => rotate = true );
}
render();
});
window.addEventListener('resize', onWindowResize);
}
function onWindowResize() {
const aspectRatio = "1.3333";
camera.aspect = aspectRatio;
camera.updateProjectionMatrix();
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
render();
}
function render() {
renderer.render(scene, camera);
}
function animate() {
const delta = clock.getDelta();
const direction = "right";
if ( rotate &amp;&amp; mesh ) {
const ry = 0.5;
if (direction === "left") {
mesh.rotation.y -= delta * ry;
} else {
mesh.rotation.y += delta * ry;
}
render();
}
}
&lt;/script>
&lt;/div>
&lt;h2>Thermal management&lt;span class="hx:absolute hx:-mt-20" id="thermal-management">&lt;/span>
&lt;a href="#thermal-management" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>This enclosure is designed to be used with passive cooling solution.&lt;/p>
&lt;p>No need for fan event with intense CPU usage.&lt;/p>
&lt;h2>Material and print settings&lt;span class="hx:absolute hx:-mt-20" id="material-and-print-settings">&lt;/span>
&lt;a href="#material-and-print-settings" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Clearance is 0.2mm for all parts to facilitate assembly. No support is needed.&lt;/p>
&lt;p>4x M3x24mm flat-head screws and 4x M3 nuts are needed to assemble the case.&lt;/p>
&lt;h2>Run button&lt;span class="hx:absolute hx:-mt-20" id="run-button">&lt;/span>
&lt;a href="#run-button" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>1mm hole above jack connector is designed to fit a 6x6mm push button. This button can be used to wake the Raspberry PI.&lt;/p></description></item><item><title>Raspberry PI 1-3 Enclosure</title><link>https://antoninpvr.github.io/hardware/rpi_case/rpi3/</link><pubDate>Sat, 03 Feb 2024 09:24:00 +0200</pubDate><guid>https://antoninpvr.github.io/hardware/rpi_case/rpi3/</guid><description>
&lt;img src="https://antoninpvr.github.io/hardware/rpi_case/rpi3/featured_hu_9ff0b8adc731a2ca.jpg" width="640" height="373"/>&lt;div class="hextra-cards hx:mt-4 hx:gap-4 hx:grid not-prose"
style="--hextra-cards-grid-cols: 3;">&lt;a
class="hextra-card hx:relative hx:group hx:flex hx:flex-col hx:justify-start hx:overflow-hidden hx:rounded-lg hx:border hx:border-gray-200 hx:text-current hx:no-underline hx:dark:shadow-none hx:hover:shadow-gray-100 hx:dark:hover:shadow-none hx:shadow-gray-100 hx:active:shadow-sm hx:active:shadow-gray-200 hx:transition-all hx:duration-200 hx:hover:border-gray-300 hx:bg-transparent hx:shadow-xs hx:dark:border-neutral-800 hx:hover:bg-slate-50 hx:hover:shadow-md hx:dark:hover:border-neutral-700 hx:dark:hover:bg-neutral-900"href="https://github.com/AntoninPvr/RPI_3Bplus_passive_enclosure"
target="_blank" rel="noreferrer">
&lt;span class="hextra-card-icon hx:flex hx:font-semibold hx:items-start hx:gap-2 hx:pt-4 hx:px-4 hx:text-gray-700 hx:hover:text-gray-900 hx:dark:text-neutral-200 hx:dark:hover:text-neutral-50">&lt;svg fill="currentColor" viewBox="3 3 18 18">
&lt;path d="M12 3C7.0275 3 3 7.12937 3 12.2276C3 16.3109 5.57625 19.7597 9.15374 20.9824C9.60374 21.0631 9.77249 20.7863 9.77249 20.5441C9.77249 20.3249 9.76125 19.5982 9.76125 18.8254C7.5 19.2522 6.915 18.2602 6.735 17.7412C6.63375 17.4759 6.19499 16.6569 5.8125 16.4378C5.4975 16.2647 5.0475 15.838 5.80124 15.8264C6.51 15.8149 7.01625 16.4954 7.18499 16.7723C7.99499 18.1679 9.28875 17.7758 9.80625 17.5335C9.885 16.9337 10.1212 16.53 10.38 16.2993C8.3775 16.0687 6.285 15.2728 6.285 11.7432C6.285 10.7397 6.63375 9.9092 7.20749 9.26326C7.1175 9.03257 6.8025 8.08674 7.2975 6.81794C7.2975 6.81794 8.05125 6.57571 9.77249 7.76377C10.4925 7.55615 11.2575 7.45234 12.0225 7.45234C12.7875 7.45234 13.5525 7.55615 14.2725 7.76377C15.9937 6.56418 16.7475 6.81794 16.7475 6.81794C17.2424 8.08674 16.9275 9.03257 16.8375 9.26326C17.4113 9.9092 17.76 10.7281 17.76 11.7432C17.76 15.2843 15.6563 16.0687 13.6537 16.2993C13.98 16.5877 14.2613 17.1414 14.2613 18.0065C14.2613 19.2407 14.25 20.2326 14.25 20.5441C14.25 20.7863 14.4188 21.0746 14.8688 20.9824C16.6554 20.364 18.2079 19.1866 19.3078 17.6162C20.4077 16.0457 20.9995 14.1611 21 12.2276C21 7.12937 16.9725 3 12 3Z">&lt;/path>
&lt;/svg>
RPI_3Bplus_passive_enclosure&lt;/span>
&lt;div class="hextra-card-subtitle hx:line-clamp-3 hx:text-sm hx:font-normal hx:text-gray-500 hx:dark:text-gray-400 hx:px-4 hx:mb-4 hx:mt-2">Compact enclosure for Raspberry PI. It support RPI from 1B+ to 3B+ and provide space for a 35*35 heatsink.&lt;/div>&lt;div class="hextra-badge hextra-card-tag">
&lt;div class="hx:inline-flex hx:gap-1 hx:items-center hx:rounded-full hx:px-2.5 hx:leading-6 hx:text-[.65rem] hx:border hx:text-gray-600 hx:bg-gray-100 hx:dark:bg-neutral-800 hx:dark:text-neutral-200 hx:border-gray-200 hx:dark:border-neutral-700">&lt;svg height=12 xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" aria-hidden="true">&lt;path stroke-linecap="round" stroke-linejoin="round" d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z"/>&lt;/svg>v2.0.0&lt;/div>
&lt;/div>
&lt;/a>
&lt;/div>
&lt;hr>
&lt;p>This enclosure is designed to be sleek and minimalistic. It is designed to be 3D printed. This case does not provide any camera or display port access. But GPIO are easily accessibles.&lt;/p>
&lt;p>&lt;img src="https://github.com/AntoninPvr/RPI_3Bplus_passive_enclosure/blob/main/img/render_sd_hdmi.jpg?raw=true" alt="HDMI side view&amp;#34;" loading="lazy" />&lt;/p>
&lt;p>&lt;img src="https://github.com/AntoninPvr/RPI_3Bplus_passive_enclosure/blob/main/img/render_hdmi_usb.jpg?raw=true" alt="“USB side view”" loading="lazy" />&lt;/p>
&lt;h2>Compatibility&lt;span class="hx:absolute hx:-mt-20" id="compatibility">&lt;/span>
&lt;a href="#compatibility" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>This enclosure support following boards:&lt;/p>
&lt;ul>
&lt;li>Raspberry PI 1B+&lt;/li>
&lt;li>Raspberry PI 2B&lt;/li>
&lt;li>Raspberry PI 2B 1.2&lt;/li>
&lt;li>Raspberry PI 3B&lt;/li>
&lt;li>Raspberry PI 3B+&lt;/li>
&lt;/ul>
&lt;h2>Files&lt;span class="hx:absolute hx:-mt-20" id="files">&lt;/span>
&lt;a href="#files" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Two versions of the upper part are available. One with GPIO access and one without. Ideal for a clean look or server applications like VPN, DNS&amp;hellip;&lt;/p>
&lt;div style="display: flex">
&lt;div style="width: 50%">
&lt;h3>Lower part&lt;span class="hx:absolute hx:-mt-20" id="lower-part">&lt;/span>
&lt;a href="#lower-part" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>&lt;strong>File&lt;/strong>: &lt;code>RPI_3bplus_enclosure_lower_1.2.STL&lt;/code> Lower part of the enclosure common to all&lt;/p>
&lt;/div>
&lt;div id="./models/RPI_3bplus_enclosure_lower_2.0.glb" style="width: 50%; height: 50%">
&lt;/div>
&lt;script type="importmap">
{
"imports": {
"three": "https://unpkg.com/three@0.161.0/build/three.module.js",
"three/addons/": "https://unpkg.com/three@0.161.0/examples/jsm/"
}
}
&lt;/script>
&lt;script type="module">
import * as THREE from 'three';
import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { MeshoptDecoder } from 'three/addons/libs/meshopt_decoder.module.js';
import { DRACOLoader } from 'three/addons/loaders/DRACOLoader.js'
let container;
let camera, scene, renderer, clock;
let mesh;
let rotate, controls;
init();
render();
function init() {
container = document.getElementById(".\/models\/RPI_3bplus_enclosure_lower_2.0.glb");
container.onload = () => {
onWindowResize();
}
const aspectRatio = "1.3333";
const model_path = ".\/models\/RPI_3bplus_enclosure_lower_2.0.glb";
const modelColor = "#3f4d52";
rotate = "true" === "true";
renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
renderer.setPixelRatio(window.devicePixelRatio);
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1;
renderer.outputColorSpace = THREE.SRGBColorSpace;
container.appendChild(renderer.domElement);
camera = new THREE.PerspectiveCamera(45, aspectRatio, 0.1, 1000);
camera.position.set(1, 0, 1);
camera.lookAt(0, 0, 0);
controls = new OrbitControls(camera, renderer.domElement);
controls.addEventListener('change', render);
controls.minDistance = 1;
controls.maxDistance = 10;
controls.target.set(0, 0, 0);
controls.update();
const environment = new RoomEnvironment(renderer);
const pmremGenerator = new THREE.PMREMGenerator(renderer);
scene = new THREE.Scene();
scene.environment = pmremGenerator.fromScene(environment).texture;
const ambientLight = new THREE.AmbientLight(0xffffff, 1);
scene.add(ambientLight);
const light1 = new THREE.PointLight( 0xffeeee, 50, 0);
light1.position.set( 0, 2, 1 );
light1.castShadow = true;
scene.add( light1 );
const loader = new GLTFLoader();
const dracoLoader = new DRACOLoader();
dracoLoader.setDecoderPath('/examples/jsm/libs/draco/');
loader.setMeshoptDecoder(MeshoptDecoder);
loader.setDRACOLoader(dracoLoader);
loader.load(model_path, function (gltf) {
const root = gltf.scene;
const box = new THREE.Box3().setFromObject(root);
const size = box.getSize(new THREE.Vector3());
const center = box.getCenter(new THREE.Vector3());
root.position.x += (root.position.x - center.x);
root.position.y += (root.position.y - center.y);
root.position.z += (root.position.z - center.z);
mesh = new THREE.Group();
mesh.add(root);
if (modelColor &amp;&amp; modelColor !== "") {
root.traverse((child) => {
if (child.isMesh) {
child.material.color.set(modelColor);
child.material.roughness = 0.8;
child.material.metalness = 0.2;
}
});
}
root.traverse((child) => {
if (child.isMesh) {
child.castShadow = true;
child.receiveShadow = true;
}
});
const maxSize = Math.max(size.x, size.y, size.z);
mesh.scale.set(1 / maxSize, 1 / maxSize, 1 / maxSize);
scene.add(mesh);
if ( rotate ) {
clock = new THREE.Clock();
renderer.setAnimationLoop( animate );
controls.addEventListener( 'start', () => rotate = false );
controls.addEventListener( 'end', () => rotate = true );
}
render();
});
window.addEventListener('resize', onWindowResize);
}
function onWindowResize() {
const aspectRatio = "1.3333";
camera.aspect = aspectRatio;
camera.updateProjectionMatrix();
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
render();
}
function render() {
renderer.render(scene, camera);
}
function animate() {
const delta = clock.getDelta();
const direction = "right";
if ( rotate &amp;&amp; mesh ) {
const ry = 0.5;
if (direction === "left") {
mesh.rotation.y -= delta * ry;
} else {
mesh.rotation.y += delta * ry;
}
render();
}
}
&lt;/script>
&lt;/div>
&lt;div style="display: flex">
&lt;div style="width: 50%">
&lt;h3>Upper part GPIO&lt;span class="hx:absolute hx:-mt-20" id="upper-part-gpio">&lt;/span>
&lt;a href="#upper-part-gpio" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>&lt;strong>File&lt;/strong>: &lt;code>RPI_3bplus_enclosure_upper_1.2.STL&lt;/code> Orginal upper part of the enclosure with GPIO access&lt;/p>
&lt;/div>
&lt;div id="./models/RPI_3bplus_enclosure_upper_2.0.glb" style="width: 50%; height: 50%">
&lt;/div>
&lt;script type="importmap">
{
"imports": {
"three": "https://unpkg.com/three@0.161.0/build/three.module.js",
"three/addons/": "https://unpkg.com/three@0.161.0/examples/jsm/"
}
}
&lt;/script>
&lt;script type="module">
import * as THREE from 'three';
import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { MeshoptDecoder } from 'three/addons/libs/meshopt_decoder.module.js';
import { DRACOLoader } from 'three/addons/loaders/DRACOLoader.js'
let container;
let camera, scene, renderer, clock;
let mesh;
let rotate, controls;
init();
render();
function init() {
container = document.getElementById(".\/models\/RPI_3bplus_enclosure_upper_2.0.glb");
container.onload = () => {
onWindowResize();
}
const aspectRatio = "1.3333";
const model_path = ".\/models\/RPI_3bplus_enclosure_upper_2.0.glb";
const modelColor = "#3f4d52";
rotate = "true" === "true";
renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
renderer.setPixelRatio(window.devicePixelRatio);
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1;
renderer.outputColorSpace = THREE.SRGBColorSpace;
container.appendChild(renderer.domElement);
camera = new THREE.PerspectiveCamera(45, aspectRatio, 0.1, 1000);
camera.position.set(1, 0, 1);
camera.lookAt(0, 0, 0);
controls = new OrbitControls(camera, renderer.domElement);
controls.addEventListener('change', render);
controls.minDistance = 1;
controls.maxDistance = 10;
controls.target.set(0, 0, 0);
controls.update();
const environment = new RoomEnvironment(renderer);
const pmremGenerator = new THREE.PMREMGenerator(renderer);
scene = new THREE.Scene();
scene.environment = pmremGenerator.fromScene(environment).texture;
const ambientLight = new THREE.AmbientLight(0xffffff, 1);
scene.add(ambientLight);
const light1 = new THREE.PointLight( 0xffeeee, 50, 0);
light1.position.set( 0, 2, 1 );
light1.castShadow = true;
scene.add( light1 );
const loader = new GLTFLoader();
const dracoLoader = new DRACOLoader();
dracoLoader.setDecoderPath('/examples/jsm/libs/draco/');
loader.setMeshoptDecoder(MeshoptDecoder);
loader.setDRACOLoader(dracoLoader);
loader.load(model_path, function (gltf) {
const root = gltf.scene;
const box = new THREE.Box3().setFromObject(root);
const size = box.getSize(new THREE.Vector3());
const center = box.getCenter(new THREE.Vector3());
root.position.x += (root.position.x - center.x);
root.position.y += (root.position.y - center.y);
root.position.z += (root.position.z - center.z);
mesh = new THREE.Group();
mesh.add(root);
if (modelColor &amp;&amp; modelColor !== "") {
root.traverse((child) => {
if (child.isMesh) {
child.material.color.set(modelColor);
child.material.roughness = 0.8;
child.material.metalness = 0.2;
}
});
}
root.traverse((child) => {
if (child.isMesh) {
child.castShadow = true;
child.receiveShadow = true;
}
});
const maxSize = Math.max(size.x, size.y, size.z);
mesh.scale.set(1 / maxSize, 1 / maxSize, 1 / maxSize);
scene.add(mesh);
if ( rotate ) {
clock = new THREE.Clock();
renderer.setAnimationLoop( animate );
controls.addEventListener( 'start', () => rotate = false );
controls.addEventListener( 'end', () => rotate = true );
}
render();
});
window.addEventListener('resize', onWindowResize);
}
function onWindowResize() {
const aspectRatio = "1.3333";
camera.aspect = aspectRatio;
camera.updateProjectionMatrix();
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
render();
}
function render() {
renderer.render(scene, camera);
}
function animate() {
const delta = clock.getDelta();
const direction = "left";
if ( rotate &amp;&amp; mesh ) {
const ry = 0.5;
if (direction === "left") {
mesh.rotation.y -= delta * ry;
} else {
mesh.rotation.y += delta * ry;
}
render();
}
}
&lt;/script>
&lt;/div>
&lt;div style="display: flex">
&lt;div style="width: 50%">
&lt;h3>Upper part without GPIO&lt;span class="hx:absolute hx:-mt-20" id="upper-part-without-gpio">&lt;/span>
&lt;a href="#upper-part-without-gpio" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>&lt;strong>File&lt;/strong>: &lt;code>RPI_3bplus_enclosure_upper_1.2_no_gpio.STL&lt;/code> Upper part of the enclosure without GPIO access&lt;/p>
&lt;/div>
&lt;div id="./models/RPI_3bplus_enclosure_upper_without_gpio_2.0.glb" style="width: 50%; height: 50%">
&lt;/div>
&lt;script type="importmap">
{
"imports": {
"three": "https://unpkg.com/three@0.161.0/build/three.module.js",
"three/addons/": "https://unpkg.com/three@0.161.0/examples/jsm/"
}
}
&lt;/script>
&lt;script type="module">
import * as THREE from 'three';
import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { MeshoptDecoder } from 'three/addons/libs/meshopt_decoder.module.js';
import { DRACOLoader } from 'three/addons/loaders/DRACOLoader.js'
let container;
let camera, scene, renderer, clock;
let mesh;
let rotate, controls;
init();
render();
function init() {
container = document.getElementById(".\/models\/RPI_3bplus_enclosure_upper_without_gpio_2.0.glb");
container.onload = () => {
onWindowResize();
}
const aspectRatio = "1.3333";
const model_path = ".\/models\/RPI_3bplus_enclosure_upper_without_gpio_2.0.glb";
const modelColor = "#3f4d52";
rotate = "true" === "true";
renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
renderer.setPixelRatio(window.devicePixelRatio);
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1;
renderer.outputColorSpace = THREE.SRGBColorSpace;
container.appendChild(renderer.domElement);
camera = new THREE.PerspectiveCamera(45, aspectRatio, 0.1, 1000);
camera.position.set(1, 0, 1);
camera.lookAt(0, 0, 0);
controls = new OrbitControls(camera, renderer.domElement);
controls.addEventListener('change', render);
controls.minDistance = 1;
controls.maxDistance = 10;
controls.target.set(0, 0, 0);
controls.update();
const environment = new RoomEnvironment(renderer);
const pmremGenerator = new THREE.PMREMGenerator(renderer);
scene = new THREE.Scene();
scene.environment = pmremGenerator.fromScene(environment).texture;
const ambientLight = new THREE.AmbientLight(0xffffff, 1);
scene.add(ambientLight);
const light1 = new THREE.PointLight( 0xffeeee, 50, 0);
light1.position.set( 0, 2, 1 );
light1.castShadow = true;
scene.add( light1 );
const loader = new GLTFLoader();
const dracoLoader = new DRACOLoader();
dracoLoader.setDecoderPath('/examples/jsm/libs/draco/');
loader.setMeshoptDecoder(MeshoptDecoder);
loader.setDRACOLoader(dracoLoader);
loader.load(model_path, function (gltf) {
const root = gltf.scene;
const box = new THREE.Box3().setFromObject(root);
const size = box.getSize(new THREE.Vector3());
const center = box.getCenter(new THREE.Vector3());
root.position.x += (root.position.x - center.x);
root.position.y += (root.position.y - center.y);
root.position.z += (root.position.z - center.z);
mesh = new THREE.Group();
mesh.add(root);
if (modelColor &amp;&amp; modelColor !== "") {
root.traverse((child) => {
if (child.isMesh) {
child.material.color.set(modelColor);
child.material.roughness = 0.8;
child.material.metalness = 0.2;
}
});
}
root.traverse((child) => {
if (child.isMesh) {
child.castShadow = true;
child.receiveShadow = true;
}
});
const maxSize = Math.max(size.x, size.y, size.z);
mesh.scale.set(1 / maxSize, 1 / maxSize, 1 / maxSize);
scene.add(mesh);
if ( rotate ) {
clock = new THREE.Clock();
renderer.setAnimationLoop( animate );
controls.addEventListener( 'start', () => rotate = false );
controls.addEventListener( 'end', () => rotate = true );
}
render();
});
window.addEventListener('resize', onWindowResize);
}
function onWindowResize() {
const aspectRatio = "1.3333";
camera.aspect = aspectRatio;
camera.updateProjectionMatrix();
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
render();
}
function render() {
renderer.render(scene, camera);
}
function animate() {
const delta = clock.getDelta();
const direction = "left";
if ( rotate &amp;&amp; mesh ) {
const ry = 0.5;
if (direction === "left") {
mesh.rotation.y -= delta * ry;
} else {
mesh.rotation.y += delta * ry;
}
render();
}
}
&lt;/script>
&lt;/div>
&lt;div style="display: flex">
&lt;div style="width: 50%">
&lt;h3>GPIO Cover&lt;span class="hx:absolute hx:-mt-20" id="gpio-cover">&lt;/span>
&lt;a href="#gpio-cover" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>&lt;strong>File&lt;/strong>: &lt;code>gpio_cover.STL&lt;/code> Cover for GPIO. Best of both worlds, clean look and easy access to GPIO&lt;/p>
&lt;/div>
&lt;div id="./models/gpio_cover.glb" style="width: 50%; height: 50%">
&lt;/div>
&lt;script type="importmap">
{
"imports": {
"three": "https://unpkg.com/three@0.161.0/build/three.module.js",
"three/addons/": "https://unpkg.com/three@0.161.0/examples/jsm/"
}
}
&lt;/script>
&lt;script type="module">
import * as THREE from 'three';
import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { MeshoptDecoder } from 'three/addons/libs/meshopt_decoder.module.js';
import { DRACOLoader } from 'three/addons/loaders/DRACOLoader.js'
let container;
let camera, scene, renderer, clock;
let mesh;
let rotate, controls;
init();
render();
function init() {
container = document.getElementById(".\/models\/gpio_cover.glb");
container.onload = () => {
onWindowResize();
}
const aspectRatio = "1.3333";
const model_path = ".\/models\/gpio_cover.glb";
const modelColor = "#3f4d52";
rotate = "true" === "true";
renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
renderer.setPixelRatio(window.devicePixelRatio);
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1;
renderer.outputColorSpace = THREE.SRGBColorSpace;
container.appendChild(renderer.domElement);
camera = new THREE.PerspectiveCamera(45, aspectRatio, 0.1, 1000);
camera.position.set(1, 0, 1);
camera.lookAt(0, 0, 0);
controls = new OrbitControls(camera, renderer.domElement);
controls.addEventListener('change', render);
controls.minDistance = 1;
controls.maxDistance = 10;
controls.target.set(0, 0, 0);
controls.update();
const environment = new RoomEnvironment(renderer);
const pmremGenerator = new THREE.PMREMGenerator(renderer);
scene = new THREE.Scene();
scene.environment = pmremGenerator.fromScene(environment).texture;
const ambientLight = new THREE.AmbientLight(0xffffff, 1);
scene.add(ambientLight);
const light1 = new THREE.PointLight( 0xffeeee, 50, 0);
light1.position.set( 0, 2, 1 );
light1.castShadow = true;
scene.add( light1 );
const loader = new GLTFLoader();
const dracoLoader = new DRACOLoader();
dracoLoader.setDecoderPath('/examples/jsm/libs/draco/');
loader.setMeshoptDecoder(MeshoptDecoder);
loader.setDRACOLoader(dracoLoader);
loader.load(model_path, function (gltf) {
const root = gltf.scene;
const box = new THREE.Box3().setFromObject(root);
const size = box.getSize(new THREE.Vector3());
const center = box.getCenter(new THREE.Vector3());
root.position.x += (root.position.x - center.x);
root.position.y += (root.position.y - center.y);
root.position.z += (root.position.z - center.z);
mesh = new THREE.Group();
mesh.add(root);
if (modelColor &amp;&amp; modelColor !== "") {
root.traverse((child) => {
if (child.isMesh) {
child.material.color.set(modelColor);
child.material.roughness = 0.8;
child.material.metalness = 0.2;
}
});
}
root.traverse((child) => {
if (child.isMesh) {
child.castShadow = true;
child.receiveShadow = true;
}
});
const maxSize = Math.max(size.x, size.y, size.z);
mesh.scale.set(1 / maxSize, 1 / maxSize, 1 / maxSize);
scene.add(mesh);
if ( rotate ) {
clock = new THREE.Clock();
renderer.setAnimationLoop( animate );
controls.addEventListener( 'start', () => rotate = false );
controls.addEventListener( 'end', () => rotate = true );
}
render();
});
window.addEventListener('resize', onWindowResize);
}
function onWindowResize() {
const aspectRatio = "1.3333";
camera.aspect = aspectRatio;
camera.updateProjectionMatrix();
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
render();
}
function render() {
renderer.render(scene, camera);
}
function animate() {
const delta = clock.getDelta();
const direction = "right";
if ( rotate &amp;&amp; mesh ) {
const ry = 0.5;
if (direction === "left") {
mesh.rotation.y -= delta * ry;
} else {
mesh.rotation.y += delta * ry;
}
render();
}
}
&lt;/script>
&lt;/div>
&lt;h2>Thermal management&lt;span class="hx:absolute hx:-mt-20" id="thermal-management">&lt;/span>
&lt;a href="#thermal-management" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>This enclosure is designed to be used with passive cooling solution.&lt;/p>
&lt;p>A 1mm copper plate between the CPU and the heatsink is needed to allow space for surrounding components.&lt;/p>
&lt;h2>Material and print settings&lt;span class="hx:absolute hx:-mt-20" id="material-and-print-settings">&lt;/span>
&lt;a href="#material-and-print-settings" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Clearance is 0.2mm for all parts to facilitate assembly. No support is needed.&lt;/p>
&lt;p>Screws are no longer needed&lt;/p>
&lt;h2>Photos&lt;span class="hx:absolute hx:-mt-20" id="photos">&lt;/span>
&lt;a href="#photos" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Version 1.0 without clearance for SD card removal&lt;/p>
&lt;p>Printing settings:&lt;/p>
&lt;ul>
&lt;li>3D printer: Zortrax M200+&lt;/li>
&lt;li>Material: Z-PLA Pro&lt;/li>
&lt;li>Nozzle diameter: 0.4mm&lt;/li>
&lt;li>High quality&lt;/li>
&lt;li>Layer height: 0.09mm&lt;/li>
&lt;li>Infill: 20%&lt;/li>
&lt;li>No support&lt;/li>
&lt;/ul>
&lt;p>&lt;figure>
&lt;img src="https://github.com/AntoninPvr/RPI_3Bplus_passive_enclosure/blob/main/img/gpio.jpg?raw=true" title="Run button hole above jack connector" alt="“Run button hole above jack connector”" loading="lazy" />
&lt;figcaption>Run button hole above jack connector&lt;/figcaption>
&lt;/figure>&lt;/p>
&lt;p>&lt;figure>
&lt;img src="https://github.com/AntoninPvr/RPI_3Bplus_passive_enclosure/blob/main/img/isometric.jpg?raw=true" title="Little issue with the GPIO border, was to tight. fixed." alt="“Little issue with the GPIO border, was to tight. fixed.”" loading="lazy" />
&lt;figcaption>Little issue with the GPIO border, was to tight. fixed.&lt;/figcaption>
&lt;/figure>&lt;/p>
&lt;p>&lt;figure>
&lt;img src="https://github.com/AntoninPvr/RPI_3Bplus_passive_enclosure/blob/main/img/rpi3b.jpg?raw=true" title="Kapton tape is used to isolate heatsink from the board." alt="“Kapton tape is used to isolate heatsink from the board.”" loading="lazy" />
&lt;figcaption>Kapton tape is used to isolate heatsink from the board.&lt;/figcaption>
&lt;/figure>&lt;/p></description></item><item><title>Single-board computer cases</title><link>https://antoninpvr.github.io/hardware/rpi_case/</link><pubDate>Sat, 03 Feb 2024 09:24:00 +0200</pubDate><guid>https://antoninpvr.github.io/hardware/rpi_case/</guid><description>
&lt;img src="https://antoninpvr.github.io/hardware/rpi_case/featured_hu_9da9b7df292243df.jpg" width="640" height="379"/></description></item><item><title>Arduino Nano Enclosure</title><link>https://antoninpvr.github.io/hardware/dev_board/arduino_nano/</link><pubDate>Tue, 04 Jul 2023 09:23:00 +0200</pubDate><guid>https://antoninpvr.github.io/hardware/dev_board/arduino_nano/</guid><description>
&lt;img src="https://antoninpvr.github.io/hardware/dev_board/arduino_nano/featured_hu_9d32bcfa82d68c19.jpg" width="640" height="512"/>&lt;div class="hextra-cards hx:mt-4 hx:gap-4 hx:grid not-prose"
style="--hextra-cards-grid-cols: 3;">&lt;a
class="hextra-card hx:relative hx:group hx:flex hx:flex-col hx:justify-start hx:overflow-hidden hx:rounded-lg hx:border hx:border-gray-200 hx:text-current hx:no-underline hx:dark:shadow-none hx:hover:shadow-gray-100 hx:dark:hover:shadow-none hx:shadow-gray-100 hx:active:shadow-sm hx:active:shadow-gray-200 hx:transition-all hx:duration-200 hx:hover:border-gray-300 hx:bg-transparent hx:shadow-xs hx:dark:border-neutral-800 hx:hover:bg-slate-50 hx:hover:shadow-md hx:dark:hover:border-neutral-700 hx:dark:hover:bg-neutral-900"href="https://github.com/AntoninPvr/dev-board-enclosures"
target="_blank" rel="noreferrer">
&lt;span class="hextra-card-icon hx:flex hx:font-semibold hx:items-start hx:gap-2 hx:pt-4 hx:px-4 hx:text-gray-700 hx:hover:text-gray-900 hx:dark:text-neutral-200 hx:dark:hover:text-neutral-50">&lt;svg fill="currentColor" viewBox="3 3 18 18">
&lt;path d="M12 3C7.0275 3 3 7.12937 3 12.2276C3 16.3109 5.57625 19.7597 9.15374 20.9824C9.60374 21.0631 9.77249 20.7863 9.77249 20.5441C9.77249 20.3249 9.76125 19.5982 9.76125 18.8254C7.5 19.2522 6.915 18.2602 6.735 17.7412C6.63375 17.4759 6.19499 16.6569 5.8125 16.4378C5.4975 16.2647 5.0475 15.838 5.80124 15.8264C6.51 15.8149 7.01625 16.4954 7.18499 16.7723C7.99499 18.1679 9.28875 17.7758 9.80625 17.5335C9.885 16.9337 10.1212 16.53 10.38 16.2993C8.3775 16.0687 6.285 15.2728 6.285 11.7432C6.285 10.7397 6.63375 9.9092 7.20749 9.26326C7.1175 9.03257 6.8025 8.08674 7.2975 6.81794C7.2975 6.81794 8.05125 6.57571 9.77249 7.76377C10.4925 7.55615 11.2575 7.45234 12.0225 7.45234C12.7875 7.45234 13.5525 7.55615 14.2725 7.76377C15.9937 6.56418 16.7475 6.81794 16.7475 6.81794C17.2424 8.08674 16.9275 9.03257 16.8375 9.26326C17.4113 9.9092 17.76 10.7281 17.76 11.7432C17.76 15.2843 15.6563 16.0687 13.6537 16.2993C13.98 16.5877 14.2613 17.1414 14.2613 18.0065C14.2613 19.2407 14.25 20.2326 14.25 20.5441C14.25 20.7863 14.4188 21.0746 14.8688 20.9824C16.6554 20.364 18.2079 19.1866 19.3078 17.6162C20.4077 16.0457 20.9995 14.1611 21 12.2276C21 7.12937 16.9725 3 12 3Z">&lt;/path>
&lt;/svg>
dev-board-enclosures&lt;/span>
&lt;div class="hextra-card-subtitle hx:line-clamp-3 hx:text-sm hx:font-normal hx:text-gray-500 hx:dark:text-gray-400 hx:px-4 hx:mb-4 hx:mt-2">Simple enclosures for dev boards&lt;/div>&lt;div class="hextra-badge hextra-card-tag">
&lt;div class="hx:inline-flex hx:gap-1 hx:items-center hx:rounded-full hx:px-2.5 hx:leading-6 hx:text-[.65rem] hx:border hx:text-gray-600 hx:bg-gray-100 hx:dark:bg-neutral-800 hx:dark:text-neutral-200 hx:border-gray-200 hx:dark:border-neutral-700">&lt;svg height=12 xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" aria-hidden="true">&lt;path stroke-linecap="round" stroke-linejoin="round" d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z"/>&lt;/svg>no-release&lt;/div>
&lt;/div>
&lt;/a>
&lt;/div>
&lt;h3>Overview&lt;span class="hx:absolute hx:-mt-20" id="overview">&lt;/span>
&lt;a href="#overview" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Simple enclosure for Arduino Nano. It is designed to be 3D printed.&lt;/p>
&lt;h3>Parts&lt;span class="hx:absolute hx:-mt-20" id="parts">&lt;/span>
&lt;a href="#parts" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;ul>
&lt;li>&lt;code>arduino-nano_top.stl&lt;/code> Top part of the enclosure&lt;/li>
&lt;li>&lt;code>arduino-nano_bottom.stl&lt;/code> Bottom part of the enclosure&lt;/li>
&lt;/ul>
&lt;div style="display: flex">
&lt;div id="arduino-nano_top.glb" style="width: 50%; height: 50%">
&lt;/div>
&lt;script type="importmap">
{
"imports": {
"three": "https://unpkg.com/three@0.161.0/build/three.module.js",
"three/addons/": "https://unpkg.com/three@0.161.0/examples/jsm/"
}
}
&lt;/script>
&lt;script type="module">
import * as THREE from 'three';
import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { MeshoptDecoder } from 'three/addons/libs/meshopt_decoder.module.js';
import { DRACOLoader } from 'three/addons/loaders/DRACOLoader.js'
let container;
let camera, scene, renderer, clock;
let mesh;
let rotate, controls;
init();
render();
function init() {
container = document.getElementById("arduino-nano_top.glb");
container.onload = () => {
onWindowResize();
}
const aspectRatio = "1.3333";
const model_path = "arduino-nano_top.glb";
const modelColor = "#2a91bc";
rotate = "true" === "true";
renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
renderer.setPixelRatio(window.devicePixelRatio);
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1;
renderer.outputColorSpace = THREE.SRGBColorSpace;
container.appendChild(renderer.domElement);
camera = new THREE.PerspectiveCamera(45, aspectRatio, 0.1, 1000);
camera.position.set(1, 0, 1);
camera.lookAt(0, 0, 0);
controls = new OrbitControls(camera, renderer.domElement);
controls.addEventListener('change', render);
controls.minDistance = 1;
controls.maxDistance = 10;
controls.target.set(0, 0, 0);
controls.update();
const environment = new RoomEnvironment(renderer);
const pmremGenerator = new THREE.PMREMGenerator(renderer);
scene = new THREE.Scene();
scene.environment = pmremGenerator.fromScene(environment).texture;
const ambientLight = new THREE.AmbientLight(0xffffff, 1);
scene.add(ambientLight);
const light1 = new THREE.PointLight( 0xffeeee, 50, 0);
light1.position.set( 0, 2, 1 );
light1.castShadow = true;
scene.add( light1 );
const loader = new GLTFLoader();
const dracoLoader = new DRACOLoader();
dracoLoader.setDecoderPath('/examples/jsm/libs/draco/');
loader.setMeshoptDecoder(MeshoptDecoder);
loader.setDRACOLoader(dracoLoader);
loader.load(model_path, function (gltf) {
const root = gltf.scene;
const box = new THREE.Box3().setFromObject(root);
const size = box.getSize(new THREE.Vector3());
const center = box.getCenter(new THREE.Vector3());
root.position.x += (root.position.x - center.x);
root.position.y += (root.position.y - center.y);
root.position.z += (root.position.z - center.z);
mesh = new THREE.Group();
mesh.add(root);
if (modelColor &amp;&amp; modelColor !== "") {
root.traverse((child) => {
if (child.isMesh) {
child.material.color.set(modelColor);
child.material.roughness = 0.8;
child.material.metalness = 0.2;
}
});
}
root.traverse((child) => {
if (child.isMesh) {
child.castShadow = true;
child.receiveShadow = true;
}
});
const maxSize = Math.max(size.x, size.y, size.z);
mesh.scale.set(1 / maxSize, 1 / maxSize, 1 / maxSize);
scene.add(mesh);
if ( rotate ) {
clock = new THREE.Clock();
renderer.setAnimationLoop( animate );
controls.addEventListener( 'start', () => rotate = false );
controls.addEventListener( 'end', () => rotate = true );
}
render();
});
window.addEventListener('resize', onWindowResize);
}
function onWindowResize() {
const aspectRatio = "1.3333";
camera.aspect = aspectRatio;
camera.updateProjectionMatrix();
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
render();
}
function render() {
renderer.render(scene, camera);
}
function animate() {
const delta = clock.getDelta();
const direction = "left";
if ( rotate &amp;&amp; mesh ) {
const ry = 0.5;
if (direction === "left") {
mesh.rotation.y -= delta * ry;
} else {
mesh.rotation.y += delta * ry;
}
render();
}
}
&lt;/script>
&lt;div id="arduino-nano_bottom.glb" style="width: 50%; height: 50%">
&lt;/div>
&lt;script type="importmap">
{
"imports": {
"three": "https://unpkg.com/three@0.161.0/build/three.module.js",
"three/addons/": "https://unpkg.com/three@0.161.0/examples/jsm/"
}
}
&lt;/script>
&lt;script type="module">
import * as THREE from 'three';
import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { MeshoptDecoder } from 'three/addons/libs/meshopt_decoder.module.js';
import { DRACOLoader } from 'three/addons/loaders/DRACOLoader.js'
let container;
let camera, scene, renderer, clock;
let mesh;
let rotate, controls;
init();
render();
function init() {
container = document.getElementById("arduino-nano_bottom.glb");
container.onload = () => {
onWindowResize();
}
const aspectRatio = "1.3333";
const model_path = "arduino-nano_bottom.glb";
const modelColor = "#2a91bc";
rotate = "true" === "true";
renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
renderer.setPixelRatio(window.devicePixelRatio);
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1;
renderer.outputColorSpace = THREE.SRGBColorSpace;
container.appendChild(renderer.domElement);
camera = new THREE.PerspectiveCamera(45, aspectRatio, 0.1, 1000);
camera.position.set(1, 0, 1);
camera.lookAt(0, 0, 0);
controls = new OrbitControls(camera, renderer.domElement);
controls.addEventListener('change', render);
controls.minDistance = 1;
controls.maxDistance = 10;
controls.target.set(0, 0, 0);
controls.update();
const environment = new RoomEnvironment(renderer);
const pmremGenerator = new THREE.PMREMGenerator(renderer);
scene = new THREE.Scene();
scene.environment = pmremGenerator.fromScene(environment).texture;
const ambientLight = new THREE.AmbientLight(0xffffff, 1);
scene.add(ambientLight);
const light1 = new THREE.PointLight( 0xffeeee, 50, 0);
light1.position.set( 0, 2, 1 );
light1.castShadow = true;
scene.add( light1 );
const loader = new GLTFLoader();
const dracoLoader = new DRACOLoader();
dracoLoader.setDecoderPath('/examples/jsm/libs/draco/');
loader.setMeshoptDecoder(MeshoptDecoder);
loader.setDRACOLoader(dracoLoader);
loader.load(model_path, function (gltf) {
const root = gltf.scene;
const box = new THREE.Box3().setFromObject(root);
const size = box.getSize(new THREE.Vector3());
const center = box.getCenter(new THREE.Vector3());
root.position.x += (root.position.x - center.x);
root.position.y += (root.position.y - center.y);
root.position.z += (root.position.z - center.z);
mesh = new THREE.Group();
mesh.add(root);
if (modelColor &amp;&amp; modelColor !== "") {
root.traverse((child) => {
if (child.isMesh) {
child.material.color.set(modelColor);
child.material.roughness = 0.8;
child.material.metalness = 0.2;
}
});
}
root.traverse((child) => {
if (child.isMesh) {
child.castShadow = true;
child.receiveShadow = true;
}
});
const maxSize = Math.max(size.x, size.y, size.z);
mesh.scale.set(1 / maxSize, 1 / maxSize, 1 / maxSize);
scene.add(mesh);
if ( rotate ) {
clock = new THREE.Clock();
renderer.setAnimationLoop( animate );
controls.addEventListener( 'start', () => rotate = false );
controls.addEventListener( 'end', () => rotate = true );
}
render();
});
window.addEventListener('resize', onWindowResize);
}
function onWindowResize() {
const aspectRatio = "1.3333";
camera.aspect = aspectRatio;
camera.updateProjectionMatrix();
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
render();
}
function render() {
renderer.render(scene, camera);
}
function animate() {
const delta = clock.getDelta();
const direction = "right";
if ( rotate &amp;&amp; mesh ) {
const ry = 0.5;
if (direction === "left") {
mesh.rotation.y -= delta * ry;
} else {
mesh.rotation.y += delta * ry;
}
render();
}
}
&lt;/script>
&lt;/div>
&lt;h3>Assembly&lt;span class="hx:absolute hx:-mt-20" id="assembly">&lt;/span>
&lt;a href="#assembly" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Provided 3mf file is tested and ready to print on Prusa MK3 .
Parts are hold together by friction. No screws are needed.&lt;/p>
&lt;h3>Download&lt;span class="hx:absolute hx:-mt-20" id="download">&lt;/span>
&lt;a href="#download" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;ul>
&lt;li>&lt;a href="https://github.com/AntoninPvr/dev-board-enclosures/tree/main/arduino-nano/stl"target="_blank" rel="noopener">STL files&lt;/a>&lt;/li>
&lt;li>&lt;a href="https://github.com/AntoninPvr/dev-board-enclosures/tree/main/arduino-nano/3mf"target="_blank" rel="noopener">3MF file&lt;/a>&lt;/li>
&lt;li>&lt;a href="https://github.com/AntoninPvr/dev-board-enclosures/tree/main/arduino-nano/step"target="_blank" rel="noopener">STEP files&lt;/a>&lt;/li>
&lt;/ul></description></item><item><title>Dev board enclosures</title><link>https://antoninpvr.github.io/hardware/dev_board/</link><pubDate>Tue, 04 Jul 2023 09:23:00 +0200</pubDate><guid>https://antoninpvr.github.io/hardware/dev_board/</guid><description>
&lt;img src="https://antoninpvr.github.io/hardware/dev_board/featured_hu_7ecd82e3930f6c2d.jpg" width="640" height="512"/></description></item><item><title>ESP32 WROOM enclosure</title><link>https://antoninpvr.github.io/hardware/dev_board/esp32_wroom/</link><pubDate>Tue, 04 Jul 2023 09:23:00 +0200</pubDate><guid>https://antoninpvr.github.io/hardware/dev_board/esp32_wroom/</guid><description>
&lt;img src="https://antoninpvr.github.io/hardware/dev_board/esp32_wroom/featured_hu_65dc1107f683aa30.jpg" width="640" height="512"/>&lt;div class="hextra-cards hx:mt-4 hx:gap-4 hx:grid not-prose"
style="--hextra-cards-grid-cols: 3;">&lt;a
class="hextra-card hx:relative hx:group hx:flex hx:flex-col hx:justify-start hx:overflow-hidden hx:rounded-lg hx:border hx:border-gray-200 hx:text-current hx:no-underline hx:dark:shadow-none hx:hover:shadow-gray-100 hx:dark:hover:shadow-none hx:shadow-gray-100 hx:active:shadow-sm hx:active:shadow-gray-200 hx:transition-all hx:duration-200 hx:hover:border-gray-300 hx:bg-transparent hx:shadow-xs hx:dark:border-neutral-800 hx:hover:bg-slate-50 hx:hover:shadow-md hx:dark:hover:border-neutral-700 hx:dark:hover:bg-neutral-900"href="https://github.com/AntoninPvr/dev-board-enclosures"
target="_blank" rel="noreferrer">
&lt;span class="hextra-card-icon hx:flex hx:font-semibold hx:items-start hx:gap-2 hx:pt-4 hx:px-4 hx:text-gray-700 hx:hover:text-gray-900 hx:dark:text-neutral-200 hx:dark:hover:text-neutral-50">&lt;svg fill="currentColor" viewBox="3 3 18 18">
&lt;path d="M12 3C7.0275 3 3 7.12937 3 12.2276C3 16.3109 5.57625 19.7597 9.15374 20.9824C9.60374 21.0631 9.77249 20.7863 9.77249 20.5441C9.77249 20.3249 9.76125 19.5982 9.76125 18.8254C7.5 19.2522 6.915 18.2602 6.735 17.7412C6.63375 17.4759 6.19499 16.6569 5.8125 16.4378C5.4975 16.2647 5.0475 15.838 5.80124 15.8264C6.51 15.8149 7.01625 16.4954 7.18499 16.7723C7.99499 18.1679 9.28875 17.7758 9.80625 17.5335C9.885 16.9337 10.1212 16.53 10.38 16.2993C8.3775 16.0687 6.285 15.2728 6.285 11.7432C6.285 10.7397 6.63375 9.9092 7.20749 9.26326C7.1175 9.03257 6.8025 8.08674 7.2975 6.81794C7.2975 6.81794 8.05125 6.57571 9.77249 7.76377C10.4925 7.55615 11.2575 7.45234 12.0225 7.45234C12.7875 7.45234 13.5525 7.55615 14.2725 7.76377C15.9937 6.56418 16.7475 6.81794 16.7475 6.81794C17.2424 8.08674 16.9275 9.03257 16.8375 9.26326C17.4113 9.9092 17.76 10.7281 17.76 11.7432C17.76 15.2843 15.6563 16.0687 13.6537 16.2993C13.98 16.5877 14.2613 17.1414 14.2613 18.0065C14.2613 19.2407 14.25 20.2326 14.25 20.5441C14.25 20.7863 14.4188 21.0746 14.8688 20.9824C16.6554 20.364 18.2079 19.1866 19.3078 17.6162C20.4077 16.0457 20.9995 14.1611 21 12.2276C21 7.12937 16.9725 3 12 3Z">&lt;/path>
&lt;/svg>
dev-board-enclosures&lt;/span>
&lt;div class="hextra-card-subtitle hx:line-clamp-3 hx:text-sm hx:font-normal hx:text-gray-500 hx:dark:text-gray-400 hx:px-4 hx:mb-4 hx:mt-2">Simple enclosures for dev boards&lt;/div>&lt;div class="hextra-badge hextra-card-tag">
&lt;div class="hx:inline-flex hx:gap-1 hx:items-center hx:rounded-full hx:px-2.5 hx:leading-6 hx:text-[.65rem] hx:border hx:text-gray-600 hx:bg-gray-100 hx:dark:bg-neutral-800 hx:dark:text-neutral-200 hx:border-gray-200 hx:dark:border-neutral-700">&lt;svg height=12 xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" aria-hidden="true">&lt;path stroke-linecap="round" stroke-linejoin="round" d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z"/>&lt;/svg>no-release&lt;/div>
&lt;/div>
&lt;/a>
&lt;/div>
&lt;h3>Overview&lt;span class="hx:absolute hx:-mt-20" id="overview">&lt;/span>
&lt;a href="#overview" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Simple enclosure for ESP32-WROOM It is designed to be 3D printed.&lt;/p>
&lt;h3>Parts&lt;span class="hx:absolute hx:-mt-20" id="parts">&lt;/span>
&lt;a href="#parts" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;ul>
&lt;li>&lt;code>esp32-wroom_top.STL&lt;/code> Top part of the enclosure&lt;/li>
&lt;li>&lt;code>esp32-wroom_bottom.STL&lt;/code> Bottom part of the enclosure&lt;/li>
&lt;/ul>
&lt;div style="display: flex">
&lt;div id="esp32-wroom_top.glb" style="width: 50%; height: 50%">
&lt;/div>
&lt;script type="importmap">
{
"imports": {
"three": "https://unpkg.com/three@0.161.0/build/three.module.js",
"three/addons/": "https://unpkg.com/three@0.161.0/examples/jsm/"
}
}
&lt;/script>
&lt;script type="module">
import * as THREE from 'three';
import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { MeshoptDecoder } from 'three/addons/libs/meshopt_decoder.module.js';
import { DRACOLoader } from 'three/addons/loaders/DRACOLoader.js'
let container;
let camera, scene, renderer, clock;
let mesh;
let rotate, controls;
init();
render();
function init() {
container = document.getElementById("esp32-wroom_top.glb");
container.onload = () => {
onWindowResize();
}
const aspectRatio = "1.3333";
const model_path = "esp32-wroom_top.glb";
const modelColor = "#de833d";
rotate = "true" === "true";
renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
renderer.setPixelRatio(window.devicePixelRatio);
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1;
renderer.outputColorSpace = THREE.SRGBColorSpace;
container.appendChild(renderer.domElement);
camera = new THREE.PerspectiveCamera(45, aspectRatio, 0.1, 1000);
camera.position.set(1, 0, 1);
camera.lookAt(0, 0, 0);
controls = new OrbitControls(camera, renderer.domElement);
controls.addEventListener('change', render);
controls.minDistance = 1;
controls.maxDistance = 10;
controls.target.set(0, 0, 0);
controls.update();
const environment = new RoomEnvironment(renderer);
const pmremGenerator = new THREE.PMREMGenerator(renderer);
scene = new THREE.Scene();
scene.environment = pmremGenerator.fromScene(environment).texture;
const ambientLight = new THREE.AmbientLight(0xffffff, 1);
scene.add(ambientLight);
const light1 = new THREE.PointLight( 0xffeeee, 50, 0);
light1.position.set( 0, 2, 1 );
light1.castShadow = true;
scene.add( light1 );
const loader = new GLTFLoader();
const dracoLoader = new DRACOLoader();
dracoLoader.setDecoderPath('/examples/jsm/libs/draco/');
loader.setMeshoptDecoder(MeshoptDecoder);
loader.setDRACOLoader(dracoLoader);
loader.load(model_path, function (gltf) {
const root = gltf.scene;
const box = new THREE.Box3().setFromObject(root);
const size = box.getSize(new THREE.Vector3());
const center = box.getCenter(new THREE.Vector3());
root.position.x += (root.position.x - center.x);
root.position.y += (root.position.y - center.y);
root.position.z += (root.position.z - center.z);
mesh = new THREE.Group();
mesh.add(root);
if (modelColor &amp;&amp; modelColor !== "") {
root.traverse((child) => {
if (child.isMesh) {
child.material.color.set(modelColor);
child.material.roughness = 0.8;
child.material.metalness = 0.2;
}
});
}
root.traverse((child) => {
if (child.isMesh) {
child.castShadow = true;
child.receiveShadow = true;
}
});
const maxSize = Math.max(size.x, size.y, size.z);
mesh.scale.set(1 / maxSize, 1 / maxSize, 1 / maxSize);
scene.add(mesh);
if ( rotate ) {
clock = new THREE.Clock();
renderer.setAnimationLoop( animate );
controls.addEventListener( 'start', () => rotate = false );
controls.addEventListener( 'end', () => rotate = true );
}
render();
});
window.addEventListener('resize', onWindowResize);
}
function onWindowResize() {
const aspectRatio = "1.3333";
camera.aspect = aspectRatio;
camera.updateProjectionMatrix();
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
render();
}
function render() {
renderer.render(scene, camera);
}
function animate() {
const delta = clock.getDelta();
const direction = "left";
if ( rotate &amp;&amp; mesh ) {
const ry = 0.5;
if (direction === "left") {
mesh.rotation.y -= delta * ry;
} else {
mesh.rotation.y += delta * ry;
}
render();
}
}
&lt;/script>
&lt;div id="esp32-wroom_bottom.glb" style="width: 50%; height: 50%">
&lt;/div>
&lt;script type="importmap">
{
"imports": {
"three": "https://unpkg.com/three@0.161.0/build/three.module.js",
"three/addons/": "https://unpkg.com/three@0.161.0/examples/jsm/"
}
}
&lt;/script>
&lt;script type="module">
import * as THREE from 'three';
import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { MeshoptDecoder } from 'three/addons/libs/meshopt_decoder.module.js';
import { DRACOLoader } from 'three/addons/loaders/DRACOLoader.js'
let container;
let camera, scene, renderer, clock;
let mesh;
let rotate, controls;
init();
render();
function init() {
container = document.getElementById("esp32-wroom_bottom.glb");
container.onload = () => {
onWindowResize();
}
const aspectRatio = "1.3333";
const model_path = "esp32-wroom_bottom.glb";
const modelColor = "#de833d";
rotate = "true" === "true";
renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
renderer.setPixelRatio(window.devicePixelRatio);
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1;
renderer.outputColorSpace = THREE.SRGBColorSpace;
container.appendChild(renderer.domElement);
camera = new THREE.PerspectiveCamera(45, aspectRatio, 0.1, 1000);
camera.position.set(1, 0, 1);
camera.lookAt(0, 0, 0);
controls = new OrbitControls(camera, renderer.domElement);
controls.addEventListener('change', render);
controls.minDistance = 1;
controls.maxDistance = 10;
controls.target.set(0, 0, 0);
controls.update();
const environment = new RoomEnvironment(renderer);
const pmremGenerator = new THREE.PMREMGenerator(renderer);
scene = new THREE.Scene();
scene.environment = pmremGenerator.fromScene(environment).texture;
const ambientLight = new THREE.AmbientLight(0xffffff, 1);
scene.add(ambientLight);
const light1 = new THREE.PointLight( 0xffeeee, 50, 0);
light1.position.set( 0, 2, 1 );
light1.castShadow = true;
scene.add( light1 );
const loader = new GLTFLoader();
const dracoLoader = new DRACOLoader();
dracoLoader.setDecoderPath('/examples/jsm/libs/draco/');
loader.setMeshoptDecoder(MeshoptDecoder);
loader.setDRACOLoader(dracoLoader);
loader.load(model_path, function (gltf) {
const root = gltf.scene;
const box = new THREE.Box3().setFromObject(root);
const size = box.getSize(new THREE.Vector3());
const center = box.getCenter(new THREE.Vector3());
root.position.x += (root.position.x - center.x);
root.position.y += (root.position.y - center.y);
root.position.z += (root.position.z - center.z);
mesh = new THREE.Group();
mesh.add(root);
if (modelColor &amp;&amp; modelColor !== "") {
root.traverse((child) => {
if (child.isMesh) {
child.material.color.set(modelColor);
child.material.roughness = 0.8;
child.material.metalness = 0.2;
}
});
}
root.traverse((child) => {
if (child.isMesh) {
child.castShadow = true;
child.receiveShadow = true;
}
});
const maxSize = Math.max(size.x, size.y, size.z);
mesh.scale.set(1 / maxSize, 1 / maxSize, 1 / maxSize);
scene.add(mesh);
if ( rotate ) {
clock = new THREE.Clock();
renderer.setAnimationLoop( animate );
controls.addEventListener( 'start', () => rotate = false );
controls.addEventListener( 'end', () => rotate = true );
}
render();
});
window.addEventListener('resize', onWindowResize);
}
function onWindowResize() {
const aspectRatio = "1.3333";
camera.aspect = aspectRatio;
camera.updateProjectionMatrix();
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
render();
}
function render() {
renderer.render(scene, camera);
}
function animate() {
const delta = clock.getDelta();
const direction = "right";
if ( rotate &amp;&amp; mesh ) {
const ry = 0.5;
if (direction === "left") {
mesh.rotation.y -= delta * ry;
} else {
mesh.rotation.y += delta * ry;
}
render();
}
}
&lt;/script>
&lt;/div>
&lt;h3>Assembly&lt;span class="hx:absolute hx:-mt-20" id="assembly">&lt;/span>
&lt;a href="#assembly" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Provided 3mf file is tested and ready to print on Prusa MK3 .
Parts are hold together by friction. No screws are needed.&lt;/p>
&lt;h3>Download&lt;span class="hx:absolute hx:-mt-20" id="download">&lt;/span>
&lt;a href="#download" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;ul>
&lt;li>&lt;a href="https://github.com/AntoninPvr/dev-board-enclosures/tree/main/esp32-wroom/stl"target="_blank" rel="noopener">STL files&lt;/a>&lt;/li>
&lt;li>&lt;a href="https://github.com/AntoninPvr/dev-board-enclosures/tree/main/esp32-wroom/3mf"target="_blank" rel="noopener">3MF file&lt;/a>&lt;/li>
&lt;li>&lt;a href="https://github.com/AntoninPvr/dev-board-enclosures/tree/main/esp32-wroom/step"target="_blank" rel="noopener">STEP files&lt;/a>&lt;/li>
&lt;/ul></description></item><item><title>FTDI Enclosure</title><link>https://antoninpvr.github.io/hardware/dev_board/ftdi/</link><pubDate>Tue, 04 Jul 2023 09:23:00 +0200</pubDate><guid>https://antoninpvr.github.io/hardware/dev_board/ftdi/</guid><description>
&lt;img src="https://antoninpvr.github.io/hardware/dev_board/ftdi/featured_hu_9fb7f7642445316a.jpg" width="640" height="512"/>&lt;div class="hextra-cards hx:mt-4 hx:gap-4 hx:grid not-prose"
style="--hextra-cards-grid-cols: 3;">&lt;a
class="hextra-card hx:relative hx:group hx:flex hx:flex-col hx:justify-start hx:overflow-hidden hx:rounded-lg hx:border hx:border-gray-200 hx:text-current hx:no-underline hx:dark:shadow-none hx:hover:shadow-gray-100 hx:dark:hover:shadow-none hx:shadow-gray-100 hx:active:shadow-sm hx:active:shadow-gray-200 hx:transition-all hx:duration-200 hx:hover:border-gray-300 hx:bg-transparent hx:shadow-xs hx:dark:border-neutral-800 hx:hover:bg-slate-50 hx:hover:shadow-md hx:dark:hover:border-neutral-700 hx:dark:hover:bg-neutral-900"href="https://github.com/AntoninPvr/dev-board-enclosures"
target="_blank" rel="noreferrer">
&lt;span class="hextra-card-icon hx:flex hx:font-semibold hx:items-start hx:gap-2 hx:pt-4 hx:px-4 hx:text-gray-700 hx:hover:text-gray-900 hx:dark:text-neutral-200 hx:dark:hover:text-neutral-50">&lt;svg fill="currentColor" viewBox="3 3 18 18">
&lt;path d="M12 3C7.0275 3 3 7.12937 3 12.2276C3 16.3109 5.57625 19.7597 9.15374 20.9824C9.60374 21.0631 9.77249 20.7863 9.77249 20.5441C9.77249 20.3249 9.76125 19.5982 9.76125 18.8254C7.5 19.2522 6.915 18.2602 6.735 17.7412C6.63375 17.4759 6.19499 16.6569 5.8125 16.4378C5.4975 16.2647 5.0475 15.838 5.80124 15.8264C6.51 15.8149 7.01625 16.4954 7.18499 16.7723C7.99499 18.1679 9.28875 17.7758 9.80625 17.5335C9.885 16.9337 10.1212 16.53 10.38 16.2993C8.3775 16.0687 6.285 15.2728 6.285 11.7432C6.285 10.7397 6.63375 9.9092 7.20749 9.26326C7.1175 9.03257 6.8025 8.08674 7.2975 6.81794C7.2975 6.81794 8.05125 6.57571 9.77249 7.76377C10.4925 7.55615 11.2575 7.45234 12.0225 7.45234C12.7875 7.45234 13.5525 7.55615 14.2725 7.76377C15.9937 6.56418 16.7475 6.81794 16.7475 6.81794C17.2424 8.08674 16.9275 9.03257 16.8375 9.26326C17.4113 9.9092 17.76 10.7281 17.76 11.7432C17.76 15.2843 15.6563 16.0687 13.6537 16.2993C13.98 16.5877 14.2613 17.1414 14.2613 18.0065C14.2613 19.2407 14.25 20.2326 14.25 20.5441C14.25 20.7863 14.4188 21.0746 14.8688 20.9824C16.6554 20.364 18.2079 19.1866 19.3078 17.6162C20.4077 16.0457 20.9995 14.1611 21 12.2276C21 7.12937 16.9725 3 12 3Z">&lt;/path>
&lt;/svg>
dev-board-enclosures&lt;/span>
&lt;div class="hextra-card-subtitle hx:line-clamp-3 hx:text-sm hx:font-normal hx:text-gray-500 hx:dark:text-gray-400 hx:px-4 hx:mb-4 hx:mt-2">Simple enclosures for dev boards&lt;/div>&lt;div class="hextra-badge hextra-card-tag">
&lt;div class="hx:inline-flex hx:gap-1 hx:items-center hx:rounded-full hx:px-2.5 hx:leading-6 hx:text-[.65rem] hx:border hx:text-gray-600 hx:bg-gray-100 hx:dark:bg-neutral-800 hx:dark:text-neutral-200 hx:border-gray-200 hx:dark:border-neutral-700">&lt;svg height=12 xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" aria-hidden="true">&lt;path stroke-linecap="round" stroke-linejoin="round" d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z"/>&lt;/svg>no-release&lt;/div>
&lt;/div>
&lt;/a>
&lt;/div>
&lt;h3>Overview&lt;span class="hx:absolute hx:-mt-20" id="overview">&lt;/span>
&lt;a href="#overview" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Simple enclosure for FTDI adapter It is designed to be 3D printed.&lt;/p>
&lt;h3>Parts&lt;span class="hx:absolute hx:-mt-20" id="parts">&lt;/span>
&lt;a href="#parts" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;ul>
&lt;li>&lt;code>ftdi_top.STL&lt;/code> Top part of the enclosure&lt;/li>
&lt;li>&lt;code>ftdi_bottom.STL&lt;/code> Bottom part of the enclosure&lt;/li>
&lt;/ul>
&lt;div style="display: flex">
&lt;div id="ftdi_top.glb" style="width: 50%; height: 50%">
&lt;/div>
&lt;script type="importmap">
{
"imports": {
"three": "https://unpkg.com/three@0.161.0/build/three.module.js",
"three/addons/": "https://unpkg.com/three@0.161.0/examples/jsm/"
}
}
&lt;/script>
&lt;script type="module">
import * as THREE from 'three';
import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { MeshoptDecoder } from 'three/addons/libs/meshopt_decoder.module.js';
import { DRACOLoader } from 'three/addons/loaders/DRACOLoader.js'
let container;
let camera, scene, renderer, clock;
let mesh;
let rotate, controls;
init();
render();
function init() {
container = document.getElementById("ftdi_top.glb");
container.onload = () => {
onWindowResize();
}
const aspectRatio = "1.3333";
const model_path = "ftdi_top.glb";
const modelColor = "#e24a43";
rotate = "true" === "true";
renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
renderer.setPixelRatio(window.devicePixelRatio);
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1;
renderer.outputColorSpace = THREE.SRGBColorSpace;
container.appendChild(renderer.domElement);
camera = new THREE.PerspectiveCamera(45, aspectRatio, 0.1, 1000);
camera.position.set(1, 0, 1);
camera.lookAt(0, 0, 0);
controls = new OrbitControls(camera, renderer.domElement);
controls.addEventListener('change', render);
controls.minDistance = 1;
controls.maxDistance = 10;
controls.target.set(0, 0, 0);
controls.update();
const environment = new RoomEnvironment(renderer);
const pmremGenerator = new THREE.PMREMGenerator(renderer);
scene = new THREE.Scene();
scene.environment = pmremGenerator.fromScene(environment).texture;
const ambientLight = new THREE.AmbientLight(0xffffff, 1);
scene.add(ambientLight);
const light1 = new THREE.PointLight( 0xffeeee, 50, 0);
light1.position.set( 0, 2, 1 );
light1.castShadow = true;
scene.add( light1 );
const loader = new GLTFLoader();
const dracoLoader = new DRACOLoader();
dracoLoader.setDecoderPath('/examples/jsm/libs/draco/');
loader.setMeshoptDecoder(MeshoptDecoder);
loader.setDRACOLoader(dracoLoader);
loader.load(model_path, function (gltf) {
const root = gltf.scene;
const box = new THREE.Box3().setFromObject(root);
const size = box.getSize(new THREE.Vector3());
const center = box.getCenter(new THREE.Vector3());
root.position.x += (root.position.x - center.x);
root.position.y += (root.position.y - center.y);
root.position.z += (root.position.z - center.z);
mesh = new THREE.Group();
mesh.add(root);
if (modelColor &amp;&amp; modelColor !== "") {
root.traverse((child) => {
if (child.isMesh) {
child.material.color.set(modelColor);
child.material.roughness = 0.8;
child.material.metalness = 0.2;
}
});
}
root.traverse((child) => {
if (child.isMesh) {
child.castShadow = true;
child.receiveShadow = true;
}
});
const maxSize = Math.max(size.x, size.y, size.z);
mesh.scale.set(1 / maxSize, 1 / maxSize, 1 / maxSize);
scene.add(mesh);
if ( rotate ) {
clock = new THREE.Clock();
renderer.setAnimationLoop( animate );
controls.addEventListener( 'start', () => rotate = false );
controls.addEventListener( 'end', () => rotate = true );
}
render();
});
window.addEventListener('resize', onWindowResize);
}
function onWindowResize() {
const aspectRatio = "1.3333";
camera.aspect = aspectRatio;
camera.updateProjectionMatrix();
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
render();
}
function render() {
renderer.render(scene, camera);
}
function animate() {
const delta = clock.getDelta();
const direction = "left";
if ( rotate &amp;&amp; mesh ) {
const ry = 0.5;
if (direction === "left") {
mesh.rotation.y -= delta * ry;
} else {
mesh.rotation.y += delta * ry;
}
render();
}
}
&lt;/script>
&lt;div id="ftdi_bottom.glb" style="width: 50%; height: 50%">
&lt;/div>
&lt;script type="importmap">
{
"imports": {
"three": "https://unpkg.com/three@0.161.0/build/three.module.js",
"three/addons/": "https://unpkg.com/three@0.161.0/examples/jsm/"
}
}
&lt;/script>
&lt;script type="module">
import * as THREE from 'three';
import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { MeshoptDecoder } from 'three/addons/libs/meshopt_decoder.module.js';
import { DRACOLoader } from 'three/addons/loaders/DRACOLoader.js'
let container;
let camera, scene, renderer, clock;
let mesh;
let rotate, controls;
init();
render();
function init() {
container = document.getElementById("ftdi_bottom.glb");
container.onload = () => {
onWindowResize();
}
const aspectRatio = "1.3333";
const model_path = "ftdi_bottom.glb";
const modelColor = "#e24a43";
rotate = "true" === "true";
renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
renderer.setPixelRatio(window.devicePixelRatio);
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1;
renderer.outputColorSpace = THREE.SRGBColorSpace;
container.appendChild(renderer.domElement);
camera = new THREE.PerspectiveCamera(45, aspectRatio, 0.1, 1000);
camera.position.set(1, 0, 1);
camera.lookAt(0, 0, 0);
controls = new OrbitControls(camera, renderer.domElement);
controls.addEventListener('change', render);
controls.minDistance = 1;
controls.maxDistance = 10;
controls.target.set(0, 0, 0);
controls.update();
const environment = new RoomEnvironment(renderer);
const pmremGenerator = new THREE.PMREMGenerator(renderer);
scene = new THREE.Scene();
scene.environment = pmremGenerator.fromScene(environment).texture;
const ambientLight = new THREE.AmbientLight(0xffffff, 1);
scene.add(ambientLight);
const light1 = new THREE.PointLight( 0xffeeee, 50, 0);
light1.position.set( 0, 2, 1 );
light1.castShadow = true;
scene.add( light1 );
const loader = new GLTFLoader();
const dracoLoader = new DRACOLoader();
dracoLoader.setDecoderPath('/examples/jsm/libs/draco/');
loader.setMeshoptDecoder(MeshoptDecoder);
loader.setDRACOLoader(dracoLoader);
loader.load(model_path, function (gltf) {
const root = gltf.scene;
const box = new THREE.Box3().setFromObject(root);
const size = box.getSize(new THREE.Vector3());
const center = box.getCenter(new THREE.Vector3());
root.position.x += (root.position.x - center.x);
root.position.y += (root.position.y - center.y);
root.position.z += (root.position.z - center.z);
mesh = new THREE.Group();
mesh.add(root);
if (modelColor &amp;&amp; modelColor !== "") {
root.traverse((child) => {
if (child.isMesh) {
child.material.color.set(modelColor);
child.material.roughness = 0.8;
child.material.metalness = 0.2;
}
});
}
root.traverse((child) => {
if (child.isMesh) {
child.castShadow = true;
child.receiveShadow = true;
}
});
const maxSize = Math.max(size.x, size.y, size.z);
mesh.scale.set(1 / maxSize, 1 / maxSize, 1 / maxSize);
scene.add(mesh);
if ( rotate ) {
clock = new THREE.Clock();
renderer.setAnimationLoop( animate );
controls.addEventListener( 'start', () => rotate = false );
controls.addEventListener( 'end', () => rotate = true );
}
render();
});
window.addEventListener('resize', onWindowResize);
}
function onWindowResize() {
const aspectRatio = "1.3333";
camera.aspect = aspectRatio;
camera.updateProjectionMatrix();
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
render();
}
function render() {
renderer.render(scene, camera);
}
function animate() {
const delta = clock.getDelta();
const direction = "right";
if ( rotate &amp;&amp; mesh ) {
const ry = 0.5;
if (direction === "left") {
mesh.rotation.y -= delta * ry;
} else {
mesh.rotation.y += delta * ry;
}
render();
}
}
&lt;/script>
&lt;/div>
&lt;h3>Assembly&lt;span class="hx:absolute hx:-mt-20" id="assembly">&lt;/span>
&lt;a href="#assembly" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Provided 3mf file is tested and ready to print on Prusa MK3 .
Parts are hold together by friction. No screws are needed.&lt;/p>
&lt;h3>Download&lt;span class="hx:absolute hx:-mt-20" id="download">&lt;/span>
&lt;a href="#download" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;ul>
&lt;li>&lt;a href="https://github.com/AntoninPvr/dev-board-enclosures/tree/main/ftdi/stl"target="_blank" rel="noopener">STL files&lt;/a>&lt;/li>
&lt;li>&lt;a href="https://github.com/AntoninPvr/dev-board-enclosures/tree/main/ftdi/3mf"target="_blank" rel="noopener">3MF file&lt;/a>&lt;/li>
&lt;li>&lt;a href="https://github.com/AntoninPvr/dev-board-enclosures/tree/main/ftdi/step"target="_blank" rel="noopener">STEP files&lt;/a>&lt;/li>
&lt;/ul></description></item><item><title>STM32 F103 Enclosure</title><link>https://antoninpvr.github.io/hardware/dev_board/stm32_f103/</link><pubDate>Tue, 04 Jul 2023 09:23:00 +0200</pubDate><guid>https://antoninpvr.github.io/hardware/dev_board/stm32_f103/</guid><description>
&lt;img src="https://antoninpvr.github.io/hardware/dev_board/stm32_f103/featured_hu_21d4cee048ecf407.jpg" width="640" height="512"/>&lt;div class="hextra-cards hx:mt-4 hx:gap-4 hx:grid not-prose"
style="--hextra-cards-grid-cols: 3;">&lt;a
class="hextra-card hx:relative hx:group hx:flex hx:flex-col hx:justify-start hx:overflow-hidden hx:rounded-lg hx:border hx:border-gray-200 hx:text-current hx:no-underline hx:dark:shadow-none hx:hover:shadow-gray-100 hx:dark:hover:shadow-none hx:shadow-gray-100 hx:active:shadow-sm hx:active:shadow-gray-200 hx:transition-all hx:duration-200 hx:hover:border-gray-300 hx:bg-transparent hx:shadow-xs hx:dark:border-neutral-800 hx:hover:bg-slate-50 hx:hover:shadow-md hx:dark:hover:border-neutral-700 hx:dark:hover:bg-neutral-900"href="https://github.com/AntoninPvr/dev-board-enclosures"
target="_blank" rel="noreferrer">
&lt;span class="hextra-card-icon hx:flex hx:font-semibold hx:items-start hx:gap-2 hx:pt-4 hx:px-4 hx:text-gray-700 hx:hover:text-gray-900 hx:dark:text-neutral-200 hx:dark:hover:text-neutral-50">&lt;svg fill="currentColor" viewBox="3 3 18 18">
&lt;path d="M12 3C7.0275 3 3 7.12937 3 12.2276C3 16.3109 5.57625 19.7597 9.15374 20.9824C9.60374 21.0631 9.77249 20.7863 9.77249 20.5441C9.77249 20.3249 9.76125 19.5982 9.76125 18.8254C7.5 19.2522 6.915 18.2602 6.735 17.7412C6.63375 17.4759 6.19499 16.6569 5.8125 16.4378C5.4975 16.2647 5.0475 15.838 5.80124 15.8264C6.51 15.8149 7.01625 16.4954 7.18499 16.7723C7.99499 18.1679 9.28875 17.7758 9.80625 17.5335C9.885 16.9337 10.1212 16.53 10.38 16.2993C8.3775 16.0687 6.285 15.2728 6.285 11.7432C6.285 10.7397 6.63375 9.9092 7.20749 9.26326C7.1175 9.03257 6.8025 8.08674 7.2975 6.81794C7.2975 6.81794 8.05125 6.57571 9.77249 7.76377C10.4925 7.55615 11.2575 7.45234 12.0225 7.45234C12.7875 7.45234 13.5525 7.55615 14.2725 7.76377C15.9937 6.56418 16.7475 6.81794 16.7475 6.81794C17.2424 8.08674 16.9275 9.03257 16.8375 9.26326C17.4113 9.9092 17.76 10.7281 17.76 11.7432C17.76 15.2843 15.6563 16.0687 13.6537 16.2993C13.98 16.5877 14.2613 17.1414 14.2613 18.0065C14.2613 19.2407 14.25 20.2326 14.25 20.5441C14.25 20.7863 14.4188 21.0746 14.8688 20.9824C16.6554 20.364 18.2079 19.1866 19.3078 17.6162C20.4077 16.0457 20.9995 14.1611 21 12.2276C21 7.12937 16.9725 3 12 3Z">&lt;/path>
&lt;/svg>
dev-board-enclosures&lt;/span>
&lt;div class="hextra-card-subtitle hx:line-clamp-3 hx:text-sm hx:font-normal hx:text-gray-500 hx:dark:text-gray-400 hx:px-4 hx:mb-4 hx:mt-2">Simple enclosures for dev boards&lt;/div>&lt;div class="hextra-badge hextra-card-tag">
&lt;div class="hx:inline-flex hx:gap-1 hx:items-center hx:rounded-full hx:px-2.5 hx:leading-6 hx:text-[.65rem] hx:border hx:text-gray-600 hx:bg-gray-100 hx:dark:bg-neutral-800 hx:dark:text-neutral-200 hx:border-gray-200 hx:dark:border-neutral-700">&lt;svg height=12 xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" aria-hidden="true">&lt;path stroke-linecap="round" stroke-linejoin="round" d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z"/>&lt;/svg>no-release&lt;/div>
&lt;/div>
&lt;/a>
&lt;/div>
&lt;h3>Overview&lt;span class="hx:absolute hx:-mt-20" id="overview">&lt;/span>
&lt;a href="#overview" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Simple enclosure for STM32F103 It is designed to be 3D printed.&lt;/p>
&lt;h3>Parts&lt;span class="hx:absolute hx:-mt-20" id="parts">&lt;/span>
&lt;a href="#parts" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;ul>
&lt;li>&lt;code>stm32f103_top.STL&lt;/code> Top part of the enclosure&lt;/li>
&lt;li>&lt;code>stm32f103_bottom.STL&lt;/code> Bottom part of the enclosure&lt;/li>
&lt;/ul>
&lt;div style="display: flex">
&lt;div id="stm32f103_top.glb" style="width: 50%; height: 50%">
&lt;/div>
&lt;script type="importmap">
{
"imports": {
"three": "https://unpkg.com/three@0.161.0/build/three.module.js",
"three/addons/": "https://unpkg.com/three@0.161.0/examples/jsm/"
}
}
&lt;/script>
&lt;script type="module">
import * as THREE from 'three';
import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { MeshoptDecoder } from 'three/addons/libs/meshopt_decoder.module.js';
import { DRACOLoader } from 'three/addons/loaders/DRACOLoader.js'
let container;
let camera, scene, renderer, clock;
let mesh;
let rotate, controls;
init();
render();
function init() {
container = document.getElementById("stm32f103_top.glb");
container.onload = () => {
onWindowResize();
}
const aspectRatio = "1.3333";
const model_path = "stm32f103_top.glb";
const modelColor = "#d2cc28";
rotate = "true" === "true";
renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
renderer.setPixelRatio(window.devicePixelRatio);
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1;
renderer.outputColorSpace = THREE.SRGBColorSpace;
container.appendChild(renderer.domElement);
camera = new THREE.PerspectiveCamera(45, aspectRatio, 0.1, 1000);
camera.position.set(1, 0, 1);
camera.lookAt(0, 0, 0);
controls = new OrbitControls(camera, renderer.domElement);
controls.addEventListener('change', render);
controls.minDistance = 1;
controls.maxDistance = 10;
controls.target.set(0, 0, 0);
controls.update();
const environment = new RoomEnvironment(renderer);
const pmremGenerator = new THREE.PMREMGenerator(renderer);
scene = new THREE.Scene();
scene.environment = pmremGenerator.fromScene(environment).texture;
const ambientLight = new THREE.AmbientLight(0xffffff, 1);
scene.add(ambientLight);
const light1 = new THREE.PointLight( 0xffeeee, 50, 0);
light1.position.set( 0, 2, 1 );
light1.castShadow = true;
scene.add( light1 );
const loader = new GLTFLoader();
const dracoLoader = new DRACOLoader();
dracoLoader.setDecoderPath('/examples/jsm/libs/draco/');
loader.setMeshoptDecoder(MeshoptDecoder);
loader.setDRACOLoader(dracoLoader);
loader.load(model_path, function (gltf) {
const root = gltf.scene;
const box = new THREE.Box3().setFromObject(root);
const size = box.getSize(new THREE.Vector3());
const center = box.getCenter(new THREE.Vector3());
root.position.x += (root.position.x - center.x);
root.position.y += (root.position.y - center.y);
root.position.z += (root.position.z - center.z);
mesh = new THREE.Group();
mesh.add(root);
if (modelColor &amp;&amp; modelColor !== "") {
root.traverse((child) => {
if (child.isMesh) {
child.material.color.set(modelColor);
child.material.roughness = 0.8;
child.material.metalness = 0.2;
}
});
}
root.traverse((child) => {
if (child.isMesh) {
child.castShadow = true;
child.receiveShadow = true;
}
});
const maxSize = Math.max(size.x, size.y, size.z);
mesh.scale.set(1 / maxSize, 1 / maxSize, 1 / maxSize);
scene.add(mesh);
if ( rotate ) {
clock = new THREE.Clock();
renderer.setAnimationLoop( animate );
controls.addEventListener( 'start', () => rotate = false );
controls.addEventListener( 'end', () => rotate = true );
}
render();
});
window.addEventListener('resize', onWindowResize);
}
function onWindowResize() {
const aspectRatio = "1.3333";
camera.aspect = aspectRatio;
camera.updateProjectionMatrix();
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
render();
}
function render() {
renderer.render(scene, camera);
}
function animate() {
const delta = clock.getDelta();
const direction = "left";
if ( rotate &amp;&amp; mesh ) {
const ry = 0.5;
if (direction === "left") {
mesh.rotation.y -= delta * ry;
} else {
mesh.rotation.y += delta * ry;
}
render();
}
}
&lt;/script>
&lt;div id="stm32f103_bottom.glb" style="width: 50%; height: 50%">
&lt;/div>
&lt;script type="importmap">
{
"imports": {
"three": "https://unpkg.com/three@0.161.0/build/three.module.js",
"three/addons/": "https://unpkg.com/three@0.161.0/examples/jsm/"
}
}
&lt;/script>
&lt;script type="module">
import * as THREE from 'three';
import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { MeshoptDecoder } from 'three/addons/libs/meshopt_decoder.module.js';
import { DRACOLoader } from 'three/addons/loaders/DRACOLoader.js'
let container;
let camera, scene, renderer, clock;
let mesh;
let rotate, controls;
init();
render();
function init() {
container = document.getElementById("stm32f103_bottom.glb");
container.onload = () => {
onWindowResize();
}
const aspectRatio = "1.3333";
const model_path = "stm32f103_bottom.glb";
const modelColor = "#d2cc28";
rotate = "true" === "true";
renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
renderer.setPixelRatio(window.devicePixelRatio);
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1;
renderer.outputColorSpace = THREE.SRGBColorSpace;
container.appendChild(renderer.domElement);
camera = new THREE.PerspectiveCamera(45, aspectRatio, 0.1, 1000);
camera.position.set(1, 0, 1);
camera.lookAt(0, 0, 0);
controls = new OrbitControls(camera, renderer.domElement);
controls.addEventListener('change', render);
controls.minDistance = 1;
controls.maxDistance = 10;
controls.target.set(0, 0, 0);
controls.update();
const environment = new RoomEnvironment(renderer);
const pmremGenerator = new THREE.PMREMGenerator(renderer);
scene = new THREE.Scene();
scene.environment = pmremGenerator.fromScene(environment).texture;
const ambientLight = new THREE.AmbientLight(0xffffff, 1);
scene.add(ambientLight);
const light1 = new THREE.PointLight( 0xffeeee, 50, 0);
light1.position.set( 0, 2, 1 );
light1.castShadow = true;
scene.add( light1 );
const loader = new GLTFLoader();
const dracoLoader = new DRACOLoader();
dracoLoader.setDecoderPath('/examples/jsm/libs/draco/');
loader.setMeshoptDecoder(MeshoptDecoder);
loader.setDRACOLoader(dracoLoader);
loader.load(model_path, function (gltf) {
const root = gltf.scene;
const box = new THREE.Box3().setFromObject(root);
const size = box.getSize(new THREE.Vector3());
const center = box.getCenter(new THREE.Vector3());
root.position.x += (root.position.x - center.x);
root.position.y += (root.position.y - center.y);
root.position.z += (root.position.z - center.z);
mesh = new THREE.Group();
mesh.add(root);
if (modelColor &amp;&amp; modelColor !== "") {
root.traverse((child) => {
if (child.isMesh) {
child.material.color.set(modelColor);
child.material.roughness = 0.8;
child.material.metalness = 0.2;
}
});
}
root.traverse((child) => {
if (child.isMesh) {
child.castShadow = true;
child.receiveShadow = true;
}
});
const maxSize = Math.max(size.x, size.y, size.z);
mesh.scale.set(1 / maxSize, 1 / maxSize, 1 / maxSize);
scene.add(mesh);
if ( rotate ) {
clock = new THREE.Clock();
renderer.setAnimationLoop( animate );
controls.addEventListener( 'start', () => rotate = false );
controls.addEventListener( 'end', () => rotate = true );
}
render();
});
window.addEventListener('resize', onWindowResize);
}
function onWindowResize() {
const aspectRatio = "1.3333";
camera.aspect = aspectRatio;
camera.updateProjectionMatrix();
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
render();
}
function render() {
renderer.render(scene, camera);
}
function animate() {
const delta = clock.getDelta();
const direction = "right";
if ( rotate &amp;&amp; mesh ) {
const ry = 0.5;
if (direction === "left") {
mesh.rotation.y -= delta * ry;
} else {
mesh.rotation.y += delta * ry;
}
render();
}
}
&lt;/script>
&lt;/div>
&lt;h3>Assembly&lt;span class="hx:absolute hx:-mt-20" id="assembly">&lt;/span>
&lt;a href="#assembly" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Provided 3mf file is tested and ready to print on Prusa MK3 .
Parts are hold together by friction. No screws are needed.&lt;/p>
&lt;h3>Download&lt;span class="hx:absolute hx:-mt-20" id="download">&lt;/span>
&lt;a href="#download" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;ul>
&lt;li>&lt;a href="https://github.com/AntoninPvr/dev-board-enclosures/tree/main/stm32f103/stl"target="_blank" rel="noopener">STL files&lt;/a>&lt;/li>
&lt;li>&lt;a href="https://github.com/AntoninPvr/dev-board-enclosures/tree/main/stm32f103/3mf"target="_blank" rel="noopener">3MF file&lt;/a>&lt;/li>
&lt;li>&lt;a href="https://github.com/AntoninPvr/dev-board-enclosures/tree/main/stm32f103/step"target="_blank" rel="noopener">STEP files&lt;/a>&lt;/li>
&lt;/ul></description></item><item><title>STM32 F411 Enclosure</title><link>https://antoninpvr.github.io/hardware/dev_board/stm32_f411/</link><pubDate>Tue, 04 Jul 2023 09:23:00 +0200</pubDate><guid>https://antoninpvr.github.io/hardware/dev_board/stm32_f411/</guid><description>
&lt;img src="https://antoninpvr.github.io/hardware/dev_board/stm32_f411/featured_hu_c7b90c926f691764.jpg" width="640" height="512"/>&lt;div class="hextra-cards hx:mt-4 hx:gap-4 hx:grid not-prose"
style="--hextra-cards-grid-cols: 3;">&lt;a
class="hextra-card hx:relative hx:group hx:flex hx:flex-col hx:justify-start hx:overflow-hidden hx:rounded-lg hx:border hx:border-gray-200 hx:text-current hx:no-underline hx:dark:shadow-none hx:hover:shadow-gray-100 hx:dark:hover:shadow-none hx:shadow-gray-100 hx:active:shadow-sm hx:active:shadow-gray-200 hx:transition-all hx:duration-200 hx:hover:border-gray-300 hx:bg-transparent hx:shadow-xs hx:dark:border-neutral-800 hx:hover:bg-slate-50 hx:hover:shadow-md hx:dark:hover:border-neutral-700 hx:dark:hover:bg-neutral-900"href="https://github.com/AntoninPvr/dev-board-enclosures"
target="_blank" rel="noreferrer">
&lt;span class="hextra-card-icon hx:flex hx:font-semibold hx:items-start hx:gap-2 hx:pt-4 hx:px-4 hx:text-gray-700 hx:hover:text-gray-900 hx:dark:text-neutral-200 hx:dark:hover:text-neutral-50">&lt;svg fill="currentColor" viewBox="3 3 18 18">
&lt;path d="M12 3C7.0275 3 3 7.12937 3 12.2276C3 16.3109 5.57625 19.7597 9.15374 20.9824C9.60374 21.0631 9.77249 20.7863 9.77249 20.5441C9.77249 20.3249 9.76125 19.5982 9.76125 18.8254C7.5 19.2522 6.915 18.2602 6.735 17.7412C6.63375 17.4759 6.19499 16.6569 5.8125 16.4378C5.4975 16.2647 5.0475 15.838 5.80124 15.8264C6.51 15.8149 7.01625 16.4954 7.18499 16.7723C7.99499 18.1679 9.28875 17.7758 9.80625 17.5335C9.885 16.9337 10.1212 16.53 10.38 16.2993C8.3775 16.0687 6.285 15.2728 6.285 11.7432C6.285 10.7397 6.63375 9.9092 7.20749 9.26326C7.1175 9.03257 6.8025 8.08674 7.2975 6.81794C7.2975 6.81794 8.05125 6.57571 9.77249 7.76377C10.4925 7.55615 11.2575 7.45234 12.0225 7.45234C12.7875 7.45234 13.5525 7.55615 14.2725 7.76377C15.9937 6.56418 16.7475 6.81794 16.7475 6.81794C17.2424 8.08674 16.9275 9.03257 16.8375 9.26326C17.4113 9.9092 17.76 10.7281 17.76 11.7432C17.76 15.2843 15.6563 16.0687 13.6537 16.2993C13.98 16.5877 14.2613 17.1414 14.2613 18.0065C14.2613 19.2407 14.25 20.2326 14.25 20.5441C14.25 20.7863 14.4188 21.0746 14.8688 20.9824C16.6554 20.364 18.2079 19.1866 19.3078 17.6162C20.4077 16.0457 20.9995 14.1611 21 12.2276C21 7.12937 16.9725 3 12 3Z">&lt;/path>
&lt;/svg>
dev-board-enclosures&lt;/span>
&lt;div class="hextra-card-subtitle hx:line-clamp-3 hx:text-sm hx:font-normal hx:text-gray-500 hx:dark:text-gray-400 hx:px-4 hx:mb-4 hx:mt-2">Simple enclosures for dev boards&lt;/div>&lt;div class="hextra-badge hextra-card-tag">
&lt;div class="hx:inline-flex hx:gap-1 hx:items-center hx:rounded-full hx:px-2.5 hx:leading-6 hx:text-[.65rem] hx:border hx:text-gray-600 hx:bg-gray-100 hx:dark:bg-neutral-800 hx:dark:text-neutral-200 hx:border-gray-200 hx:dark:border-neutral-700">&lt;svg height=12 xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" aria-hidden="true">&lt;path stroke-linecap="round" stroke-linejoin="round" d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z"/>&lt;/svg>no-release&lt;/div>
&lt;/div>
&lt;/a>
&lt;/div>
&lt;h3>Overview&lt;span class="hx:absolute hx:-mt-20" id="overview">&lt;/span>
&lt;a href="#overview" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Simple enclosure for STM32F411 It is designed to be 3D printed.&lt;/p>
&lt;h3>Parts&lt;span class="hx:absolute hx:-mt-20" id="parts">&lt;/span>
&lt;a href="#parts" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;ul>
&lt;li>&lt;code>stm32f411_top.STL&lt;/code> Top part of the enclosure&lt;/li>
&lt;li>&lt;code>stm32f411_bottom.STL&lt;/code> Bottom part of the enclosure&lt;/li>
&lt;/ul>
&lt;div style="display: flex">
&lt;div id="stm32f411_top.glb" style="width: 50%; height: 50%">
&lt;/div>
&lt;script type="importmap">
{
"imports": {
"three": "https://unpkg.com/three@0.161.0/build/three.module.js",
"three/addons/": "https://unpkg.com/three@0.161.0/examples/jsm/"
}
}
&lt;/script>
&lt;script type="module">
import * as THREE from 'three';
import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { MeshoptDecoder } from 'three/addons/libs/meshopt_decoder.module.js';
import { DRACOLoader } from 'three/addons/loaders/DRACOLoader.js'
let container;
let camera, scene, renderer, clock;
let mesh;
let rotate, controls;
init();
render();
function init() {
container = document.getElementById("stm32f411_top.glb");
container.onload = () => {
onWindowResize();
}
const aspectRatio = "1.3333";
const model_path = "stm32f411_top.glb";
const modelColor = "#d2cc28";
rotate = "true" === "true";
renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
renderer.setPixelRatio(window.devicePixelRatio);
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1;
renderer.outputColorSpace = THREE.SRGBColorSpace;
container.appendChild(renderer.domElement);
camera = new THREE.PerspectiveCamera(45, aspectRatio, 0.1, 1000);
camera.position.set(1, 0, 1);
camera.lookAt(0, 0, 0);
controls = new OrbitControls(camera, renderer.domElement);
controls.addEventListener('change', render);
controls.minDistance = 1;
controls.maxDistance = 10;
controls.target.set(0, 0, 0);
controls.update();
const environment = new RoomEnvironment(renderer);
const pmremGenerator = new THREE.PMREMGenerator(renderer);
scene = new THREE.Scene();
scene.environment = pmremGenerator.fromScene(environment).texture;
const ambientLight = new THREE.AmbientLight(0xffffff, 1);
scene.add(ambientLight);
const light1 = new THREE.PointLight( 0xffeeee, 50, 0);
light1.position.set( 0, 2, 1 );
light1.castShadow = true;
scene.add( light1 );
const loader = new GLTFLoader();
const dracoLoader = new DRACOLoader();
dracoLoader.setDecoderPath('/examples/jsm/libs/draco/');
loader.setMeshoptDecoder(MeshoptDecoder);
loader.setDRACOLoader(dracoLoader);
loader.load(model_path, function (gltf) {
const root = gltf.scene;
const box = new THREE.Box3().setFromObject(root);
const size = box.getSize(new THREE.Vector3());
const center = box.getCenter(new THREE.Vector3());
root.position.x += (root.position.x - center.x);
root.position.y += (root.position.y - center.y);
root.position.z += (root.position.z - center.z);
mesh = new THREE.Group();
mesh.add(root);
if (modelColor &amp;&amp; modelColor !== "") {
root.traverse((child) => {
if (child.isMesh) {
child.material.color.set(modelColor);
child.material.roughness = 0.8;
child.material.metalness = 0.2;
}
});
}
root.traverse((child) => {
if (child.isMesh) {
child.castShadow = true;
child.receiveShadow = true;
}
});
const maxSize = Math.max(size.x, size.y, size.z);
mesh.scale.set(1 / maxSize, 1 / maxSize, 1 / maxSize);
scene.add(mesh);
if ( rotate ) {
clock = new THREE.Clock();
renderer.setAnimationLoop( animate );
controls.addEventListener( 'start', () => rotate = false );
controls.addEventListener( 'end', () => rotate = true );
}
render();
});
window.addEventListener('resize', onWindowResize);
}
function onWindowResize() {
const aspectRatio = "1.3333";
camera.aspect = aspectRatio;
camera.updateProjectionMatrix();
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
render();
}
function render() {
renderer.render(scene, camera);
}
function animate() {
const delta = clock.getDelta();
const direction = "left";
if ( rotate &amp;&amp; mesh ) {
const ry = 0.5;
if (direction === "left") {
mesh.rotation.y -= delta * ry;
} else {
mesh.rotation.y += delta * ry;
}
render();
}
}
&lt;/script>
&lt;div id="stm32f411_bottom.glb" style="width: 50%; height: 50%">
&lt;/div>
&lt;script type="importmap">
{
"imports": {
"three": "https://unpkg.com/three@0.161.0/build/three.module.js",
"three/addons/": "https://unpkg.com/three@0.161.0/examples/jsm/"
}
}
&lt;/script>
&lt;script type="module">
import * as THREE from 'three';
import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { MeshoptDecoder } from 'three/addons/libs/meshopt_decoder.module.js';
import { DRACOLoader } from 'three/addons/loaders/DRACOLoader.js'
let container;
let camera, scene, renderer, clock;
let mesh;
let rotate, controls;
init();
render();
function init() {
container = document.getElementById("stm32f411_bottom.glb");
container.onload = () => {
onWindowResize();
}
const aspectRatio = "1.3333";
const model_path = "stm32f411_bottom.glb";
const modelColor = "#d2cc28";
rotate = "true" === "true";
renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
renderer.setPixelRatio(window.devicePixelRatio);
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1;
renderer.outputColorSpace = THREE.SRGBColorSpace;
container.appendChild(renderer.domElement);
camera = new THREE.PerspectiveCamera(45, aspectRatio, 0.1, 1000);
camera.position.set(1, 0, 1);
camera.lookAt(0, 0, 0);
controls = new OrbitControls(camera, renderer.domElement);
controls.addEventListener('change', render);
controls.minDistance = 1;
controls.maxDistance = 10;
controls.target.set(0, 0, 0);
controls.update();
const environment = new RoomEnvironment(renderer);
const pmremGenerator = new THREE.PMREMGenerator(renderer);
scene = new THREE.Scene();
scene.environment = pmremGenerator.fromScene(environment).texture;
const ambientLight = new THREE.AmbientLight(0xffffff, 1);
scene.add(ambientLight);
const light1 = new THREE.PointLight( 0xffeeee, 50, 0);
light1.position.set( 0, 2, 1 );
light1.castShadow = true;
scene.add( light1 );
const loader = new GLTFLoader();
const dracoLoader = new DRACOLoader();
dracoLoader.setDecoderPath('/examples/jsm/libs/draco/');
loader.setMeshoptDecoder(MeshoptDecoder);
loader.setDRACOLoader(dracoLoader);
loader.load(model_path, function (gltf) {
const root = gltf.scene;
const box = new THREE.Box3().setFromObject(root);
const size = box.getSize(new THREE.Vector3());
const center = box.getCenter(new THREE.Vector3());
root.position.x += (root.position.x - center.x);
root.position.y += (root.position.y - center.y);
root.position.z += (root.position.z - center.z);
mesh = new THREE.Group();
mesh.add(root);
if (modelColor &amp;&amp; modelColor !== "") {
root.traverse((child) => {
if (child.isMesh) {
child.material.color.set(modelColor);
child.material.roughness = 0.8;
child.material.metalness = 0.2;
}
});
}
root.traverse((child) => {
if (child.isMesh) {
child.castShadow = true;
child.receiveShadow = true;
}
});
const maxSize = Math.max(size.x, size.y, size.z);
mesh.scale.set(1 / maxSize, 1 / maxSize, 1 / maxSize);
scene.add(mesh);
if ( rotate ) {
clock = new THREE.Clock();
renderer.setAnimationLoop( animate );
controls.addEventListener( 'start', () => rotate = false );
controls.addEventListener( 'end', () => rotate = true );
}
render();
});
window.addEventListener('resize', onWindowResize);
}
function onWindowResize() {
const aspectRatio = "1.3333";
camera.aspect = aspectRatio;
camera.updateProjectionMatrix();
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
render();
}
function render() {
renderer.render(scene, camera);
}
function animate() {
const delta = clock.getDelta();
const direction = "right";
if ( rotate &amp;&amp; mesh ) {
const ry = 0.5;
if (direction === "left") {
mesh.rotation.y -= delta * ry;
} else {
mesh.rotation.y += delta * ry;
}
render();
}
}
&lt;/script>
&lt;/div>
&lt;h3>Assembly&lt;span class="hx:absolute hx:-mt-20" id="assembly">&lt;/span>
&lt;a href="#assembly" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Provided 3mf file is tested and ready to print on Prusa MK3 .
Parts are hold together by friction. No screws are needed.&lt;/p>
&lt;h3>Download&lt;span class="hx:absolute hx:-mt-20" id="download">&lt;/span>
&lt;a href="#download" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;ul>
&lt;li>&lt;a href="https://github.com/AntoninPvr/dev-board-enclosures/tree/main/stm32f411/stl"target="_blank" rel="noopener">STL files&lt;/a>&lt;/li>
&lt;li>&lt;a href="https://github.com/AntoninPvr/dev-board-enclosures/tree/main/stm32f411/3mf"target="_blank" rel="noopener">3MF file&lt;/a>&lt;/li>
&lt;li>&lt;a href="https://github.com/AntoninPvr/dev-board-enclosures/tree/main/stm32f411/step"target="_blank" rel="noopener">STEP files&lt;/a>&lt;/li>
&lt;/ul></description></item><item><title>STM32 NUCLEO 64 Enclosure</title><link>https://antoninpvr.github.io/hardware/dev_board/stm32_nucleo_64/</link><pubDate>Tue, 04 Jul 2023 09:23:00 +0200</pubDate><guid>https://antoninpvr.github.io/hardware/dev_board/stm32_nucleo_64/</guid><description>
&lt;img src="https://antoninpvr.github.io/hardware/dev_board/stm32_nucleo_64/featured_hu_7ecd82e3930f6c2d.jpg" width="640" height="512"/>&lt;div class="hextra-cards hx:mt-4 hx:gap-4 hx:grid not-prose"
style="--hextra-cards-grid-cols: 3;">&lt;a
class="hextra-card hx:relative hx:group hx:flex hx:flex-col hx:justify-start hx:overflow-hidden hx:rounded-lg hx:border hx:border-gray-200 hx:text-current hx:no-underline hx:dark:shadow-none hx:hover:shadow-gray-100 hx:dark:hover:shadow-none hx:shadow-gray-100 hx:active:shadow-sm hx:active:shadow-gray-200 hx:transition-all hx:duration-200 hx:hover:border-gray-300 hx:bg-transparent hx:shadow-xs hx:dark:border-neutral-800 hx:hover:bg-slate-50 hx:hover:shadow-md hx:dark:hover:border-neutral-700 hx:dark:hover:bg-neutral-900"href="https://github.com/AntoninPvr/dev-board-enclosures"
target="_blank" rel="noreferrer">
&lt;span class="hextra-card-icon hx:flex hx:font-semibold hx:items-start hx:gap-2 hx:pt-4 hx:px-4 hx:text-gray-700 hx:hover:text-gray-900 hx:dark:text-neutral-200 hx:dark:hover:text-neutral-50">&lt;svg fill="currentColor" viewBox="3 3 18 18">
&lt;path d="M12 3C7.0275 3 3 7.12937 3 12.2276C3 16.3109 5.57625 19.7597 9.15374 20.9824C9.60374 21.0631 9.77249 20.7863 9.77249 20.5441C9.77249 20.3249 9.76125 19.5982 9.76125 18.8254C7.5 19.2522 6.915 18.2602 6.735 17.7412C6.63375 17.4759 6.19499 16.6569 5.8125 16.4378C5.4975 16.2647 5.0475 15.838 5.80124 15.8264C6.51 15.8149 7.01625 16.4954 7.18499 16.7723C7.99499 18.1679 9.28875 17.7758 9.80625 17.5335C9.885 16.9337 10.1212 16.53 10.38 16.2993C8.3775 16.0687 6.285 15.2728 6.285 11.7432C6.285 10.7397 6.63375 9.9092 7.20749 9.26326C7.1175 9.03257 6.8025 8.08674 7.2975 6.81794C7.2975 6.81794 8.05125 6.57571 9.77249 7.76377C10.4925 7.55615 11.2575 7.45234 12.0225 7.45234C12.7875 7.45234 13.5525 7.55615 14.2725 7.76377C15.9937 6.56418 16.7475 6.81794 16.7475 6.81794C17.2424 8.08674 16.9275 9.03257 16.8375 9.26326C17.4113 9.9092 17.76 10.7281 17.76 11.7432C17.76 15.2843 15.6563 16.0687 13.6537 16.2993C13.98 16.5877 14.2613 17.1414 14.2613 18.0065C14.2613 19.2407 14.25 20.2326 14.25 20.5441C14.25 20.7863 14.4188 21.0746 14.8688 20.9824C16.6554 20.364 18.2079 19.1866 19.3078 17.6162C20.4077 16.0457 20.9995 14.1611 21 12.2276C21 7.12937 16.9725 3 12 3Z">&lt;/path>
&lt;/svg>
dev-board-enclosures&lt;/span>
&lt;div class="hextra-card-subtitle hx:line-clamp-3 hx:text-sm hx:font-normal hx:text-gray-500 hx:dark:text-gray-400 hx:px-4 hx:mb-4 hx:mt-2">Simple enclosures for dev boards&lt;/div>&lt;div class="hextra-badge hextra-card-tag">
&lt;div class="hx:inline-flex hx:gap-1 hx:items-center hx:rounded-full hx:px-2.5 hx:leading-6 hx:text-[.65rem] hx:border hx:text-gray-600 hx:bg-gray-100 hx:dark:bg-neutral-800 hx:dark:text-neutral-200 hx:border-gray-200 hx:dark:border-neutral-700">&lt;svg height=12 xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" aria-hidden="true">&lt;path stroke-linecap="round" stroke-linejoin="round" d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z"/>&lt;/svg>no-release&lt;/div>
&lt;/div>
&lt;/a>
&lt;/div>
&lt;h3>Overview&lt;span class="hx:absolute hx:-mt-20" id="overview">&lt;/span>
&lt;a href="#overview" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Simple enclosure for STM32 Nucleo 64 It is designed to be 3D printed.&lt;/p>
&lt;h3>Parts&lt;span class="hx:absolute hx:-mt-20" id="parts">&lt;/span>
&lt;a href="#parts" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;ul>
&lt;li>&lt;code>stm32-nucleo-64_top.STL&lt;/code> Top part of the enclosure&lt;/li>
&lt;li>&lt;code>stm32-nucleo-64_bottom.STL&lt;/code> Bottom part of the enclosure&lt;/li>
&lt;/ul>
&lt;div style="display: flex">
&lt;div id="stm32-nucleo-64_top.glb" style="width: 50%; height: 50%">
&lt;/div>
&lt;script type="importmap">
{
"imports": {
"three": "https://unpkg.com/three@0.161.0/build/three.module.js",
"three/addons/": "https://unpkg.com/three@0.161.0/examples/jsm/"
}
}
&lt;/script>
&lt;script type="module">
import * as THREE from 'three';
import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { MeshoptDecoder } from 'three/addons/libs/meshopt_decoder.module.js';
import { DRACOLoader } from 'three/addons/loaders/DRACOLoader.js'
let container;
let camera, scene, renderer, clock;
let mesh;
let rotate, controls;
init();
render();
function init() {
container = document.getElementById("stm32-nucleo-64_top.glb");
container.onload = () => {
onWindowResize();
}
const aspectRatio = "1.3333";
const model_path = "stm32-nucleo-64_top.glb";
const modelColor = "#7d7875";
rotate = "true" === "true";
renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
renderer.setPixelRatio(window.devicePixelRatio);
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1;
renderer.outputColorSpace = THREE.SRGBColorSpace;
container.appendChild(renderer.domElement);
camera = new THREE.PerspectiveCamera(45, aspectRatio, 0.1, 1000);
camera.position.set(1, 0, 1);
camera.lookAt(0, 0, 0);
controls = new OrbitControls(camera, renderer.domElement);
controls.addEventListener('change', render);
controls.minDistance = 1;
controls.maxDistance = 10;
controls.target.set(0, 0, 0);
controls.update();
const environment = new RoomEnvironment(renderer);
const pmremGenerator = new THREE.PMREMGenerator(renderer);
scene = new THREE.Scene();
scene.environment = pmremGenerator.fromScene(environment).texture;
const ambientLight = new THREE.AmbientLight(0xffffff, 1);
scene.add(ambientLight);
const light1 = new THREE.PointLight( 0xffeeee, 50, 0);
light1.position.set( 0, 2, 1 );
light1.castShadow = true;
scene.add( light1 );
const loader = new GLTFLoader();
const dracoLoader = new DRACOLoader();
dracoLoader.setDecoderPath('/examples/jsm/libs/draco/');
loader.setMeshoptDecoder(MeshoptDecoder);
loader.setDRACOLoader(dracoLoader);
loader.load(model_path, function (gltf) {
const root = gltf.scene;
const box = new THREE.Box3().setFromObject(root);
const size = box.getSize(new THREE.Vector3());
const center = box.getCenter(new THREE.Vector3());
root.position.x += (root.position.x - center.x);
root.position.y += (root.position.y - center.y);
root.position.z += (root.position.z - center.z);
mesh = new THREE.Group();
mesh.add(root);
if (modelColor &amp;&amp; modelColor !== "") {
root.traverse((child) => {
if (child.isMesh) {
child.material.color.set(modelColor);
child.material.roughness = 0.8;
child.material.metalness = 0.2;
}
});
}
root.traverse((child) => {
if (child.isMesh) {
child.castShadow = true;
child.receiveShadow = true;
}
});
const maxSize = Math.max(size.x, size.y, size.z);
mesh.scale.set(1 / maxSize, 1 / maxSize, 1 / maxSize);
scene.add(mesh);
if ( rotate ) {
clock = new THREE.Clock();
renderer.setAnimationLoop( animate );
controls.addEventListener( 'start', () => rotate = false );
controls.addEventListener( 'end', () => rotate = true );
}
render();
});
window.addEventListener('resize', onWindowResize);
}
function onWindowResize() {
const aspectRatio = "1.3333";
camera.aspect = aspectRatio;
camera.updateProjectionMatrix();
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
render();
}
function render() {
renderer.render(scene, camera);
}
function animate() {
const delta = clock.getDelta();
const direction = "left";
if ( rotate &amp;&amp; mesh ) {
const ry = 0.5;
if (direction === "left") {
mesh.rotation.y -= delta * ry;
} else {
mesh.rotation.y += delta * ry;
}
render();
}
}
&lt;/script>
&lt;div id="stm32-nucleo-64_bottom.glb" style="width: 50%; height: 50%">
&lt;/div>
&lt;script type="importmap">
{
"imports": {
"three": "https://unpkg.com/three@0.161.0/build/three.module.js",
"three/addons/": "https://unpkg.com/three@0.161.0/examples/jsm/"
}
}
&lt;/script>
&lt;script type="module">
import * as THREE from 'three';
import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { MeshoptDecoder } from 'three/addons/libs/meshopt_decoder.module.js';
import { DRACOLoader } from 'three/addons/loaders/DRACOLoader.js'
let container;
let camera, scene, renderer, clock;
let mesh;
let rotate, controls;
init();
render();
function init() {
container = document.getElementById("stm32-nucleo-64_bottom.glb");
container.onload = () => {
onWindowResize();
}
const aspectRatio = "1.3333";
const model_path = "stm32-nucleo-64_bottom.glb";
const modelColor = "#7d7875";
rotate = "true" === "true";
renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
renderer.setPixelRatio(window.devicePixelRatio);
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1;
renderer.outputColorSpace = THREE.SRGBColorSpace;
container.appendChild(renderer.domElement);
camera = new THREE.PerspectiveCamera(45, aspectRatio, 0.1, 1000);
camera.position.set(1, 0, 1);
camera.lookAt(0, 0, 0);
controls = new OrbitControls(camera, renderer.domElement);
controls.addEventListener('change', render);
controls.minDistance = 1;
controls.maxDistance = 10;
controls.target.set(0, 0, 0);
controls.update();
const environment = new RoomEnvironment(renderer);
const pmremGenerator = new THREE.PMREMGenerator(renderer);
scene = new THREE.Scene();
scene.environment = pmremGenerator.fromScene(environment).texture;
const ambientLight = new THREE.AmbientLight(0xffffff, 1);
scene.add(ambientLight);
const light1 = new THREE.PointLight( 0xffeeee, 50, 0);
light1.position.set( 0, 2, 1 );
light1.castShadow = true;
scene.add( light1 );
const loader = new GLTFLoader();
const dracoLoader = new DRACOLoader();
dracoLoader.setDecoderPath('/examples/jsm/libs/draco/');
loader.setMeshoptDecoder(MeshoptDecoder);
loader.setDRACOLoader(dracoLoader);
loader.load(model_path, function (gltf) {
const root = gltf.scene;
const box = new THREE.Box3().setFromObject(root);
const size = box.getSize(new THREE.Vector3());
const center = box.getCenter(new THREE.Vector3());
root.position.x += (root.position.x - center.x);
root.position.y += (root.position.y - center.y);
root.position.z += (root.position.z - center.z);
mesh = new THREE.Group();
mesh.add(root);
if (modelColor &amp;&amp; modelColor !== "") {
root.traverse((child) => {
if (child.isMesh) {
child.material.color.set(modelColor);
child.material.roughness = 0.8;
child.material.metalness = 0.2;
}
});
}
root.traverse((child) => {
if (child.isMesh) {
child.castShadow = true;
child.receiveShadow = true;
}
});
const maxSize = Math.max(size.x, size.y, size.z);
mesh.scale.set(1 / maxSize, 1 / maxSize, 1 / maxSize);
scene.add(mesh);
if ( rotate ) {
clock = new THREE.Clock();
renderer.setAnimationLoop( animate );
controls.addEventListener( 'start', () => rotate = false );
controls.addEventListener( 'end', () => rotate = true );
}
render();
});
window.addEventListener('resize', onWindowResize);
}
function onWindowResize() {
const aspectRatio = "1.3333";
camera.aspect = aspectRatio;
camera.updateProjectionMatrix();
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
render();
}
function render() {
renderer.render(scene, camera);
}
function animate() {
const delta = clock.getDelta();
const direction = "right";
if ( rotate &amp;&amp; mesh ) {
const ry = 0.5;
if (direction === "left") {
mesh.rotation.y -= delta * ry;
} else {
mesh.rotation.y += delta * ry;
}
render();
}
}
&lt;/script>
&lt;/div>
&lt;h3>Assembly&lt;span class="hx:absolute hx:-mt-20" id="assembly">&lt;/span>
&lt;a href="#assembly" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Provided 3mf file is tested and ready to print on Prusa MK3 .
Parts are hold together by friction. No screws are needed.&lt;/p>
&lt;h3>Download&lt;span class="hx:absolute hx:-mt-20" id="download">&lt;/span>
&lt;a href="#download" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;ul>
&lt;li>&lt;a href="https://github.com/AntoninPvr/dev-board-enclosures/tree/main/stm32-nucleo-64/stl"target="_blank" rel="noopener">STL files&lt;/a>&lt;/li>
&lt;li>&lt;a href="https://github.com/AntoninPvr/dev-board-enclosures/tree/main/stm32-nucleo-64/3mf"target="_blank" rel="noopener">3MF file&lt;/a>&lt;/li>
&lt;li>&lt;a href="https://github.com/AntoninPvr/dev-board-enclosures/tree/main/stm32-nucleo-64/step"target="_blank" rel="noopener">STEP files&lt;/a>&lt;/li>
&lt;/ul></description></item><item><title>MarcoSlim</title><link>https://antoninpvr.github.io/hardware/marco/marcoslim/</link><pubDate>Fri, 17 Mar 2023 11:23:00 +0200</pubDate><guid>https://antoninpvr.github.io/hardware/marco/marcoslim/</guid><description>
&lt;img src="https://antoninpvr.github.io/hardware/marco/marcoslim/featured_hu_f8a6d027f847bf05.jpg" width="640" height="539"/>&lt;div class="hextra-cards hx:mt-4 hx:gap-4 hx:grid not-prose"
style="--hextra-cards-grid-cols: 3;">&lt;a
class="hextra-card hx:relative hx:group hx:flex hx:flex-col hx:justify-start hx:overflow-hidden hx:rounded-lg hx:border hx:border-gray-200 hx:text-current hx:no-underline hx:dark:shadow-none hx:hover:shadow-gray-100 hx:dark:hover:shadow-none hx:shadow-gray-100 hx:active:shadow-sm hx:active:shadow-gray-200 hx:transition-all hx:duration-200 hx:hover:border-gray-300 hx:bg-transparent hx:shadow-xs hx:dark:border-neutral-800 hx:hover:bg-slate-50 hx:hover:shadow-md hx:dark:hover:border-neutral-700 hx:dark:hover:bg-neutral-900"href="https://github.com/AntoninPvr/MarcoSlim"
target="_blank" rel="noreferrer">
&lt;span class="hextra-card-icon hx:flex hx:font-semibold hx:items-start hx:gap-2 hx:pt-4 hx:px-4 hx:text-gray-700 hx:hover:text-gray-900 hx:dark:text-neutral-200 hx:dark:hover:text-neutral-50">&lt;svg fill="currentColor" viewBox="3 3 18 18">
&lt;path d="M12 3C7.0275 3 3 7.12937 3 12.2276C3 16.3109 5.57625 19.7597 9.15374 20.9824C9.60374 21.0631 9.77249 20.7863 9.77249 20.5441C9.77249 20.3249 9.76125 19.5982 9.76125 18.8254C7.5 19.2522 6.915 18.2602 6.735 17.7412C6.63375 17.4759 6.19499 16.6569 5.8125 16.4378C5.4975 16.2647 5.0475 15.838 5.80124 15.8264C6.51 15.8149 7.01625 16.4954 7.18499 16.7723C7.99499 18.1679 9.28875 17.7758 9.80625 17.5335C9.885 16.9337 10.1212 16.53 10.38 16.2993C8.3775 16.0687 6.285 15.2728 6.285 11.7432C6.285 10.7397 6.63375 9.9092 7.20749 9.26326C7.1175 9.03257 6.8025 8.08674 7.2975 6.81794C7.2975 6.81794 8.05125 6.57571 9.77249 7.76377C10.4925 7.55615 11.2575 7.45234 12.0225 7.45234C12.7875 7.45234 13.5525 7.55615 14.2725 7.76377C15.9937 6.56418 16.7475 6.81794 16.7475 6.81794C17.2424 8.08674 16.9275 9.03257 16.8375 9.26326C17.4113 9.9092 17.76 10.7281 17.76 11.7432C17.76 15.2843 15.6563 16.0687 13.6537 16.2993C13.98 16.5877 14.2613 17.1414 14.2613 18.0065C14.2613 19.2407 14.25 20.2326 14.25 20.5441C14.25 20.7863 14.4188 21.0746 14.8688 20.9824C16.6554 20.364 18.2079 19.1866 19.3078 17.6162C20.4077 16.0457 20.9995 14.1611 21 12.2276C21 7.12937 16.9725 3 12 3Z">&lt;/path>
&lt;/svg>
MarcoSlim&lt;/span>
&lt;div class="hextra-card-subtitle hx:line-clamp-3 hx:text-sm hx:font-normal hx:text-gray-500 hx:dark:text-gray-400 hx:px-4 hx:mb-4 hx:mt-2">DISCONTINUED Slim version of Marco, payment terminal for Télécom Physique Strasbourg&lt;/div>&lt;div class="hextra-badge hextra-card-tag">
&lt;div class="hx:inline-flex hx:gap-1 hx:items-center hx:rounded-full hx:px-2.5 hx:leading-6 hx:text-[.65rem] hx:border hx:text-gray-600 hx:bg-gray-100 hx:dark:bg-neutral-800 hx:dark:text-neutral-200 hx:border-gray-200 hx:dark:border-neutral-700">&lt;svg height=12 xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" aria-hidden="true">&lt;path stroke-linecap="round" stroke-linejoin="round" d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z"/>&lt;/svg>v6.1.0&lt;/div>
&lt;/div>
&lt;/a>
&lt;/div>
&lt;div class="hx:overflow-x-auto hx:mt-6 hx:flex hx:rounded-lg hx:border hx:py-2 hx:ltr:pr-4 hx:rtl:pl-4 hx:contrast-more:border-current hx:contrast-more:dark:border-current hx:border-purple-200 hx:bg-purple-100 hx:text-purple-900 hx:dark:border-purple-200/30 hx:dark:bg-purple-900/30 hx:dark:text-purple-200">
&lt;div class="hx:ltr:pl-3 hx:ltr:pr-2 hx:rtl:pr-3 hx:rtl:pl-2">&lt;svg height=1.2em class="hx:inline-block hx:align-middle" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" aria-hidden="true">&lt;path stroke-linecap="round" stroke-linejoin="round" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>&lt;/svg>&lt;/div>
&lt;div class="hx:w-full hx:min-w-0 hx:leading-7">
&lt;div class="hx:mt-6 hx:leading-7 hx:first:mt-0">DISCONTINUED This project is discontinued and will not be updated anymore. Please refer to the new version of the Marco project&lt;/div>
&lt;/div>
&lt;/div>
&lt;hr>
&lt;h2>Preface&lt;span class="hx:absolute hx:-mt-20" id="preface">&lt;/span>
&lt;a href="#preface" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>MarcoSlim is an upgrade of original Marco. Project had been launched during the 2023 BDE of Télécom Physique Strasbourg. Original Marco was based on Raspberry PI and old style resistive screen and 3 meters of cables and adapters hardly stuff in rectangular 3D printed case.&lt;/p>
&lt;p>This new version is based on Orange PI zero 2W with a 7&amp;quot; capacitive touch screen. This modernized versiona aims to be more reliable and compact with a new RFID reader.&lt;/p>
&lt;p>Software was also modernized and is available here :
&lt;div class="hextra-cards hx:mt-4 hx:gap-4 hx:grid not-prose"
style="--hextra-cards-grid-cols: 3;">&lt;a
class="hextra-card hx:relative hx:group hx:flex hx:flex-col hx:justify-start hx:overflow-hidden hx:rounded-lg hx:border hx:border-gray-200 hx:text-current hx:no-underline hx:dark:shadow-none hx:hover:shadow-gray-100 hx:dark:hover:shadow-none hx:shadow-gray-100 hx:active:shadow-sm hx:active:shadow-gray-200 hx:transition-all hx:duration-200 hx:hover:border-gray-300 hx:bg-transparent hx:shadow-xs hx:dark:border-neutral-800 hx:hover:bg-slate-50 hx:hover:shadow-md hx:dark:hover:border-neutral-700 hx:dark:hover:bg-neutral-900">
&lt;span class="hextra-card-icon hx:flex hx:font-semibold hx:items-start hx:gap-2 hx:pt-4 hx:px-4 hx:text-gray-700 hx:hover:text-gray-900 hx:dark:text-neutral-200 hx:dark:hover:text-neutral-50">&lt;svg fill="currentColor" viewBox="3 3 18 18">
&lt;path d="M12 3C7.0275 3 3 7.12937 3 12.2276C3 16.3109 5.57625 19.7597 9.15374 20.9824C9.60374 21.0631 9.77249 20.7863 9.77249 20.5441C9.77249 20.3249 9.76125 19.5982 9.76125 18.8254C7.5 19.2522 6.915 18.2602 6.735 17.7412C6.63375 17.4759 6.19499 16.6569 5.8125 16.4378C5.4975 16.2647 5.0475 15.838 5.80124 15.8264C6.51 15.8149 7.01625 16.4954 7.18499 16.7723C7.99499 18.1679 9.28875 17.7758 9.80625 17.5335C9.885 16.9337 10.1212 16.53 10.38 16.2993C8.3775 16.0687 6.285 15.2728 6.285 11.7432C6.285 10.7397 6.63375 9.9092 7.20749 9.26326C7.1175 9.03257 6.8025 8.08674 7.2975 6.81794C7.2975 6.81794 8.05125 6.57571 9.77249 7.76377C10.4925 7.55615 11.2575 7.45234 12.0225 7.45234C12.7875 7.45234 13.5525 7.55615 14.2725 7.76377C15.9937 6.56418 16.7475 6.81794 16.7475 6.81794C17.2424 8.08674 16.9275 9.03257 16.8375 9.26326C17.4113 9.9092 17.76 10.7281 17.76 11.7432C17.76 15.2843 15.6563 16.0687 13.6537 16.2993C13.98 16.5877 14.2613 17.1414 14.2613 18.0065C14.2613 19.2407 14.25 20.2326 14.25 20.5441C14.25 20.7863 14.4188 21.0746 14.8688 20.9824C16.6554 20.364 18.2079 19.1866 19.3078 17.6162C20.4077 16.0457 20.9995 14.1611 21 12.2276C21 7.12937 16.9725 3 12 3Z">&lt;/path>
&lt;/svg>
GitHub Repo&lt;/span>
&lt;div class="hextra-card-subtitle hx:line-clamp-3 hx:text-sm hx:font-normal hx:text-gray-500 hx:dark:text-gray-400 hx:px-4 hx:mb-4 hx:mt-2">GitHub repository&lt;/div>&lt;div class="hextra-badge hextra-card-tag">
&lt;div class="hx:inline-flex hx:gap-1 hx:items-center hx:rounded-full hx:px-2.5 hx:leading-6 hx:text-[.65rem] hx:border hx:text-gray-600 hx:bg-gray-100 hx:dark:bg-neutral-800 hx:dark:text-neutral-200 hx:border-gray-200 hx:dark:border-neutral-700">&lt;svg height=12 xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" aria-hidden="true">&lt;path stroke-linecap="round" stroke-linejoin="round" d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z"/>&lt;/svg>no-release&lt;/div>
&lt;/div>
&lt;/a>
&lt;/div>
&lt;/p>
&lt;p>Marco is a project that aims to create a payment terminal for a school BDE (French equivalent of Student Union). It is based on a RFID card system. The terminal is used by the students to pay for their meals or drinks. The terminal is connected to a self-hosted server that manages the accounts of the students. This terminal act as cash register.&lt;/p>
&lt;p>This project is related to InsidePSBS app. Students can check their account and history on this mobile app available on IOS and Android. &lt;a href="https://github.com/info-telecom-strasbourg/InsidePSBS"target="_blank" rel="noopener">https://github.com/info-telecom-strasbourg/InsidePSBS&lt;/a> (Reserved to TPS and ESBS students)&lt;/p>
&lt;p>&lt;figure>
&lt;img src="https://github.com/AntoninPvr/MarcoSlim/blob/main/img/render/full.JPG?raw=true" title="Front view" alt="Marco slim front" loading="lazy" />
&lt;figcaption>Front view&lt;/figcaption>
&lt;/figure>&lt;/p>
&lt;p>&lt;figure>
&lt;img src="https://github.com/AntoninPvr/MarcoSlim/blob/main/img/render/internal.JPG?raw=true" title="Internal view, with electrical components exposed" alt="“Internal view, with electrical components exposed”" loading="lazy" />
&lt;figcaption>Internal view, with electrical components exposed&lt;/figcaption>
&lt;/figure>&lt;/p>
&lt;h2>Specifications&lt;span class="hx:absolute hx:-mt-20" id="specifications">&lt;/span>
&lt;a href="#specifications" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ul>
&lt;li>&lt;strong>Screen&lt;/strong>: 7&amp;quot; capacitive touch screen&lt;/li>
&lt;li>&lt;strong>RFID reader&lt;/strong>: 125kHz compatible&lt;/li>
&lt;li>&lt;strong>Power supply&lt;/strong>: USB-C 5V 3A&lt;/li>
&lt;li>&lt;strong>SBC&lt;/strong>: Orange PI zero 2W
&lt;ul>
&lt;li>SoC: Allwinner H618 quad-core Cortex-A53 @ 1.5GHz&lt;/li>
&lt;li>RAM: 1GB LPDDR4&lt;/li>
&lt;li>Wi-Fi: 802.11 ac (Wi-Fi 5)&lt;/li>
&lt;li>Bluetooth: 5.0&lt;/li>
&lt;/ul>
&lt;/li>
&lt;li>&lt;strong>Battery&lt;/strong>: 29.6Wh Li-ion (4x 3.7V 2000mAh)&lt;/li>
&lt;li>&lt;strong>Dimensions&lt;/strong>:
&lt;ul>
&lt;li>Length: 200mm&lt;/li>
&lt;li>Depth: 129mm&lt;/li>
&lt;li>Height: 38mm&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ul>
&lt;h2>Shopping list&lt;span class="hx:absolute hx:-mt-20" id="shopping-list">&lt;/span>
&lt;a href="#shopping-list" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;h3>Electronic parts&lt;span class="hx:absolute hx:-mt-20" id="electronic-parts">&lt;/span>
&lt;a href="#electronic-parts" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;ul>
&lt;li>
&lt;p>Orange PI zero 2W with 1GB RAM or more&lt;/p>
&lt;/li>
&lt;li>
&lt;p>Micro SD card 16GB ore more, U3 speed class or more&lt;/p>
&lt;/li>
&lt;li>
&lt;p>7&amp;quot; Waveshare touch screen or compatible with HDMI input&lt;/p>
&lt;/li>
&lt;li>
&lt;p>125kHz RFID reader&lt;/p>
&lt;/li>
&lt;li>
&lt;p>4x 3.7V 2000mAh Li-ion battery&lt;/p>
&lt;/li>
&lt;li>
&lt;p>USB-C power supply&lt;/p>
&lt;/li>
&lt;li>
&lt;p>mini HDMI to HDMI 100mm cable: A2 to C1&lt;/p>
&lt;/li>
&lt;li>
&lt;p>USB-C to micro usb 100mm cable: W3R to T1B&lt;/p>
&lt;/li>
&lt;li>
&lt;p>USB-C pannel 17mm center distance mount connector 20mm&lt;/p>
&lt;hr>
&lt;/li>
&lt;/ul>
&lt;h3>Mechanical parts&lt;span class="hx:absolute hx:-mt-20" id="mechanical-parts">&lt;/span>
&lt;a href="#mechanical-parts" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;ul>
&lt;li>
&lt;p>M4 screws 16mm x4&lt;/p>
&lt;/li>
&lt;li>
&lt;p>M4 inserts x4&lt;/p>
&lt;/li>
&lt;li>
&lt;p>M3 screws 10mm x15&lt;/p>
&lt;/li>
&lt;li>
&lt;p>M3 inserts x15&lt;/p>
&lt;hr>
&lt;/li>
&lt;/ul>
&lt;h3>Parts details&lt;span class="hx:absolute hx:-mt-20" id="parts-details">&lt;/span>
&lt;a href="#parts-details" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;table>
&lt;thead>
&lt;tr>
&lt;th>&lt;/th>
&lt;th>&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>Orange PI zero 2W&lt;/td>
&lt;td>&lt;p align="right"> &lt;img alt="Orange PI zero 2W" src="https://github.com/AntoninPvr/MarcoX/blob/main/img/orange_pi_zero_2w.jpg?raw=true" width="200">&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>Mini HDMI to HDMI 100mm cable: (A2 to C1). Large side of HDMI is on ribbon cable side. Mini HDMI large side is on ribbon connector side.&lt;/td>
&lt;td>&lt;p align="right">&lt;img alt="HDMI to Micro HDMI adapter" src="https://github.com/AntoninPvr/MarcoX/blob/main/img/hdmi_to_micro_hdmi.jpg?raw=true" width="200">&lt;/p>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>USB-C to micro usb 100mm cable: W3R to T1B&lt;/td>
&lt;td>&lt;p align="right">&lt;img alt="HDMI to Micro HDMI adapter" src="https://github.com/AntoninPvr/MarcoX/blob/main/img/usbc_to_micro_usb.jpg?raw=true" width="200">&lt;/p>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>USB-C pannel 17mm center distance mount connector 200mm&lt;/td>
&lt;td>&lt;p align="right">&lt;img alt="HDMI to Micro HDMI adapter" src="https://github.com/AntoninPvr/MarcoX/blob/main/img/usbc_pannel_mount.jpg?raw=true" width="200">&lt;/p>&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h3>Screen&lt;span class="hx:absolute hx:-mt-20" id="screen">&lt;/span>
&lt;a href="#screen" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Screen is 7&amp;quot; Waveshare touch screen or compatible with HDMI input it can be a compatible one as long as it has the same connectors and the same size.&lt;/p>
&lt;p>![&amp;ldquo;Front screen view&amp;rdquo;](&lt;a href="https://github.com/AntoninPvr/MarcoSlim/blob/main/img/screen_front.jpg?raw=true"target="_blank" rel="noopener">https://github.com/AntoninPvr/MarcoSlim/blob/main/img/screen_front.jpg?raw=true&lt;/a> &amp;ldquo;7&amp;rdquo; inch Screen&amp;quot;)&lt;/p>
&lt;p>&lt;figure>
&lt;img src="https://github.com/AntoninPvr/MarcoSlim/blob/main/img/screen_back.jpg?raw=true" title="Screen PCB" alt="“Back screen PCB view”" loading="lazy" />
&lt;figcaption>Screen PCB&lt;/figcaption>
&lt;/figure>&lt;/p>
&lt;h2>3D printed parts&lt;span class="hx:absolute hx:-mt-20" id="3d-printed-parts">&lt;/span>
&lt;a href="#3d-printed-parts" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>All parts are in &lt;code>.stl&lt;/code> format. They are designed to be printed without support.&lt;/p>
&lt;h3>Printer settings&lt;span class="hx:absolute hx:-mt-20" id="printer-settings">&lt;/span>
&lt;a href="#printer-settings" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Tested settings, but feel free to adapt them to your printer and material.:&lt;/p>
&lt;ul>
&lt;li>Layer height: 0.2mm&lt;/li>
&lt;li>Infill: 20%&lt;/li>
&lt;li>Nozzle diameter: 0.4mm&lt;/li>
&lt;li>Material: PLA or PETG&lt;/li>
&lt;/ul>
&lt;p>&lt;em>Note: PTEG is recommended for better water and UV resistance.&lt;/em>&lt;/p>
&lt;h3>All parts&lt;span class="hx:absolute hx:-mt-20" id="all-parts">&lt;/span>
&lt;a href="#all-parts" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>&lt;figure>
&lt;img src="https://github.com/AntoninPvr/MarcoSlim/blob/main/img/render/front.JPG?raw=true" title="Front cover" alt="“Front cover part marco slim view”" loading="lazy" />
&lt;figcaption>Front cover&lt;/figcaption>
&lt;/figure>&lt;/p>
&lt;p>&lt;figure>
&lt;img src="https://github.com/AntoninPvr/MarcoSlim/blob/main/img/render/enclosure.JPG?raw=true" title="Back cover" alt="“Back cover part marco slim view”" loading="lazy" />
&lt;figcaption>Back cover&lt;/figcaption>
&lt;/figure>&lt;/p>
&lt;p>&lt;figure>
&lt;img src="https://github.com/AntoninPvr/MarcoSlim/blob/main/img/render/frame.JPG?raw=true" title="Main frame" alt="“Internal frame part marco slim view”" loading="lazy" />
&lt;figcaption>Main frame&lt;/figcaption>
&lt;/figure>&lt;/p>
&lt;h3>Front&lt;span class="hx:absolute hx:-mt-20" id="front">&lt;/span>
&lt;a href="#front" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;div style="display: flex">
&lt;div style="width: 50%">
&lt;p>&lt;strong>File:&lt;/strong> &lt;a href="https://github.com/AntoninPvr/MarcoSlim/blob/main/front.STL"target="_blank" rel="noopener">front.STL&lt;/a>&lt;/p>
&lt;p>The front cover hold the screen and allow it to be attached without overthickness. It also includes the RFID reader coil use for card detection, USB-C charging hole, power activity LED and Power button. This part is attached to the enclosure with 4 M4 screws, and provide a sort of seal to prevent dust and water to enter the enclosure.&lt;/p>
&lt;p>&lt;em>Note: clearance for sealing are very tight. This make 3D printing more difficult and printer dependent.&lt;/em>&lt;/p>
&lt;/div>
&lt;div id="./models/front.glb" style="width: 50%; height: 50%">
&lt;/div>
&lt;script type="importmap">
{
"imports": {
"three": "https://unpkg.com/three@0.161.0/build/three.module.js",
"three/addons/": "https://unpkg.com/three@0.161.0/examples/jsm/"
}
}
&lt;/script>
&lt;script type="module">
import * as THREE from 'three';
import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { MeshoptDecoder } from 'three/addons/libs/meshopt_decoder.module.js';
import { DRACOLoader } from 'three/addons/loaders/DRACOLoader.js'
let container;
let camera, scene, renderer, clock;
let mesh;
let rotate, controls;
init();
render();
function init() {
container = document.getElementById(".\/models\/front.glb");
container.onload = () => {
onWindowResize();
}
const aspectRatio = "1.3333";
const model_path = ".\/models\/front.glb";
const modelColor = "#3f4d52";
rotate = "true" === "true";
renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
renderer.setPixelRatio(window.devicePixelRatio);
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1;
renderer.outputColorSpace = THREE.SRGBColorSpace;
container.appendChild(renderer.domElement);
camera = new THREE.PerspectiveCamera(45, aspectRatio, 0.1, 1000);
camera.position.set(1, 0, 1);
camera.lookAt(0, 0, 0);
controls = new OrbitControls(camera, renderer.domElement);
controls.addEventListener('change', render);
controls.minDistance = 1;
controls.maxDistance = 10;
controls.target.set(0, 0, 0);
controls.update();
const environment = new RoomEnvironment(renderer);
const pmremGenerator = new THREE.PMREMGenerator(renderer);
scene = new THREE.Scene();
scene.environment = pmremGenerator.fromScene(environment).texture;
const ambientLight = new THREE.AmbientLight(0xffffff, 1);
scene.add(ambientLight);
const light1 = new THREE.PointLight( 0xffeeee, 50, 0);
light1.position.set( 0, 2, 1 );
light1.castShadow = true;
scene.add( light1 );
const loader = new GLTFLoader();
const dracoLoader = new DRACOLoader();
dracoLoader.setDecoderPath('/examples/jsm/libs/draco/');
loader.setMeshoptDecoder(MeshoptDecoder);
loader.setDRACOLoader(dracoLoader);
loader.load(model_path, function (gltf) {
const root = gltf.scene;
const box = new THREE.Box3().setFromObject(root);
const size = box.getSize(new THREE.Vector3());
const center = box.getCenter(new THREE.Vector3());
root.position.x += (root.position.x - center.x);
root.position.y += (root.position.y - center.y);
root.position.z += (root.position.z - center.z);
mesh = new THREE.Group();
mesh.add(root);
if (modelColor &amp;&amp; modelColor !== "") {
root.traverse((child) => {
if (child.isMesh) {
child.material.color.set(modelColor);
child.material.roughness = 0.8;
child.material.metalness = 0.2;
}
});
}
root.traverse((child) => {
if (child.isMesh) {
child.castShadow = true;
child.receiveShadow = true;
}
});
const maxSize = Math.max(size.x, size.y, size.z);
mesh.scale.set(1 / maxSize, 1 / maxSize, 1 / maxSize);
scene.add(mesh);
if ( rotate ) {
clock = new THREE.Clock();
renderer.setAnimationLoop( animate );
controls.addEventListener( 'start', () => rotate = false );
controls.addEventListener( 'end', () => rotate = true );
}
render();
});
window.addEventListener('resize', onWindowResize);
}
function onWindowResize() {
const aspectRatio = "1.3333";
camera.aspect = aspectRatio;
camera.updateProjectionMatrix();
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
render();
}
function render() {
renderer.render(scene, camera);
}
function animate() {
const delta = clock.getDelta();
const direction = "left";
if ( rotate &amp;&amp; mesh ) {
const ry = 0.5;
if (direction === "left") {
mesh.rotation.y -= delta * ry;
} else {
mesh.rotation.y += delta * ry;
}
render();
}
}
&lt;/script>
&lt;/div>
&lt;h3>Internal frame&lt;span class="hx:absolute hx:-mt-20" id="internal-frame">&lt;/span>
&lt;a href="#internal-frame" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;div style="display: flex">
&lt;div style="width: 50%">
&lt;p>&lt;strong>File:&lt;/strong> &lt;a href="https://github.com/AntoninPvr/MarcoSlim/blob/main/internal_frame.STL"target="_blank" rel="noopener">internal_frame.STL&lt;/a>&lt;/p>
&lt;p>This frame is attached to the screen and is used to mount Orange PI, Li-ion batteries, PCB and Wi-Fi antenna.&lt;/p>
&lt;p>&lt;em>Note: For this part adding support may improve print quality&lt;/em>&lt;/p>
&lt;/div>
&lt;div id="./models/internal_frame.glb" style="width: 50%; height: 50%">
&lt;/div>
&lt;script type="importmap">
{
"imports": {
"three": "https://unpkg.com/three@0.161.0/build/three.module.js",
"three/addons/": "https://unpkg.com/three@0.161.0/examples/jsm/"
}
}
&lt;/script>
&lt;script type="module">
import * as THREE from 'three';
import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { MeshoptDecoder } from 'three/addons/libs/meshopt_decoder.module.js';
import { DRACOLoader } from 'three/addons/loaders/DRACOLoader.js'
let container;
let camera, scene, renderer, clock;
let mesh;
let rotate, controls;
init();
render();
function init() {
container = document.getElementById(".\/models\/internal_frame.glb");
container.onload = () => {
onWindowResize();
}
const aspectRatio = "1.3333";
const model_path = ".\/models\/internal_frame.glb";
const modelColor = "#3f4d52";
rotate = "true" === "true";
renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
renderer.setPixelRatio(window.devicePixelRatio);
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1;
renderer.outputColorSpace = THREE.SRGBColorSpace;
container.appendChild(renderer.domElement);
camera = new THREE.PerspectiveCamera(45, aspectRatio, 0.1, 1000);
camera.position.set(1, 0, 1);
camera.lookAt(0, 0, 0);
controls = new OrbitControls(camera, renderer.domElement);
controls.addEventListener('change', render);
controls.minDistance = 1;
controls.maxDistance = 10;
controls.target.set(0, 0, 0);
controls.update();
const environment = new RoomEnvironment(renderer);
const pmremGenerator = new THREE.PMREMGenerator(renderer);
scene = new THREE.Scene();
scene.environment = pmremGenerator.fromScene(environment).texture;
const ambientLight = new THREE.AmbientLight(0xffffff, 1);
scene.add(ambientLight);
const light1 = new THREE.PointLight( 0xffeeee, 50, 0);
light1.position.set( 0, 2, 1 );
light1.castShadow = true;
scene.add( light1 );
const loader = new GLTFLoader();
const dracoLoader = new DRACOLoader();
dracoLoader.setDecoderPath('/examples/jsm/libs/draco/');
loader.setMeshoptDecoder(MeshoptDecoder);
loader.setDRACOLoader(dracoLoader);
loader.load(model_path, function (gltf) {
const root = gltf.scene;
const box = new THREE.Box3().setFromObject(root);
const size = box.getSize(new THREE.Vector3());
const center = box.getCenter(new THREE.Vector3());
root.position.x += (root.position.x - center.x);
root.position.y += (root.position.y - center.y);
root.position.z += (root.position.z - center.z);
mesh = new THREE.Group();
mesh.add(root);
if (modelColor &amp;&amp; modelColor !== "") {
root.traverse((child) => {
if (child.isMesh) {
child.material.color.set(modelColor);
child.material.roughness = 0.8;
child.material.metalness = 0.2;
}
});
}
root.traverse((child) => {
if (child.isMesh) {
child.castShadow = true;
child.receiveShadow = true;
}
});
const maxSize = Math.max(size.x, size.y, size.z);
mesh.scale.set(1 / maxSize, 1 / maxSize, 1 / maxSize);
scene.add(mesh);
if ( rotate ) {
clock = new THREE.Clock();
renderer.setAnimationLoop( animate );
controls.addEventListener( 'start', () => rotate = false );
controls.addEventListener( 'end', () => rotate = true );
}
render();
});
window.addEventListener('resize', onWindowResize);
}
function onWindowResize() {
const aspectRatio = "1.3333";
camera.aspect = aspectRatio;
camera.updateProjectionMatrix();
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
render();
}
function render() {
renderer.render(scene, camera);
}
function animate() {
const delta = clock.getDelta();
const direction = "right";
if ( rotate &amp;&amp; mesh ) {
const ry = 0.5;
if (direction === "left") {
mesh.rotation.y -= delta * ry;
} else {
mesh.rotation.y += delta * ry;
}
render();
}
}
&lt;/script>
&lt;/div>
&lt;h3>Cover&lt;span class="hx:absolute hx:-mt-20" id="cover">&lt;/span>
&lt;a href="#cover" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;div style="display: flex">
&lt;div style="width: 50%">
&lt;p>&lt;strong>File:&lt;/strong> &lt;a href="https://github.com/AntoninPvr/MarcoSlim/blob/main/cover.STL"target="_blank" rel="noopener">cover.STL&lt;/a>&lt;/p>
&lt;p>The back cover is used to close the enclosure. It is attached to the enclosure with 4 M4 screws.&lt;/p>
&lt;p>&lt;em>Note: clearance for sealing are very tight. This make 3D printing more difficult and printer dependent.&lt;/em>&lt;/p>
&lt;/div>
&lt;div id="./models/cover.glb" style="width: 50%; height: 50%">
&lt;/div>
&lt;script type="importmap">
{
"imports": {
"three": "https://unpkg.com/three@0.161.0/build/three.module.js",
"three/addons/": "https://unpkg.com/three@0.161.0/examples/jsm/"
}
}
&lt;/script>
&lt;script type="module">
import * as THREE from 'three';
import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { MeshoptDecoder } from 'three/addons/libs/meshopt_decoder.module.js';
import { DRACOLoader } from 'three/addons/loaders/DRACOLoader.js'
let container;
let camera, scene, renderer, clock;
let mesh;
let rotate, controls;
init();
render();
function init() {
container = document.getElementById(".\/models\/cover.glb");
container.onload = () => {
onWindowResize();
}
const aspectRatio = "1.3333";
const model_path = ".\/models\/cover.glb";
const modelColor = "#3f4d52";
rotate = "true" === "true";
renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
renderer.setPixelRatio(window.devicePixelRatio);
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1;
renderer.outputColorSpace = THREE.SRGBColorSpace;
container.appendChild(renderer.domElement);
camera = new THREE.PerspectiveCamera(45, aspectRatio, 0.1, 1000);
camera.position.set(1, 0, 1);
camera.lookAt(0, 0, 0);
controls = new OrbitControls(camera, renderer.domElement);
controls.addEventListener('change', render);
controls.minDistance = 1;
controls.maxDistance = 10;
controls.target.set(0, 0, 0);
controls.update();
const environment = new RoomEnvironment(renderer);
const pmremGenerator = new THREE.PMREMGenerator(renderer);
scene = new THREE.Scene();
scene.environment = pmremGenerator.fromScene(environment).texture;
const ambientLight = new THREE.AmbientLight(0xffffff, 1);
scene.add(ambientLight);
const light1 = new THREE.PointLight( 0xffeeee, 50, 0);
light1.position.set( 0, 2, 1 );
light1.castShadow = true;
scene.add( light1 );
const loader = new GLTFLoader();
const dracoLoader = new DRACOLoader();
dracoLoader.setDecoderPath('/examples/jsm/libs/draco/');
loader.setMeshoptDecoder(MeshoptDecoder);
loader.setDRACOLoader(dracoLoader);
loader.load(model_path, function (gltf) {
const root = gltf.scene;
const box = new THREE.Box3().setFromObject(root);
const size = box.getSize(new THREE.Vector3());
const center = box.getCenter(new THREE.Vector3());
root.position.x += (root.position.x - center.x);
root.position.y += (root.position.y - center.y);
root.position.z += (root.position.z - center.z);
mesh = new THREE.Group();
mesh.add(root);
if (modelColor &amp;&amp; modelColor !== "") {
root.traverse((child) => {
if (child.isMesh) {
child.material.color.set(modelColor);
child.material.roughness = 0.8;
child.material.metalness = 0.2;
}
});
}
root.traverse((child) => {
if (child.isMesh) {
child.castShadow = true;
child.receiveShadow = true;
}
});
const maxSize = Math.max(size.x, size.y, size.z);
mesh.scale.set(1 / maxSize, 1 / maxSize, 1 / maxSize);
scene.add(mesh);
if ( rotate ) {
clock = new THREE.Clock();
renderer.setAnimationLoop( animate );
controls.addEventListener( 'start', () => rotate = false );
controls.addEventListener( 'end', () => rotate = true );
}
render();
});
window.addEventListener('resize', onWindowResize);
}
function onWindowResize() {
const aspectRatio = "1.3333";
camera.aspect = aspectRatio;
camera.updateProjectionMatrix();
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
render();
}
function render() {
renderer.render(scene, camera);
}
function animate() {
const delta = clock.getDelta();
const direction = "left";
if ( rotate &amp;&amp; mesh ) {
const ry = 0.5;
if (direction === "left") {
mesh.rotation.y -= delta * ry;
} else {
mesh.rotation.y += delta * ry;
}
render();
}
}
&lt;/script>
&lt;/div>
&lt;h3>Miscellanous&lt;span class="hx:absolute hx:-mt-20" id="miscellanous">&lt;/span>
&lt;a href="#miscellanous" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;div style="display: flex">
&lt;div style="width: 70%">
&lt;h4>Orange PI Standoff&lt;span class="hx:absolute hx:-mt-20" id="orange-pi-standoff">&lt;/span>
&lt;a href="#orange-pi-standoff" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h4>&lt;p>&lt;strong>File:&lt;/strong> &lt;a href="https://github.com/AntoninPvr/MarcoSlim/blob/main/orange_pi_standoff.STL"target="_blank" rel="noopener">orange_pi_standoff.STL&lt;/a>&lt;/p>
&lt;p>Standoff are used to maintain Orange PI in place and add spacing for electronics components bellow. They are attached to the internal frame with 3 M3 screws.&lt;/p>
&lt;/div>
&lt;div id="./models/orange_pi_standoff.glb" style="width: 20%; height: 50%">
&lt;/div>
&lt;script type="importmap">
{
"imports": {
"three": "https://unpkg.com/three@0.161.0/build/three.module.js",
"three/addons/": "https://unpkg.com/three@0.161.0/examples/jsm/"
}
}
&lt;/script>
&lt;script type="module">
import * as THREE from 'three';
import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { MeshoptDecoder } from 'three/addons/libs/meshopt_decoder.module.js';
import { DRACOLoader } from 'three/addons/loaders/DRACOLoader.js'
let container;
let camera, scene, renderer, clock;
let mesh;
let rotate, controls;
init();
render();
function init() {
container = document.getElementById(".\/models\/orange_pi_standoff.glb");
container.onload = () => {
onWindowResize();
}
const aspectRatio = "1.3333";
const model_path = ".\/models\/orange_pi_standoff.glb";
const modelColor = "#3f4d52";
rotate = "true" === "true";
renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
renderer.setPixelRatio(window.devicePixelRatio);
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1;
renderer.outputColorSpace = THREE.SRGBColorSpace;
container.appendChild(renderer.domElement);
camera = new THREE.PerspectiveCamera(45, aspectRatio, 0.1, 1000);
camera.position.set(1, 0, 1);
camera.lookAt(0, 0, 0);
controls = new OrbitControls(camera, renderer.domElement);
controls.addEventListener('change', render);
controls.minDistance = 1;
controls.maxDistance = 10;
controls.target.set(0, 0, 0);
controls.update();
const environment = new RoomEnvironment(renderer);
const pmremGenerator = new THREE.PMREMGenerator(renderer);
scene = new THREE.Scene();
scene.environment = pmremGenerator.fromScene(environment).texture;
const ambientLight = new THREE.AmbientLight(0xffffff, 1);
scene.add(ambientLight);
const light1 = new THREE.PointLight( 0xffeeee, 50, 0);
light1.position.set( 0, 2, 1 );
light1.castShadow = true;
scene.add( light1 );
const loader = new GLTFLoader();
const dracoLoader = new DRACOLoader();
dracoLoader.setDecoderPath('/examples/jsm/libs/draco/');
loader.setMeshoptDecoder(MeshoptDecoder);
loader.setDRACOLoader(dracoLoader);
loader.load(model_path, function (gltf) {
const root = gltf.scene;
const box = new THREE.Box3().setFromObject(root);
const size = box.getSize(new THREE.Vector3());
const center = box.getCenter(new THREE.Vector3());
root.position.x += (root.position.x - center.x);
root.position.y += (root.position.y - center.y);
root.position.z += (root.position.z - center.z);
mesh = new THREE.Group();
mesh.add(root);
if (modelColor &amp;&amp; modelColor !== "") {
root.traverse((child) => {
if (child.isMesh) {
child.material.color.set(modelColor);
child.material.roughness = 0.8;
child.material.metalness = 0.2;
}
});
}
root.traverse((child) => {
if (child.isMesh) {
child.castShadow = true;
child.receiveShadow = true;
}
});
const maxSize = Math.max(size.x, size.y, size.z);
mesh.scale.set(1 / maxSize, 1 / maxSize, 1 / maxSize);
scene.add(mesh);
if ( rotate ) {
clock = new THREE.Clock();
renderer.setAnimationLoop( animate );
controls.addEventListener( 'start', () => rotate = false );
controls.addEventListener( 'end', () => rotate = true );
}
render();
});
window.addEventListener('resize', onWindowResize);
}
function onWindowResize() {
const aspectRatio = "1.3333";
camera.aspect = aspectRatio;
camera.updateProjectionMatrix();
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
render();
}
function render() {
renderer.render(scene, camera);
}
function animate() {
const delta = clock.getDelta();
const direction = "left";
if ( rotate &amp;&amp; mesh ) {
const ry = 0.5;
if (direction === "left") {
mesh.rotation.y -= delta * ry;
} else {
mesh.rotation.y += delta * ry;
}
render();
}
}
&lt;/script>
&lt;/div>
&lt;div style="display: flex">
&lt;div style="width: 70%">
&lt;h4>Battery holder&lt;span class="hx:absolute hx:-mt-20" id="battery-holder">&lt;/span>
&lt;a href="#battery-holder" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h4>&lt;p>&lt;strong>File:&lt;/strong> &lt;a href="https://github.com/AntoninPvr/MarcoSlim/blob/main/18650_4_holder.STL"target="_blank" rel="noopener">18650_4_holder.STL&lt;/a>&lt;/p>
&lt;p>Battery holder is used to maintain 4 Li-ion batteries in place. It is attached to the internal frame with 4 M3 screws.&lt;/p>
&lt;/div>
&lt;div id="./models/18650_4_holder.glb" style="width: 50%; height: 50%">
&lt;/div>
&lt;script type="importmap">
{
"imports": {
"three": "https://unpkg.com/three@0.161.0/build/three.module.js",
"three/addons/": "https://unpkg.com/three@0.161.0/examples/jsm/"
}
}
&lt;/script>
&lt;script type="module">
import * as THREE from 'three';
import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { MeshoptDecoder } from 'three/addons/libs/meshopt_decoder.module.js';
import { DRACOLoader } from 'three/addons/loaders/DRACOLoader.js'
let container;
let camera, scene, renderer, clock;
let mesh;
let rotate, controls;
init();
render();
function init() {
container = document.getElementById(".\/models\/18650_4_holder.glb");
container.onload = () => {
onWindowResize();
}
const aspectRatio = "1.3333";
const model_path = ".\/models\/18650_4_holder.glb";
const modelColor = "#3f4d52";
rotate = "true" === "true";
renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
renderer.setPixelRatio(window.devicePixelRatio);
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1;
renderer.outputColorSpace = THREE.SRGBColorSpace;
container.appendChild(renderer.domElement);
camera = new THREE.PerspectiveCamera(45, aspectRatio, 0.1, 1000);
camera.position.set(1, 0, 1);
camera.lookAt(0, 0, 0);
controls = new OrbitControls(camera, renderer.domElement);
controls.addEventListener('change', render);
controls.minDistance = 1;
controls.maxDistance = 10;
controls.target.set(0, 0, 0);
controls.update();
const environment = new RoomEnvironment(renderer);
const pmremGenerator = new THREE.PMREMGenerator(renderer);
scene = new THREE.Scene();
scene.environment = pmremGenerator.fromScene(environment).texture;
const ambientLight = new THREE.AmbientLight(0xffffff, 1);
scene.add(ambientLight);
const light1 = new THREE.PointLight( 0xffeeee, 50, 0);
light1.position.set( 0, 2, 1 );
light1.castShadow = true;
scene.add( light1 );
const loader = new GLTFLoader();
const dracoLoader = new DRACOLoader();
dracoLoader.setDecoderPath('/examples/jsm/libs/draco/');
loader.setMeshoptDecoder(MeshoptDecoder);
loader.setDRACOLoader(dracoLoader);
loader.load(model_path, function (gltf) {
const root = gltf.scene;
const box = new THREE.Box3().setFromObject(root);
const size = box.getSize(new THREE.Vector3());
const center = box.getCenter(new THREE.Vector3());
root.position.x += (root.position.x - center.x);
root.position.y += (root.position.y - center.y);
root.position.z += (root.position.z - center.z);
mesh = new THREE.Group();
mesh.add(root);
if (modelColor &amp;&amp; modelColor !== "") {
root.traverse((child) => {
if (child.isMesh) {
child.material.color.set(modelColor);
child.material.roughness = 0.8;
child.material.metalness = 0.2;
}
});
}
root.traverse((child) => {
if (child.isMesh) {
child.castShadow = true;
child.receiveShadow = true;
}
});
const maxSize = Math.max(size.x, size.y, size.z);
mesh.scale.set(1 / maxSize, 1 / maxSize, 1 / maxSize);
scene.add(mesh);
if ( rotate ) {
clock = new THREE.Clock();
renderer.setAnimationLoop( animate );
controls.addEventListener( 'start', () => rotate = false );
controls.addEventListener( 'end', () => rotate = true );
}
render();
});
window.addEventListener('resize', onWindowResize);
}
function onWindowResize() {
const aspectRatio = "1.3333";
camera.aspect = aspectRatio;
camera.updateProjectionMatrix();
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
render();
}
function render() {
renderer.render(scene, camera);
}
function animate() {
const delta = clock.getDelta();
const direction = "right";
if ( rotate &amp;&amp; mesh ) {
const ry = 0.5;
if (direction === "left") {
mesh.rotation.y -= delta * ry;
} else {
mesh.rotation.y += delta * ry;
}
render();
}
}
&lt;/script>
&lt;/div>
&lt;div style="display: flex">
&lt;div style="width: 70%">
&lt;h4>Power button&lt;span class="hx:absolute hx:-mt-20" id="power-button">&lt;/span>
&lt;a href="#power-button" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h4>&lt;p>&lt;strong>File:&lt;/strong> &lt;a href="https://github.com/AntoninPvr/MarcoSlim/blob/main/power_button.STL"target="_blank" rel="noopener">power_button.STL&lt;/a>&lt;/p>
&lt;p>Power button.&lt;/p>
&lt;/div>
&lt;div id="./models/power_button.glb" style="width: 20%; height: 50%">
&lt;/div>
&lt;script type="importmap">
{
"imports": {
"three": "https://unpkg.com/three@0.161.0/build/three.module.js",
"three/addons/": "https://unpkg.com/three@0.161.0/examples/jsm/"
}
}
&lt;/script>
&lt;script type="module">
import * as THREE from 'three';
import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { MeshoptDecoder } from 'three/addons/libs/meshopt_decoder.module.js';
import { DRACOLoader } from 'three/addons/loaders/DRACOLoader.js'
let container;
let camera, scene, renderer, clock;
let mesh;
let rotate, controls;
init();
render();
function init() {
container = document.getElementById(".\/models\/power_button.glb");
container.onload = () => {
onWindowResize();
}
const aspectRatio = "1.3333";
const model_path = ".\/models\/power_button.glb";
const modelColor = "#3f4d52";
rotate = "true" === "true";
renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
renderer.setPixelRatio(window.devicePixelRatio);
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1;
renderer.outputColorSpace = THREE.SRGBColorSpace;
container.appendChild(renderer.domElement);
camera = new THREE.PerspectiveCamera(45, aspectRatio, 0.1, 1000);
camera.position.set(1, 0, 1);
camera.lookAt(0, 0, 0);
controls = new OrbitControls(camera, renderer.domElement);
controls.addEventListener('change', render);
controls.minDistance = 1;
controls.maxDistance = 10;
controls.target.set(0, 0, 0);
controls.update();
const environment = new RoomEnvironment(renderer);
const pmremGenerator = new THREE.PMREMGenerator(renderer);
scene = new THREE.Scene();
scene.environment = pmremGenerator.fromScene(environment).texture;
const ambientLight = new THREE.AmbientLight(0xffffff, 1);
scene.add(ambientLight);
const light1 = new THREE.PointLight( 0xffeeee, 50, 0);
light1.position.set( 0, 2, 1 );
light1.castShadow = true;
scene.add( light1 );
const loader = new GLTFLoader();
const dracoLoader = new DRACOLoader();
dracoLoader.setDecoderPath('/examples/jsm/libs/draco/');
loader.setMeshoptDecoder(MeshoptDecoder);
loader.setDRACOLoader(dracoLoader);
loader.load(model_path, function (gltf) {
const root = gltf.scene;
const box = new THREE.Box3().setFromObject(root);
const size = box.getSize(new THREE.Vector3());
const center = box.getCenter(new THREE.Vector3());
root.position.x += (root.position.x - center.x);
root.position.y += (root.position.y - center.y);
root.position.z += (root.position.z - center.z);
mesh = new THREE.Group();
mesh.add(root);
if (modelColor &amp;&amp; modelColor !== "") {
root.traverse((child) => {
if (child.isMesh) {
child.material.color.set(modelColor);
child.material.roughness = 0.8;
child.material.metalness = 0.2;
}
});
}
root.traverse((child) => {
if (child.isMesh) {
child.castShadow = true;
child.receiveShadow = true;
}
});
const maxSize = Math.max(size.x, size.y, size.z);
mesh.scale.set(1 / maxSize, 1 / maxSize, 1 / maxSize);
scene.add(mesh);
if ( rotate ) {
clock = new THREE.Clock();
renderer.setAnimationLoop( animate );
controls.addEventListener( 'start', () => rotate = false );
controls.addEventListener( 'end', () => rotate = true );
}
render();
});
window.addEventListener('resize', onWindowResize);
}
function onWindowResize() {
const aspectRatio = "1.3333";
camera.aspect = aspectRatio;
camera.updateProjectionMatrix();
renderer.setSize(container.clientWidth, container.clientWidth / aspectRatio);
render();
}
function render() {
renderer.render(scene, camera);
}
function animate() {
const delta = clock.getDelta();
const direction = "left";
if ( rotate &amp;&amp; mesh ) {
const ry = 0.5;
if (direction === "left") {
mesh.rotation.y -= delta * ry;
} else {
mesh.rotation.y += delta * ry;
}
render();
}
}
&lt;/script>
&lt;/div>
&lt;h2>Electronics&lt;span class="hx:absolute hx:-mt-20" id="electronics">&lt;/span>
&lt;a href="#electronics" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;h3>RFID reader&lt;span class="hx:absolute hx:-mt-20" id="rfid-reader">&lt;/span>
&lt;a href="#rfid-reader" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>RFID reader is connected to Orange PI with either USB if the reader use is the classic USB reader that act as a keyboard or GPIO if the reader is custom made on PCB.&lt;/p>
&lt;p>The RFID coil is placed in the front cover arround the screen. By this way the card can be detected by the reader when the card is hold in the user hand when he interact with the screen. This is a way to make the user experience more intuitive. Coil is connected to a tiny PCB with a connector that is attached to the front cover with 2 M3 screws. (see front cover bellow)&lt;/p>
&lt;figure class="zoom-figure ">
&lt;input type="checkbox" id="zoom-c4b301d1adb93ad8e00ccbe16f4c72fb" class="zoom-checkbox">
&lt;label for="zoom-c4b301d1adb93ad8e00ccbe16f4c72fb" class="zoom-thumb">
&lt;img src="https://github.com/AntoninPvr/MarcoSlim/blob/main/img/render/coil_detail.JPG?raw=true" alt="Coil details">
&lt;/label>
&lt;label for="zoom-c4b301d1adb93ad8e00ccbe16f4c72fb" class="zoom-overlay">
&lt;img src="https://github.com/AntoninPvr/MarcoSlim/blob/main/img/render/coil_detail.JPG?raw=true" alt="Coil details">
&lt;/label>
&lt;figcaption>&lt;p>Coil details&lt;/p>&lt;/figcaption>
&lt;/figure>
&lt;p>But the screen is made with a large metal plate that disturb the magnetic field of the coil. After multiple tests this idea was abandoned in favor of a more classic placement of the coil with the new MarcoX version.&lt;/p>
&lt;h3>Power button and activity LED&lt;span class="hx:absolute hx:-mt-20" id="power-button-and-activity-led">&lt;/span>
&lt;a href="#power-button-and-activity-led" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Power button is connected to PCB with a 2 pin connector and trigger Orange PI zero 2W boot. It is placed in the front cover and is used to power on and off the terminal.&lt;/p>
&lt;p>Activity LED is bicolor and connected to PCB with a 3 pin connector. It is placed in the front cover to indicate the charging status of the terminal.&lt;/p>
&lt;figure class="zoom-figure ">
&lt;input type="checkbox" id="zoom-4ba250e20fb9c33d5632f5b2fc1752ef" class="zoom-checkbox">
&lt;label for="zoom-4ba250e20fb9c33d5632f5b2fc1752ef" class="zoom-thumb">
&lt;img src="https://github.com/AntoninPvr/MarcoSlim/blob/main/img/render/power_button.JPG?raw=true" alt="Power button details">
&lt;/label>
&lt;label for="zoom-4ba250e20fb9c33d5632f5b2fc1752ef" class="zoom-overlay">
&lt;img src="https://github.com/AntoninPvr/MarcoSlim/blob/main/img/render/power_button.JPG?raw=true" alt="Power button details">
&lt;/label>
&lt;figcaption>&lt;p>Power button&lt;/p>&lt;/figcaption>
&lt;/figure>
&lt;h2>Related projects&lt;span class="hx:absolute hx:-mt-20" id="related-projects">&lt;/span>
&lt;a href="#related-projects" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>
&lt;div class="hextra-cards hx:mt-4 hx:gap-4 hx:grid not-prose"
style="--hextra-cards-grid-cols: 3;">&lt;a
class="hextra-card hx:relative hx:group hx:flex hx:flex-col hx:justify-start hx:overflow-hidden hx:rounded-lg hx:border hx:border-gray-200 hx:text-current hx:no-underline hx:dark:shadow-none hx:hover:shadow-gray-100 hx:dark:hover:shadow-none hx:shadow-gray-100 hx:active:shadow-sm hx:active:shadow-gray-200 hx:transition-all hx:duration-200 hx:hover:border-gray-300 hx:bg-transparent hx:shadow-xs hx:dark:border-neutral-800 hx:hover:bg-slate-50 hx:hover:shadow-md hx:dark:hover:border-neutral-700 hx:dark:hover:bg-neutral-900">
&lt;span class="hextra-card-icon hx:flex hx:font-semibold hx:items-start hx:gap-2 hx:pt-4 hx:px-4 hx:text-gray-700 hx:hover:text-gray-900 hx:dark:text-neutral-200 hx:dark:hover:text-neutral-50">&lt;svg fill="currentColor" viewBox="3 3 18 18">
&lt;path d="M12 3C7.0275 3 3 7.12937 3 12.2276C3 16.3109 5.57625 19.7597 9.15374 20.9824C9.60374 21.0631 9.77249 20.7863 9.77249 20.5441C9.77249 20.3249 9.76125 19.5982 9.76125 18.8254C7.5 19.2522 6.915 18.2602 6.735 17.7412C6.63375 17.4759 6.19499 16.6569 5.8125 16.4378C5.4975 16.2647 5.0475 15.838 5.80124 15.8264C6.51 15.8149 7.01625 16.4954 7.18499 16.7723C7.99499 18.1679 9.28875 17.7758 9.80625 17.5335C9.885 16.9337 10.1212 16.53 10.38 16.2993C8.3775 16.0687 6.285 15.2728 6.285 11.7432C6.285 10.7397 6.63375 9.9092 7.20749 9.26326C7.1175 9.03257 6.8025 8.08674 7.2975 6.81794C7.2975 6.81794 8.05125 6.57571 9.77249 7.76377C10.4925 7.55615 11.2575 7.45234 12.0225 7.45234C12.7875 7.45234 13.5525 7.55615 14.2725 7.76377C15.9937 6.56418 16.7475 6.81794 16.7475 6.81794C17.2424 8.08674 16.9275 9.03257 16.8375 9.26326C17.4113 9.9092 17.76 10.7281 17.76 11.7432C17.76 15.2843 15.6563 16.0687 13.6537 16.2993C13.98 16.5877 14.2613 17.1414 14.2613 18.0065C14.2613 19.2407 14.25 20.2326 14.25 20.5441C14.25 20.7863 14.4188 21.0746 14.8688 20.9824C16.6554 20.364 18.2079 19.1866 19.3078 17.6162C20.4077 16.0457 20.9995 14.1611 21 12.2276C21 7.12937 16.9725 3 12 3Z">&lt;/path>
&lt;/svg>
GitHub Repo&lt;/span>
&lt;div class="hextra-card-subtitle hx:line-clamp-3 hx:text-sm hx:font-normal hx:text-gray-500 hx:dark:text-gray-400 hx:px-4 hx:mb-4 hx:mt-2">GitHub repository&lt;/div>&lt;div class="hextra-badge hextra-card-tag">
&lt;div class="hx:inline-flex hx:gap-1 hx:items-center hx:rounded-full hx:px-2.5 hx:leading-6 hx:text-[.65rem] hx:border hx:text-gray-600 hx:bg-gray-100 hx:dark:bg-neutral-800 hx:dark:text-neutral-200 hx:border-gray-200 hx:dark:border-neutral-700">&lt;svg height=12 xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" aria-hidden="true">&lt;path stroke-linecap="round" stroke-linejoin="round" d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z"/>&lt;/svg>no-release&lt;/div>
&lt;/div>
&lt;/a>
&lt;/div>
&lt;div class="hextra-cards hx:mt-4 hx:gap-4 hx:grid not-prose"
style="--hextra-cards-grid-cols: 3;">&lt;a
class="hextra-card hx:relative hx:group hx:flex hx:flex-col hx:justify-start hx:overflow-hidden hx:rounded-lg hx:border hx:border-gray-200 hx:text-current hx:no-underline hx:dark:shadow-none hx:hover:shadow-gray-100 hx:dark:hover:shadow-none hx:shadow-gray-100 hx:active:shadow-sm hx:active:shadow-gray-200 hx:transition-all hx:duration-200 hx:hover:border-gray-300 hx:bg-transparent hx:shadow-xs hx:dark:border-neutral-800 hx:hover:bg-slate-50 hx:hover:shadow-md hx:dark:hover:border-neutral-700 hx:dark:hover:bg-neutral-900"href="https://github.com/info-telecom-strasbourg/InsidePSBS"
target="_blank" rel="noreferrer">
&lt;span class="hextra-card-icon hx:flex hx:font-semibold hx:items-start hx:gap-2 hx:pt-4 hx:px-4 hx:text-gray-700 hx:hover:text-gray-900 hx:dark:text-neutral-200 hx:dark:hover:text-neutral-50">&lt;svg fill="currentColor" viewBox="3 3 18 18">
&lt;path d="M12 3C7.0275 3 3 7.12937 3 12.2276C3 16.3109 5.57625 19.7597 9.15374 20.9824C9.60374 21.0631 9.77249 20.7863 9.77249 20.5441C9.77249 20.3249 9.76125 19.5982 9.76125 18.8254C7.5 19.2522 6.915 18.2602 6.735 17.7412C6.63375 17.4759 6.19499 16.6569 5.8125 16.4378C5.4975 16.2647 5.0475 15.838 5.80124 15.8264C6.51 15.8149 7.01625 16.4954 7.18499 16.7723C7.99499 18.1679 9.28875 17.7758 9.80625 17.5335C9.885 16.9337 10.1212 16.53 10.38 16.2993C8.3775 16.0687 6.285 15.2728 6.285 11.7432C6.285 10.7397 6.63375 9.9092 7.20749 9.26326C7.1175 9.03257 6.8025 8.08674 7.2975 6.81794C7.2975 6.81794 8.05125 6.57571 9.77249 7.76377C10.4925 7.55615 11.2575 7.45234 12.0225 7.45234C12.7875 7.45234 13.5525 7.55615 14.2725 7.76377C15.9937 6.56418 16.7475 6.81794 16.7475 6.81794C17.2424 8.08674 16.9275 9.03257 16.8375 9.26326C17.4113 9.9092 17.76 10.7281 17.76 11.7432C17.76 15.2843 15.6563 16.0687 13.6537 16.2993C13.98 16.5877 14.2613 17.1414 14.2613 18.0065C14.2613 19.2407 14.25 20.2326 14.25 20.5441C14.25 20.7863 14.4188 21.0746 14.8688 20.9824C16.6554 20.364 18.2079 19.1866 19.3078 17.6162C20.4077 16.0457 20.9995 14.1611 21 12.2276C21 7.12937 16.9725 3 12 3Z">&lt;/path>
&lt;/svg>
InsidePSBS&lt;/span>
&lt;div class="hextra-card-subtitle hx:line-clamp-3 hx:text-sm hx:font-normal hx:text-gray-500 hx:dark:text-gray-400 hx:px-4 hx:mb-4 hx:mt-2">Application de TPS et BS&lt;/div>&lt;div class="hextra-badge hextra-card-tag">
&lt;div class="hx:inline-flex hx:gap-1 hx:items-center hx:rounded-full hx:px-2.5 hx:leading-6 hx:text-[.65rem] hx:border hx:text-gray-600 hx:bg-gray-100 hx:dark:bg-neutral-800 hx:dark:text-neutral-200 hx:border-gray-200 hx:dark:border-neutral-700">&lt;svg height=12 xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" aria-hidden="true">&lt;path stroke-linecap="round" stroke-linejoin="round" d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z"/>&lt;/svg>no-release&lt;/div>
&lt;/div>
&lt;/a>
&lt;/div></description></item><item><title>Marco TPS</title><link>https://antoninpvr.github.io/hardware/marco/</link><pubDate>Thu, 16 Mar 2023 11:23:00 +0200</pubDate><guid>https://antoninpvr.github.io/hardware/marco/</guid><description>
&lt;img src="https://antoninpvr.github.io/hardware/marco/featured_hu_7cc3a49a2a6a06d3.jpg" width="640" height="455"/></description></item></channel></rss>